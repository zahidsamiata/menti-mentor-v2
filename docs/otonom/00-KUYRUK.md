# 00-KUYRUK — Otonom İş Kuyruğu (v2)
Güncelleme: 2026-09-10 · Sahip: PO (Zahid)

**🔄 YAŞAYAN** (canonical: **TEK aktif iş kaynağı** — otonom motorun okuduğu tek iş listesi)
> ⭐ **Bu belge projenin TEK iş kuyruğudur (2026-09-21).** `10-yol-haritasi.md` ve `00-CIKIS-PLANI.md` 📸 donduruldu; `00-ONCELIK-SIRASI-2026-08-28.md`'nin açık kalemleri **AŞAMA F**, PO'nun "EN ÖN SIRA" içerik bloğu **AŞAMA I**, yol-haritası/karar-takibi devri **AŞAMA Y** olarak buraya alındı.
> ⛔ **Yeni iş başka hiçbir belgeye açılmaz.** Tek istisna kod-dışı işler: `docs/otonom/03-PO-ELLE-ISLER.md`.
Ajan bu dosyayı OKUR, yalnız `Durum` ve `Not` kolonlarını günceller. İş EKLEMEZ.

## Kapılar
🟢 **YAP+MERGE** — yap, doğrulama listesi tam ✅ ise merge et, canlıya al
🟡 **YAP+PR** — yap, PR aç, merge etme (riskli/geniş)
🔴 **KARAR BEKLER** — ilgili KARAR cevapsızsa DOKUNMA, atla, sonrakine geç

> ⚠️ GÜNCELLEME (2026-09-19, kapı politikası gevşetildi — PO): **Varsayılan kapı artık 🟢'dır.**
> Bir iş yalnız ŞU ÜÇ İSTİSNADAN birine giriyorsa 🟡 kalır (yoksa 🟢 yapılır):
>  1. **MIGRATION / SEED** gerektiriyorsa
>  2. **HASSAS DOSYA'ya dokunuyorsa:** auth/yetkilendirme (guard·middleware·token·session) · KVKK/rıza/aydınlatma/veri silme-dışa aktarma · matching/eşleştirme motoru/skorlama
>  3. **Canlı veriye GERİ DÖNÜLMEZ dokunuyorsa**
> Belirsizse 🟡 kalır. 🔴 (KARAR bekleyen) işler bu gevşetmeden **ETKİLENMEZ.**
> Gerekçe (PO): gerçek kullanıcı ~sıfır · her iş ayrı PR (tek tek revert edilebilir) · `npm run verify` + CI kapısı var.

## ⛔ ÇIKIŞ BLOKERİ işareti (2026-09-21)

Not sütununda **`⛔ ÇIKIŞ BLOKERİ (T…)`** gören iş, **canlıya çıkışı tek başına engeller** — kapı renginden (🟢/🟡/🔴) **bağımsızdır** ve `OTONOM-PROMPT.txt` §4 sırasında **0. öncelikte**, hepsinden önce işlenir.

**Test** (kaynak: `00-CIKIS-PLANI.md` katı testi — o belge 📸 donduruldu ama **ölçüt yerinde kalır**):
- **T1** — yasa/sızıntı/geri alınamaz veri kaybı
- **T2** — ana akış kırılır (başvuru → onay → davet → giriş → eşleşme → randevu)
- **T3** — sessiz yanlış: kimse fark etmez, sistem "çalışıyor" görünür

⚠️ **Varsayılan = ERTELE.** Bir işin bloker olduğu **KANITLANIR**; şüpheyle işaretlenmez.
**Sayı (2026-09-21):** 24 çıkış blokeri — **14'ü ajanda** (bu dosyada işaretli), **10'u PO'da** (`docs/otonom/03-PO-ELLE-ISLER.md` en üstteki tablo). Kaynak: `docs/raporlar/kesif/devir-analizi-2026-09-21.md` §11.

## "Bitti" tanımı
1. **Kullanıcı görüyor** — ekranda bir şey değişti ya da bir hata kayboldu. "Backend hazır" sayılmaz.
2. **Testler yeşil** — mevcutlar + yeni davranış için en az bir test
3. **02-ILERLEME.md'ye yazıldı** — ne yapıldı, dosyalar, PR, "kullanıcı artık şunu görüyor"

## ⛔ K-20 ZAMANLAMASI — belge senkronu gerçekten EN SON
K-20 PR'ı, diğer TÜM PR'lar merge edildikten SONRA hazırlanır.
Bir iş merge edilmeden belgeye "CANLIDA" YAZILMAZ; "PR'DA" da yazılmaz —
o iş belge senkronuna kadar beklenir.
Gerekçe (Tur 1'de yaşandı): K-20 PR'ı K-02 merge olmadan hazırlandı, sonra
K-02 merge edildi ama 09-DURUM'a dönülmedi → aynı commit içinde iki belge çelişti.
Kontrol: K-20'yi açmadan önce `gh pr list --state open --author @me` boş olmalı.

## ⏱️ CI BEKLERKEN BOŞ DURMA
CI koşarken poller döngüsüne girme (`sleep`, tekrarlı `gh pr checks`).
CI beklerken SIRADAKİ işin kök sebep analizine başla; merge'leri topluca yap.
Gerekçe (Tur 1'de yaşandı): 32 dakikanın kayda değer kısmı CI beklemekle geçti.
İstisna: turun SON PR'ı (K-20) — orada beklemek doğru, yapılacak başka iş yok.

## Durum kodları
BEKLIYOR · CALISILIYOR · BITTI · ATLANDI(karar) · BASARISIZ · IPTAL(PO)

---

## ŞERİT DAĞILIMI (paralel yazma — dosya sahipliği)

| Şerit | Sahip olduğu alan | İşler |
|---|---|---|
| **Ş1 · Menti akışı** | `app/(dashboard)/disc-test/**` · `app/(dashboard)/learning-journey/**` · `app/(dashboard)/menti/**` · DISC/test bileşenleri | K-02, K-06, K-07, K-09 |
| **Ş2 · Randevu** | `app/(dashboard)/book-meeting/**` · `app/(dashboard)/mentor/availability/**` · `components/organisms/MeetingScheduler.tsx` · `backend/src/controllers/meetingController.ts` · `backend/src/routes/meetingRoutes.ts` | K-03, K-05, K-15 |
| **Ş3 · Profil & KVKK** | `app/(dashboard)/profile/**` · `lib/api/profile.ts` · `backend/src/controllers/onboardingController.ts` · KVKK/veri sayfaları · fotoğraf yükleme | K-04, K-08, K-12, K-17 |
| **Ş4 · Admin & altyapı** | `app/(dashboard)/admin/**` · `backend/src/routes/adminRoutes.ts` · `backend/src/server.ts` · güvenlik ara katmanları · tema/global CSS | K-11, K-14, K-10 |
| **Ş0 · Sıralı** | Birden fazla şeridi ilgilendiren, ortak dosyaya dokunan işler. TEK BAŞINA çalışır, diğerleri beklemez ama onun dosyasına dokunmaz. | K-01, K-13, K-16, K-18, K-19, E-1..E-4, K-20 |

⚠️ Bir iş kendi şeridinin dışına çıkmak zorundaysa → Ş0'a taşı, Not'a yaz.

---

## ⛔⛔ EN ÜST — KİMLİK SAHTECİLİĞİ (GÜVENLİK KONSEYİ ①②, 2026-09-21)

> ⭐ **AŞAMA I dâhil her işten ÖNCE.** Güvenlik konseyinin iki numaralı bulgusu: kimlik istek **gövdesinden**
> alındığı için bir kullanıcı **başkasının adına** hareket edebiliyor. İkisi de canlıda, ikisi de sömürüsü basit.
> Kaynak: `docs/raporlar/kesif/konsey-guvenlik-kvkk-2026-09-21.md` §0①/②.
> ⚠️ **Bu iki satır bu turda KODLANMIYOR** — ayrı bir bulut turu (`otonom/BD-...`) düzeltme PR'ini hazırlıyor.
> Bu kuyruk satırları işin **görünür kalması** içindir; o PR merge edilince `BITTI`'ya çekilir.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| GV-01 | Ş0 | **⛔ Puanlama geri bildiriminde kimlik istemciden alınıyor — bir kullanıcı tanımadığı iki kişinin mentörlük ilişkisini sonlandırabiliyor.** Aynı uç gerçek geri bildirimin üzerine yazıyor ve kayda sahte rol basıyor. | 🟡 | Kullanıcı artık başkasının eşleşmesine geri bildirim yazamıyor ve başkasının mentörlüğünü bitiremiyor; kimlik ve rol yalnız oturumdan alınıyor | BEKLIYOR | ⛔ ÇIKIŞ BLOKERİ · 🔴 KİMLİK SAHTECİLİĞİ · ayrı bulut turu (otonom/BD-...) düzeltme PR'i hazırlıyor — o PR merge edilince BITTI · 🟡 auth+matching. **G-2 · güvenlik konseyi §0①/§3.1.** Kanıt (orkestratör teyitli): `sjtScoringController.ts:39-40` (`fromUserId`+`role` gövdeden) · `:255` `{...parsed.data, tenantId}` · `feedback.service.ts:26-32` yalnız tenant · **`:61-65` `match.updateMany({status:'EARLY_EXIT'})`**. Karşı örnek aynı dosyada `:230-231`. Şiddet: 🔴 KRİTİK |
| GV-02 | Ş0 | **⛔ Görüşme değerlendirmesine sahiplik kapısı yok — herkes başkasının görüşmesine puan yazabiliyor.** Hedef mentörün kalıcı kalite katsayısı düşürülebiliyor, hedef mentiye oryantasyon kilidi bastırılabiliyor, gerçek taraflar bir daha yazamıyor. | 🟡 | Kullanıcı artık yalnız kendi katıldığı görüşmeyi değerlendirebiliyor; başkasının görüşmesine yazma denemesi reddediliyor | BEKLIYOR | ⛔ ÇIKIŞ BLOKERİ · 🔴 KİMLİK SAHTECİLİĞİ · ayrı bulut turu (otonom/BD-...) düzeltme PR'i hazırlıyor — o PR merge edilince BITTI · 🟡 auth. **G-1 · güvenlik konseyi §0②/§3.1.** Kanıt: `meetingRoutes.ts:86` her role açık; `feedbackController.ts:30-104` içinde `req.auth` **0 kez**; uç `mentorUserId`/`mentiUserId`'yi `:38`'de **zaten seçiyor**. Karşı örnek aynı dosyada `:122-128`. Etki: `:83` kalite katsayısı · `:92-96` oryantasyon kilidi · `:69-72` 409 kilidi. ⚠️ `tests/` altında sahiplik testi **YOK** → düzeltmeyle birlikte test. ⚠️ **V-15 ile zincirli** (kilidi basan yer burası) |

---

## AŞAMA I — İÇERİK (PO "EN ÖN SIRA", 2026-09-03)

> ⭐ **Bu aşama kuyruğun EN ÜSTÜNDE.** PO kararı (2026-09-03, `10-yol-haritasi.md:69`): *"bu testler bizim için en önemli işler"* → **EN ÖN SIRA.**
> 🔴 **Neden buraya yeni eklendi:** blok `10-yol-haritasi` ve `00-KARAR-TAKIP` B.1'de duruyordu ama **`00-KUYRUK.md`'ye hiç girmemişti.** Otonom motor yalnız kuyruğu işlediği için (`OTONOM-PROMPT.txt:33-37`) **21 gün hiçbir tura girmedi.** Kaynak: `docs/raporlar/kesif/devir-analizi-2026-09-21.md` §0.2.
>
> **SAYILAN BİRİM (KURAL 16):** "kalem" = `00-KARAR-TAKIP.md` B.1'de (satır 287-309) **kendi numarası olan madde** = madde 138…160 → **23 madde**. Bunların **3'ü zaten CANLIDA** (143·144·145), **2'si mevcut K-18'in kapsamında** (147·148) → **kuyruğa yeni giren: 18 satır (I-01…I-18)**.
> ⚠️ **Durumlar bu turda koda karşı TEK TEK doğrulandı** (backend `b5415bd` · çatı `a8cec0f`); kaynak raporun 5 maddedeki iddiası **çürüdü** ve burada düzeltilmiş hâliyle yazıldı (149·150·155·157·158 → ⬜ değil **🟡 YARIM**). Rapor terim taraması yapmış, davranış taraması yapmamıştı.
>
> ✅ **ZATEN CANLIDA — satır açılmadı (kod+test kanıtlı, 2026-09-21 teyidi):**
> · **madde 143** şık karıştırma → `frontend/src/lib/shuffle.ts:9` · `ScenarioGuideEngine.tsx:21,67,94,115-117` · test `__tests__/ScenarioGuideEngine.test.tsx:83-86`
> · **madde 144** nötr geri bildirim → `ScenarioGuideEngine.tsx:71-75,190-192` · `NeutralReveal` `:270-307` · test `:99-111`
> · **madde 145** yolculuk cevabı profile yazılmıyor → `backend/src/services/learningJourney.service.ts:169-176` · regresyon testi `tests/learning-journey.test.ts:116`
> · **madde 164** kritik konu eşiği `>= 2` → `backend/src/services/certification.service.ts:72-73`
> ⚠️ **madde 147+148** = mevcut **K-18** (öğrenme yolculuğu seed) kapsamında — yeni satır AÇILMADI, K-18 Not'una bakın.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| I-01 | Ş2 | **madde 31+151 — eşleşen taraflar birbirine nasıl yaklaşacağını hiçbir yerde okumuyor.** 8 hazır metin (4 mentöre + 4 mentiye) YAZILI ama ekranda gösterilmiyor. | 🟢 | Eşleşme kurulunca iki taraf da karşısındakine nasıl yaklaşacağını okuyor | BEKLIYOR | ⭐ **İÇERİK HAZIR — iş "sıfırdan yaz" değil "hazır metni bağla".** ⚠️ **İÇERİK KONSEYİ TEYİDİ (2026-09-21): metin ✅ 8/8 TAM** (yer tutucu/editör notu yok, redaksiyonsuz ekrana konabilir) — **AMA bağlanamaz durumda:** 8 adın hiçbirinin kod değerine (`M1..M4`/`m1..m4`) **eşlemesi yazılı değil** ve "Kâşif" adı üç ayrı anlamda kullanılıyor (canlı DISC kartı C harfi `onboardingController.ts:94,97-98` ↔ canonical mentör M2 `03-psikometri-ve-algoritma.md:14` ↔ yeni menti arketipi `arketip-ve-yaklasim-icerigi-2026-09-03.md:53`). **Ad↔kod eşlemesi bir ÜRÜN KARARIDIR** → I-15/KARAR-10 ile birlikte. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md` §0② / §2.1③. 8/8 metin: `docs/raporlar/icerik/arketip-ve-yaklasim-icerigi-2026-09-03.md:307` (başlık) · mentöre `:322-363` · mentiye `:364-405`. Kod kanıtı (⬜): 5 terim (`approachGuide`·`mentorApproach`·`approachText`·`yaklasim`·`yaklaşım`) × BE `src/`+`prisma/` + FE `src/` harf duyarsız → 11 satır, **0'ı alakalı**. madde 152'den BAĞIMSIZ. Migration yok. ⚠️ = içerik konseyi §2.1, ek bulgu: metin 8/8 TAM doğrulandı (yer tutucu yok, editör notu yok, redaksiyonsuz konabilir) — ama iş "S" değil: şema/seed gerekmiyor, buna karşılık metni gösterecek **eşleşme-detay ekranı FE'de hiç yok** (metin S, uçtan uca **L**) ve ön koşul **ad↔kod eşlemesi** (kart §6#1) — anahtarlar `M1..M4`/`m1..m4`, yeni 8 ad için 0 eşleme. |
| I-02 | Ş1 | **madde 141 — karakter kartı üç sorudan ÖNCE gösteriliyor, karar tersini söylüyor.** Kullanıcı ödül anını form doldurmadan görüyor, etki kayboluyor. | 🟢 | Kullanıcı üç soruyu bitirdikten SONRA karakter kartını görüyor | BEKLIYOR | Yalnız FE. Kanıt (2026-09-21 teyitli): `frontend/src/app/onboarding/_OnboardingContent.tsx:34` `STEPS=['Profil','Mizaç Testi','Sonuç','Tercihler']` · `:174` `step===2 && 'Sonucun Hazır! 🎉'` · `:175` `step===3 && 'Son Birkaç Soru'` · kart `:223-224` · `ThreeQuestionsStep` `:227-228`. ⚠️ **Metin değil AKIŞ SIRASI** işi. Kart İÇERİĞİ madde 140'a bağlı (motor bekliyor) → bu iş yalnız sıra + araya tek cümle. Akış kararı yazılı: `arketip-...md:114-141`. |
| I-03 | Ş1 | **madde 150 — sınavı geçemeyen zayıf konusunu ÖĞRENMİYOR.** Sonuç ekranı yalnız **sayı** basıyor ("Pekiştirilecek konu sayısı: N"); konu ADLARI ve gidilecek öğrenme aşaması yok. | 🟢 | Sınav sonunda kişi zayıf konusunun ADINI ve gideceği öğrenme aşamasını görüyor | BEKLIYOR | 🟡 **YARIM — rapor "yok" diyordu, ÇÜRÜDÜ.** VAR (BE): `certification.service.ts:97-102` `CertTopicResult{topic,isRedLine,firstScore,passed}` + `:117` `topicResults` dönüyor. VAR (FE kısmen): `mentor/certification/page.tsx:186` `failed=topicResults.filter(t=>!t.passed)` → `:211-215` yalnız `failed.length`. YOK: konu adları + aşama linki (yolculuk linki `:324-335` yalnız cooldown bloğunda). **Yeni alan GEREKMEZ.** ⚠️ = içerik konseyi §6.4, ek bulgu: aynı ekranda ikinci ve daha ağır bir hata var — `failReason` hiç okunmadığı için **kriz konusundan elenen mentör, %80 üstü puan almışken "en az %80 gerekli" görüyor** (`mentor/certification/page.tsx:205-209`) → **IC-04** ile SIRALI, tercihen tek PR. |
| I-04 | Ş1 | **madde 149 — sınavda 4 kritik konu garantisi TASARLANMIŞ mekanizmayla değil, tesadüfen sağlanıyor.** Çekim/örnekleme algoritması yok; tüm aktif sorular dönüyor. | 🟢 | 4 garantili + 4 rastgele çekim açıkça uygulanıyor; soru havuzu büyüyünce de garanti bozulmuyor | BEKLIYOR | 🟡 **YARIM — rapor "garanti yok" diyordu, ÇÜRÜDÜ.** Bugün garanti **fiilen var** ama kırılgan: `certification.service.ts:288-316` `getCertificationQuestions` **örnekleme YAPMIYOR**, tüm aktif soruları döndürüyor → 4 kritik konu zaten hep geliyor; red-line konular kapatılamıyor (`:382,405-408`); seed'de tam 4 red-line konu var (`seed-certification.ts`: `yapici-geri-bildirim`·`sinir-koyma`·`gizlilik-guven`·`kriz-yonetimi` = PO'nun saydığı 4). ⚠️ **Havuz büyüyünce garanti KENDİLİĞİNDEN bozulur** — iş budur. Eşik ayağı (madde 164) zaten kodda. Seed'e bağlı DEĞİL. |
| I-05 | Ş1 | **madde 156 — görüşme sıklığı yalnız anlaşma sayfasında.** Profilde ve bekleme metninde görünmüyor. | 🟢 | Menti görüşme sıklığını profilinde ve beklerken görüyor | BEKLIYOR | Kanıt VAR: `menti/agreement/[id]/page.tsx:13` `FREQ_LABEL` · `:77-78` gösterim. YOK: `profile/page.tsx` içinde `sıklı|frequency` → **0**; bekleme banner'ı `menti/page.tsx:199-203` → yok. Efor S, migration yok. |
| I-06 | Ş0 | **madde 160 — iptal edilmiş karar hâlâ geçerli görünüyor.** "İsimler unisex, karşı taraf isimsiz" kararı iptal edildi ama belgede üstü çizilmedi. | 🟢 | Eski karar `[ESKİ]` damgalı, okuyan yanılmıyor | BEKLIYOR | **BELGE işi.** Kanıt (2026-09-21 teyitli): `docs/kararlar/konu/degerlendirme-sistemi-tasarim-2026-08-27.md:410-411` — üstü çizili DEĞİL, `[ESKİ]` damgası YOK, faz6 §4'e yönlendirme YOK. G9-03 deseni (SİLME YOK). |
| I-07 | Ş1 | **madde 157 — yanlış yapılan konu tekrar sınavda MUTLAKA gelmiyor.** Ağırlıklı öne alma var, zorunluluk ve "diğer varyantıyla sor" yok. | 🟢 | Yanlış yapılan konu bir sonraki denemede mutlaka ve farklı varyantıyla çıkıyor | BEKLIYOR | 🟡 **YARIM — rapor "yok" diyordu, ÇÜRÜDÜ.** VAR: `certification.service.ts:232-234` `wrongTopics` · `:245` `certWrongTopics` DB'ye yazılıyor · `:284-286,309-316` `priorityTopics` → yanlış konular listenin BAŞINA (`weight()` stabil sıralama). YOK: konunun listede kalma **garantisi** + "diğer varyant" zorlaması. Rapor `attemptLimit`·`dailyAttempt`·`guaranteed` aradı (0 sonuç doğru) ama kod bu işi başka isimlerle yapıyor. |
| I-08 | Ş1 | **madde 158 — deneme sınırı GÜNLÜK değil.** Kural "her 2 başarısız denemede 24s bekleme"; madde "günde 2 deneme" istiyor (aynı gün ezberlenmesin). | 🟡 | Aynı takvim gününde 2'den fazla deneme yapılamıyor | BEKLIYOR | 🟡 KALIR: deneme sayacına **takvim-günü** alanı = migration olası. 🟡 **YARIM — rapor "disiplin yok" diyordu, ÇÜRÜDÜ.** VAR: `certification.service.ts:30` `attemptsBeforeCooldown:2` · `:32` `cooldownHours:24` · `:144-151` cooldown kapısı + `COOLDOWN_ACTIVE` · `:221-229` sayaç yazımı · FE bekleme mesajı **ve** yolculuğa yönlendirme `mentor/certification/page.tsx:65-66,149-152,326-335`. FARK: `:226-227` `newAttempts % 2 === 0` — takvim-günü sıfırlaması yok. |
| I-09 | Ş1 | **madde 146 — isim değişkeni altyapısı (14 değişken).** Metinlerdeki kişi isimleri koda gömülü; kurum kendi adlarını kullanamıyor. | 🟡 | Kurum senaryolardaki isimleri kendi bağlamına göre değiştirebiliyor | BEKLIYOR | 🟡 KALIR: tenant override alanı ❓ migration. Kanıt: 9 terim (`menti_denge`·`menti_rotaci`·`menti_izacan`·`menti_kasif`·`mentor_mimar`·`mentor_ayna`·`mentor_liman`·`mentor_pusula`·`sert_1`) × iki repo tamamı, harf duyarsız → **kodda 0 dosya** (7 isabetin hepsi `.md`). İçerik hazır: `menti-yolculugu-ve-eslesme-metinleri-2026-09-03.md:56`. ⚠️ **SIRA ÖNEMLİ:** K-16/K-18 seed'inden ÖNCE yapılırsa isimler seed'e değişken girer; SONRA yapılırsa **ikinci kez seed** gerekir → **PO'ya sor** (kart: KARAR-30). |
| I-10 | Ş2 | **madde 154 — bekleyen talep süresiz asılı kalıyor.** 3. gün hatırlatma + 7. gün kapanış + alternatif; 3 metin YAZILI, cron yok. | 🟡 | Menti 3. günde hatırlatma, 7. günde alternatif mentör görüyor | BEKLIYOR | 🟡 KALIR: **şema + cron.** ⭐ İÇERİK HAZIR ~~[ESKİ · 2026-09-21] 3/3~~ → ⚠️ **🟡 3/3 YAZILI AMA İKİ AÇIK** (2026-09-21, içerik konseyi): `{n}` **sıfır hâli tanımsız** (belgenin kendi açık kalemi `:420`) · kapanış metni ilk cümlede olumsuzu söylüyor (`:340`) → **ton kararı**. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md` §0② / §2.1. Metin: `menti-yolculugu-...md:316` (`:320` hemen sonra · `:330` üç gün · `:338` yedi gün kapanış). ⛔ **ŞEMA ENGELİ (2026-09-21 teyidi):** `MatchRequest` (`prisma/schema.prisma:439-456`) yalnız `createdAt` tutuyor — **durum/kapanış alanı YOK** → cron yazılamadan önce migration gerekir. Cron listesi (`cronScheduler.ts:410-458`, 8 iş) içinde bekleme zamanlayıcısı yok. ⚠️ U-01 ve P-05 ile aynı `meetingController.ts` → SIRALI. |
| I-11 | Ş1 | **madde 151+152+153 — eşleşme detay sayfası yok.** Bugün yalnız tek cümlelik "neden uyumlu" var. | 🟡 | Menti "neden bu mentör · nasıl çalışırsınız · ilk görüşmede ne konuşulur" bölümlerini görüyor | BEKLIYOR | 🟡 KALIR: **içerik ön koşullu** — Bölüm 2'nin 16 kombinasyonundan **15'i YAZILMADI**. Katman-1 VAR: `compatibilityReason` render ediliyor (`menti/page.tsx:318`, `mentor/page.tsx:482-484`). Detay rotası YOK (FE dizin listesi teyitli). madde 153: cümle S1/S2/S3'ten DEĞİL, skor eşiğinden üretiliyor (`matchingController.ts:12-15,48-60`; `supportApproach|needStatement|threeQuestion` → 0). ⚠️ **Bölüm 1 ve 3 BAĞIMSIZ ilerler** — PO isterse ikiye bölünsün. ⚠️ S1 (`mentiNeeds`) karşı tarafa GÖSTERİLMEZ, yalnız örtüşme cümlesi. ⚠️ = psikometri konseyi §5 D.2 (KOPMA 3), ek bulgu: üç sorunun (mentiNeeds · mentorStrengths · supportApproach · priorityValue) tek yazanı onboardingController.ts; matching.ts · scoring.ts · scoring.service.ts · sector-scorer.service.ts içinde 0 eşleşme ⇒ veri toplanıyor ama hiçbir skorlama dosyasında okunmuyor (I-11'in "detay sayfası" ayağından ayrı, SKORLAMA ayağı). |
| I-12 | Ş1 | **madde 142 — karakter kartı derinleştikçe yeniden hesaplanmıyor (15→35 soru).** Kart onboarding'de bir kez üretiliyor, sonra hiç tazelenmiyor. | 🟡 | Kullanıcı daha fazla soru cevaplayınca kartı güncelleniyor | BEKLIYOR | 🟡 KALIR: matching/skorlama dosyası. Kanıt (2026-09-21, raporda yoktu — bu turda bulundu): `discResultCard` YALNIZ `backend/src/controllers/onboardingController.ts:465-493`'te üretiliyor; `recalcDiscVector` (`discVectorService.ts:152-155`) `UserProfile.discD..C`'yi günceller ama **`discResultCard`'a dokunmaz**. ⚠️ I-13'ün ölçek düzeltmesinden SONRA anlamlı. |
| I-13 | Ş0 | **⛔ ÖN KOŞUL — OCEAN motoru 100× ölçek hatasıyla ÖLÜ.** `UserProfile.discD/I/S/C` **0-1** yazılıyor, `discToOcean` **0-100** bekliyor → OCEAN çıktısı **[49.75, 50.30]**'a sıkışıyor, eşikler (60/55/45) **matematiksel olarak aşılamıyor**, **8 arketipin 6'sı ölü kod**, herkes fallback M1/m1 alıyor. Hata yok, uyarı yok, log yok. | 🟡 | Kullanıcı kendi verisine göre DEĞİŞEN arketip görüyor (herkes aynı değil) | BEKLIYOR | 🟡 KALIR: matching/skorlama. **NUMARASIZ** (`00-KARAR-TAKIP` F.14 `[aday]`). **Aritmetik (2026-09-21 uçtan uca teyitli):** `discVectorService.ts:130-143` toplamı 1.0'a normalize → `:152-155` `UserProfile`'a 0-1 yazar → `scoring.service.ts:92-97` okur → `disc-to-ocean.adapter.ts:15-16` `clamp(50+(50*raw)/100)`. `disc` olasılık vektörü olduğundan `raw ∈ [min w, max w]`; `scoring.config.ts:23-29` ağırlıklarıyla O[49.85,50.20]·C[49.95,50.30]·E[49.85,50.30]·A[49.75,50.25]·N[49.80,50.05] ⇒ **mutlak sınır [49.75, 50.30]**, `scoring.config.ts:31` `{HIGH:60,MID:55,LOW:45}` → `deriveArchetype:27-43` **her koşul erişilemez**. ⚠️ F-11/KARAR-10'dan **BAĞIMSIZ** — motor kararı ne olursa olsun bu hata düzeltilmeli. ⚠️ I-14 ile **AYNI KÖK** → birlikte yapılmalı. ⛔ **138·139·140 bu düzeltmeden ÖNCE kodlanırsa boşa gider.** ⚠️ = psikometri konseyi §3 B.1, ek bulgu: ölçek hatası bağımsız doğrulandı ve iki yeni zincir çıktı — COMPATIBILITY_MATRIX['M1_m1']=60 (scoring.config.ts:38-44) yüzünden motor bağlanırsa karakter skoru HİÇ ayırt etmez (scoring.service.ts:39-42), BLOCKED_PAIRS={M4:['m3'],M1:['m4']} (scoring.config.ts:33) yüzünden toksik-çift vetosu hiç tetiklenmez (isHardBlocked daima false), discToOcean/deriveArchetype için SIFIR birim testi var (kapsam: tests/, 8 terim → 0) ve onarım şema migration'ı değil backfill/recompute gerektirir (kolon tipleri aynı, kayıtlı ocean*/archetype değerleri çöp). ⚠️ = psikometri konseyi §3 B.2, ek bulgu: "Bitti demek" sütunu bugünkü hâliyle ULAŞILAMAZ — kullanıcının gördüğü arketip DISC harfinden geliyor (onboardingController.ts:464-466 → DiscRecallCard.tsx:57), OCEAN'a hiç uğramıyor; yalnız ölçek düzeltilirse kullanıcı ekranında HİÇBİR ŞEY değişmez (motorun bağlanması I-15/F-11 = 🔴 KARAR-10'a bağlı). |
| I-14 | Ş0 | **madde 169 — iki farklı `DiscVector` tipi.** `scoring.ts:12-18` `{D,I,S,C,confidence}` (BÜYÜK harf, `confidence` zorunlu) ↔ `scoring.config.ts:8-13` `{d,i,s,c}` (küçük harf). Tip sistemi I-13'teki ölçek hatasını **yakalayamıyor**. | 🟡 | (iç kalite — kullanıcı etkisi I-13 ile birlikte görünür) | BEKLIYOR | 🟡 KALIR: matching. madde 169. Kanıt yukarıda; `discVectorService.ts:133-142` büyük-harflisini üretiyor, `scoring.service.ts:92-97` elle küçük harfe map'liyor. Tek tipe indirgenmeli. **I-13 ile birlikte.** ⚠️ = psikometri konseyi §3 B.3, ek bulgu: hatanın tam yeri bulundu — scoring.service.ts:92-97 tek geçiş noktası, adlandırılmış dönüştürücü yok, isimler küçük harfe dönüyor ama sayılar dönmüyor ve iki tarafta da number olduğu için TypeScript bunu YAKALAYAMAZ; üstelik üçüncü bir ölçek yorumu var (tests/compute-profile-idor.test.ts:33 {discD:8,discI:2,discS:5,discC:5} ≈ 0-10) → kastı TEYİT GEREK. |
| I-15 | Ş0 | **madde 138·139·140 — Big Five arketip kartı üçlüsü** (baskın+ikincil boyut · çoklu-arketip "şimdilik" dili · iki katmanlı kart). | 🔴 KARAR-10 | KARAR-10'a göre motor bağlanır, kullanıcı arketip kartını görür | BEKLIYOR | ⛔ **ÖN KOŞUL I-13** — motor ölü; önce ölçek düzeltilmeli. ⛔ **İÇERİK KONSEYİ DÜZELTMESİ (2026-09-21) — ~~[ESKİ] İÇERİK HAZIR~~ ÜÇÜ İÇİN DE GEÇERLİ DEĞİL:** **madde 138** 🟡 8/8 tam ve akıcı AMA *"İz Açan"* adı **PO-onaysız** (belge `:263` bunu kendisi yazıyor) + "Kâşif" çakışması (I-01 notu). **madde 139** ⛔ **4/8 — YARIM:** dört "şimdilik" varyantının dördü de **yalnız mentör tarafı**; menti sürümü **YAZILMAMIŞ**, belge `arketip-ve-yaklasim-icerigi-2026-09-03.md:269` yalnız *"menti tarafında aynı yapı, arketip adları değişerek"* diye **talimat bırakmış**. → Önce **4 menti varyantı yazılacak** (yazım turu), sonra eşik dalı + sabit dizi; eşik sabiti `scoring.config.ts:31` komşusuna (sihirli sayı yazılmaz). Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md` §0② / §2.1. ⭐ Metin kaynağı: 8 arketip kartı `arketip-ve-yaklasim-icerigi-2026-09-03.md:142` (`:153,167,181,195` mentör · `:209,223,237,251` menti) + 4 "şimdilik" varyantı `:267` (`:271,279,287,295`). Kod ⬜: baskın/ikincil hesabı ve <10 eşiği yok; `UserProfile.archetype` FE'de hiç okunmuyor. = F-11'in alt kümesi → PO isterse F-11 Not'una eklenip satır kapatılır. ⚠️ = psikometri konseyi §3 B.4, ek bulgu: KARAR-10 kartına eklenen üç seçenek maliyet tablosu bu satırın karar dayanağıdır; I-13 düzeltilmeden bağlama (seçenek C) RASTGELEDEN BETERDİR çünkü M1_m1=60 sabiti karakter skorunu düzleştirir. ⛔ **BİRLİKTE YAPILMALI — önce silme yolu düzeltilir, SONRA Match yazımı açılır. Ters sıra = KVKK ihlali.** *(karşı taraf: **GV-08** — F-11 ile aynı zincir.)* |
| I-16 | Ş2 | **madde 155 — eşleşme ret ekranı YOK; sebep gizli kalmalı, alternatif aynı ekranda.** ~~[ESKİ · 2026-09-21] 2 metin YAZILI.~~ ⚠️ **2026-09-21 (içerik konseyi): mentör metni ✅ tam hazır (`:374-376`), menti metninde `{n}` boşluğu var (`:362`)** — I-10'la aynı açık. | 🔴 KARAR-22 | Reddedilen menti sebep yerine alternatif mentör görüyor | BEKLIYOR | ⚠️ **RAPOR DÜZELTMESİ (2026-09-21):** rapor "ret ekranı sebep gösteriyor" dedi — **yanlış ekrana bakmış.** Sebep gösteren yer `LoginForm.tsx:119-121` = **kurum üyelik onayı reddi**, eşleşme reddi değil. **Eşleşme reddi akışı hiç yok:** `MatchRequest` (`schema.prisma:439-456`) **durum alanı bile içermiyor** → reddedilemiyor. ⇒ İş "ekranı düzelt" değil, **"akışı kur"** (I-10 ile aynı şema engeli). ⭐ İçerik hazır 2/2: `menti-yolculugu-...md:352` (menti `:356-364`) · `:372` (mentöre). ⚠️ **P-05 ile AYNI konu** → P-05'in içerik/metin ayağı; PO isterse P-05 Not'una eklenip bu satır kapatılır. |
| I-17 | Ş1 | **madde 167 — öğrenme yolculuğunda GİZLİLİK + BİTİRME aşaması yok (8→10).** İki konu sertifikada SINANIYOR ama yolculukta hiç öğretilmiyor → "öğretilmemiş konuda eleme". | 🟡 | Mentör yolculukta gizlilik ve bitirme aşamalarını görüyor | BEKLIYOR | 🟡 KALIR: **SEED** (KARAR + yedek şart; seed çalıştırılmaz). ⛔ **İÇERİK KONSEYİ DÜZELTMESİ (2026-09-21): bu satır "seed işi" diyor ama SEED'E KOYACAK İÇERİK HİÇ YAZILMAMIŞ** → önce **yazım turu**, seed ondan sonra. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md` §0② / §2.1. Kod kanıtı: `prisma/seed-learning-journey.ts` içinde `GIZLILIK`/`BITIRME` → **0 sonuç**; bugünkü sayım MENTOR 7 + MENTI 6 (`:7` yorumu + `:39-299`/`:300-500`). İçerik yazım turu ÖNKOŞUL. ⚠️ K-18 ile aynı seed dosyası → SIRALI. |
| I-18 | Ş0 | **madde 159 — kriz bildirimi akışı yok.** Kendine zarar ifadesinde kimse haberdar olmuyor; sertifikada senaryo olarak SINANIYOR ama canlı karşılığı yok. | 🔴 KARAR-31 + avukat | Kriz ifadesinde kurum yöneticisine bildirim gidiyor | BEKLIYOR | ⛔ **HUKUKİ ÖN KOŞUL.** Kanıt (7 terim, İKİ DİLLİ, harf duyarsız, BE `src/`+`prisma/seed.ts`+FE `src/`): `kriz`·`crisis`·`selfharm`·`self-harm`·`kendine zarar`·`acil durum`·`emergency` → **2 satır, 0'ı akış** (`analyticsEngine.ts:299` iş unvanı listesi · `admin/certification/page.tsx:23` sınav konu etiketi); ayrıca `seed-certification.ts` `topic:'kriz-yonetimi'` = **sınav konusu**, bildirim akışı değil. ⚠️ **G1-01 ÇELİŞKİSİ:** 18 yaş altı menti kabul edilecekse gerçek yaş + veli onayı gerekir → "18+ beyanı yeterli" çöker. Avukat paketine TEK SORU. |

---

## AŞAMA A — Migration'sız, kullanıcının hemen göreceği işler

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| K-01 | Ş0 | **Submodule pointer re-bump.** Çatı main'deki backend pointer `02129fe`, backend main HEAD `1304790`. İçerik aynı, defter düzeltmesi. | 🟢 | pointer == backend main HEAD | BITTI | PR #176 merged. pointer→1304790, ata teyitli, CI 8/8. |
| K-02 | Ş1 | **/disc-test boş sayfa.** "DISC Testini Güncelle" → sayfa yükleme iskeletinde takılıyor. Üç şıktan hangisi: istek atılmıyor / hata catch loading'i kapatmıyor / boş-durum ekranı yok. | 🟢 | /disc-test açılıyor, test başlıyor | BITTI | PR #179 merged (CI 8/8). Kök: page:86 loading==questions.length; hata sonsuz iskelet. loading bayrağı+reload+hata/boş ekran. Test 3/3. |
| K-03 | Ş2 | **Müsaitlik çoklu aralık.** Pzt + Cuma eklenince yalnız Cuma kalıyor. Backend diziyi olduğu gibi yazıyor (meetingController.ts:342-360) → şüphe FE'de. | 🟢 | 2+ aralık eklenip listede görünüyor | BITTI | PR #191 (O) merged, CI 8/8. CANLIDA BAK: mentör Pzt 09:00-17:00 + Cuma 11:00-17:00 ekleyince İKİSİ de kalıyor. |
| K-04 | Ş3 | **Fotoğraf yükleme sunucu hatası.** ÖNCE `docs/kararlar/dokploy-foto-volume-talimati.md` oku. Kod mu (dizin/izin/hata yutma) altyapı mı (volume yok)? Kod ise düzelt; altyapı ise 01-KARARLAR'a PO için TAM talimat + anlaşılır hata mesajı. | 🟢 | Kod: yükleme çalışıyor · Altyapı: talimat yazıldı + hata mesajı anlaşılır | BITTI | PR #85(BE)+#229(pointer) merged, CANLIDA. Kök neden ALTYAPI (kalıcı disk yok → EACCES): PO talimatı `03-PO-ELLE-ISLER` Bölüm A#1'de detaylı ZATEN yazılı. Agent parçası: yazma hatası jenerik 500 yerine **503 + 'Fotoğraf kaydedilemedi'** (iç detay sızmaz, operatöre loglanır). CANLIDA BAK: disk yoksa kullanıcı anlaşılır mesaj görüyor. 1 test. ⚠️ Kalıcı disk = PO adımı. | = G8-01/G8-02 foto volume (Faz 0 altyapı → KARAR-18). · **= W §4#1** (avatar kalıcı disk YOK: `docker-compose` `volumes` yok, `UPLOAD_DIR` tanımsız, `Dockerfile` `chown` yok → her deploy'da foto siliniyor, `avatarUrl` DB'de kalıp kırılıyor; PO adımı → `03-PO-ELLE-ISLER.md`). Ayrıca 2026-09-09 "sunucu hatası"nın muhtemel kök nedeni (uid 1001 `/app` yazma izni). |
| K-05 | Ş2 | **Menti randevu — migration'sız yarısı.** Müsaitlik formdan ÖNCE gösterilsin; menti serbest tarih yerine mevcut aralıklardan seçsin; 409 ("saat müsaitliğe uymuyor") anlaşılır Türkçe gösterilsin. `MeetingScheduler.tsx:144 MentiBooking` slot seçtiriyorsa bağla, yoksa yaz. Format/süre şimdilik mentide. | ~~🟡~~ 🟢 | Menti müsait olmayan saati SEÇEMİYOR | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2)** — menti müsait olmayan saati seçebiliyor, 409 anlaşılmaz — randevu halkası kırık · (kapı gevşetildi 2026-09-19: migration'sız, FE düzeltmesi; auth/KVKK/matching değil) · **= X §6#1 / §10#1,#2** (bookMeeting 409/400 yanıtına `message` ekle + FE boş-müsaitlik uyarısı — `book-meeting/page.tsx:57`, `meetingController.ts:483/500/506`, `client.ts:25-34`) |
| K-06 | Ş1 | **Öğrenme yolculuğu — diğer şıkların açıklaması.** Şu an yalnız seçilenin açıklaması görünüyor. | 🔴 KARAR-29 | 4 şıkın da açıklaması görünüyor | ATLANDI(karar) | Açıklamalar API'de **bilinçli** istemciye yüklenmiyor ("cevap anahtarı sızmasın", `ScenarioGuideEngine.tsx:271`). Değiştirmek pedagojik ürün kararı + backend işi → KARAR-29 açıldı (cevap-sonrası göster/bugünkü/baştan göster). Kapı 🟢→🔴. |
| K-07 | Ş1 | **Şık harfleri.** Şıklar karıştırılıyor, A/B/C/D harfleri orijinal sıraya yapışık kalıyor. Karıştırmadan SONRA yeniden ata. | 🟢 | Üstten alta her zaman A→D | BITTI | PR #177 merged. görünüm harfi=index, kimlik korundu. certification+ScenarioGuideEngine. Test 5/5. |
| K-08 | Ş3 | **Sosyal alan doğrulaması.** LinkedIn alanına YouTube linki kaydedilebiliyor. FE+BE platform doğrulaması (`/api/users/me/profile`). Yeni alan EKLEME. | 🟢 | Yanlış platform linki reddediliyor | BEKLIYOR | |
| K-09 | Ş1 | **Menti panelinde sabit sıfır kartlar.** Gerçek veriye bağla; veri kaynağı yoksa kartı kaldır. Hangisi yapıldı → Not. | 🟢 | Ekranda sahte sayı yok | BITTI | PR #178 merged. 2 kart /api/meetings'e bağlandı (bind, kaldırma değil). mentiMetrics.ts+test 3/3. "Gönderilen Talepler"=oturum sayacı, dokunulmadı. |
| K-10 | Ş4 | **Dark mode kontrast.** Test 3.4'teki okunmayan alanlar. Palet tutarlılığı TEKNİK karar, sorma. | 🟢 | Belirtilen ekranlarda metin okunuyor | BEKLIYOR | ⚠️ **MÜKERRER NOTU (2026-09-21):** F-21 ile kesişir (F-21 kapsamında *"DISC kontrast"* var). **K-10 kalsın** (küçük, tek başına çıkar); F-21 *"aria/role/klavye"* ayağına daralsın.|
| K-11 | Ş4 | **Tenant-admin şikayet inceleme paneli.** `GET/PATCH /admin/reports` var, UI yok → şikayet döngüsü kapanmıyor. Liste + durum değiştirme. | ~~🟡~~ 🟢 | Admin şikayeti görüp durumunu değiştirebiliyor | BITTI | PR #230 merged, CANLIDA. Yeni `/admin/reports` sayfası (açık/incelenen/reddedilen filtre, taraflar+gerekçe, 'İncelendi'/'Reddet'+not) + sol menüye 'Şikayetler'. Backend değişmedi (mevcut uçlar). 3 test. CANLIDA BAK: admin panelde Şikayetler'i açıp durum değiştirebiliyor. |
| K-12 | Ş3 | **KVKK insan-okunur veri özeti.** Mevcut `/users/me/data-export` çıktısını okunur sayfada göster (ham JSON değil). Yeni uç YOK. | 🟢 | Kullanıcı Türkçe veri özeti görüyor | BITTI | PR #220 merged (CI 8/8). Profil→'Verilerim'e 'Görüntüle': ham JSON yerine okunur bölümler (kimlik/profil · etkinlik sayıları · rıza geçmişi). Yeni uç YOK. Saf `summarizeDataExport` 5 test. CANLIDA BAK: kullanıcı verisini okunur özet olarak görüyor. ⚠️ = güvenlik konseyi §2.B.3, ek bulgu: K-12 doğru iş yaptı ama sınırı var — okunur özet backend'in verdiğinden fazlasını üretemez. `exportUserData` (`gdprService.ts:284-333`) yalnız 6 kaynak dönüyor; `UserProfile` (OCEAN/arketip/DISC) HİÇ yok, mesaj yalnız `count` (`:314`). 16 tablo + ≈22 `User` alanı eksik → backend ayağı GV-17. |

## AŞAMA B — Temizlik ve güvenlik

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| K-13 | Ş0 | **Mükerrer uçlar — NİYET + KARANTİNA (silme YOK).** `/users/me/social`, `/system-logs` (↔/platform/logs), tenants CRUD ikilikleri. ⛔ SİLME PROTOKOLÜ (prompt §3.5) uygulanır: her uç için önce **neden yazıldığı** (git log + commit + belge), sonra ikame kanıtı, sonra arşiv belgesi, sonra karantina. **SİLME YAPILMAZ** — KARAR-11 cevabı beklenir. | 🟡 | Her uç için gerekçe + ikame kanıtı + arşiv satırı; karantina PR'da | BEKLIYOR | = G10-01(c) MeetingScheduler bileşeni · G4-09/G4-10 super-admin ikizi (FE 0 kullanım) · G10-23 (silinmiş, çözülmüş). E-2/E-3/E-4 hayalet akışıyla aynı. ⚠️ **KAPI DÜZELTİLDİ (2026-09-21): 🔴 KARAR-11 → 🟡.** Gereksiz fren: teslimat **karantina**, silme DEĞİL — `CLAUDE.md` SİLME PROTOKOLÜ *"Karantina 🟡'dır, gerçek silme 🔴'dır"*. 🔴 kalması tüm hayalet şeridini donduruyordu. ⚠️ **MÜKERRER: E-4 ile aynı teslimat** (gerekçe + ikame kanıtı + arşiv + karantina PR) → **E-4 kalsın**, bu satır onun referansı. ⚠️ = güvenlik konseyi §2.A.2 (G-4), ek bulgu: mükerrer uç listesinde OLMAYAN bir ikiz daha var — `POST /api/meetings` (`createMeeting`, kimlik gövdeden, sahiplik yok) ↔ `POST /api/meetings/book` (`bookMeeting:515`, kimlik token'dan). Eski ikiz AÇIK bir güvenlik açığı taşıyor → GV-06 olarak ayrı satır açıldı; kalıcı çözüm (düzelt mi / karantina mı) KARAR-40'ta. |
| K-14 | Ş4 | **S19 sunucu sertleştirme — kod tarafı.** helmet · rate-limit · CORS · body-limit · güvenlik başlıkları. Altyapı kısmı (HTTPS, firewall, SSH, yedek) → 01-KARARLAR'a PO kontrol listesi. | 🟡 | Kod PR'da · altyapı listesi kararlarda | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — global limit anahtarı doğrulanmamış `X-Tenant-Id`, `trust proxy` yok · 🟡 KALIR: güvenlik ara katmanı (`server.ts` helmet/rate-limit/CORS/body-limit). = G1-28 (altyapı → KARAR-18). ⚠️ F-04 (G1-23 CSP) aynı `server.ts`'e dokunur → SIRALI yap. F-05 (G1-26) ile ilişkili. · **= W §4#3** (⛔ BU TUR YASAK BÖLGE): global limit anahtarı doğrulanmamış `X-Tenant-Id` (`rateLimiter.ts:36`) + `trust proxy` yok → 9 IP limitçisi vekil arkasında bozuk (kendi kendine DoS + `forgot-password` mail-bomb). server.ts rate-limit/trust-proxy düzeltmesi PO'nun başında olduğu ayrı tur. |

## AŞAMA C — Ürün kararı / migration bekleyenler

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| K-15 | Ş2 | **AvailabilityBlock'a format + süre (tam randevu mimarisi).** ⛔ önce `availability_block_yedek_20260910`; migration default'lu (format=ONLINE, durationMin=60); mentör slot açarken format+süre seçer; menti slottan seçer. | 🔴 KARAR-1 | Mentör format+süre belirliyor, menti seçiyor, eski kayıtlar bozulmadı | BEKLIYOR | |
| K-16 | Ş0 | **madde 30 — sertifika bankası seed.** ⛔ önce `certification_option_yedek_20260910`, yalnız `seed-certification`. | 🔴 KARAR-3, KARAR-4 | Sertifika ekranında "Seçenek A" yerine gerçek metin | BEKLIYOR | = G3-08 (seed `seed-certification.ts` şu an **20 senaryo/80 şık** — eski sürüm; finalize **22/88 içeriği** `docs/raporlar/icerik/` 3 belgede yazılı ama seed'e taşınMADI → K-16 bugün seed atarsa 20/80 çıkar, içerik→seed taşıma adımı gerekir) · G3-09 (npm runner yok, `seed-certification.ts:320` tsx guard var). ⚠️ (PR #184'teki "sayı bayat" ifadesi düzeltildi: iki sayı da gerçek, farklı kaynak.) **ÖNKOŞUL: P-99 — içerik seed'e taşınmadan seed atılırsa 20/80 çıkar** ~~[ESKİ · 2026-09-21] (yeni 2 senaryo+8 şık canlıya çıkmaz)~~ → ⚠️ **SAYI DÜZELTMESİ-2 (2026-09-21, içerik konseyi ① — kaynak-teyitli):** ~~[ESKİ · 2026-09-21] fark yalnız **2 senaryo + 8 şık** (20/80 → 22/88)~~ — **BU YANLIŞTI.** Doğrusu: `tam.md` (= seed'in birebir kaynağı, `seed-certification.ts:7`) 20 sahne sundu, finalize içerik bunların **yalnız 5'ini** aldı (%25) — yani seed'deki **20 senaryonun 15'i gerekçeli olarak ELENDİ** ("ELENEN SAHNELER" tabloları: yüzey ayrımı/çelişki). Kalan **5'inin 5'i de yeniden yazıldı** (5/5'inde metin farkı, **birinde puan anlamı TERS DÖNDÜ**). ⛔ Pratik sonuç: taşınacak şık sayısı 8 değil **88'in TAMAMI**; düşecek senaryo **15**. Efor **S değil L**. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md:16-19,118-122`. |
| K-17 | Ş3 | **Sosyal profile serbest bağlantı alanı** (migration). | 🔴 KARAR-2 | Kullanıcı ek link ekleyebiliyor | BEKLIYOR | |
| K-18 | Ş0 | **Öğrenme yolculuğu seed** (madde 147/148). ⛔ önce yedek, `seed-learning-journey`. ⛔ **2026-09-21 (içerik konseyi): madde 147 içeriği 5/5 DOLU AMA HER AŞAMADA `{mentor_*}` YER TUTUCUSU taşıyor** ve bu değişkenlerin kodda **0 karşılığı** var (kapsam: iki repo `src`+`prisma`, harf duyarsız → 0 dosya) → **bugün seed edilirse kullanıcı ekranda ham `{mentor_mimar}` görür.** İki engel: (a) **I-09 / madde 146** isim değişkeni altyapısı, (b) seed'de **zaten 6 farklı menti aşaması var** → üzerine yazma/pasifleştirme **PO KARARI**. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md` §0② / §2.1. | 🔴 KARAR-5 | Canlıda içerik görünüyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2 KOŞULLU)** — yalnız C11 "seed tabloları boş" derse bloker · |
| K-19 | Ş0 | **Kalan ürün kararları uygulaması:** menti mentör listesi · toplantı linki kimde. Her biri ayrı PR. | 🔴 KARAR-8, KARAR-10 | Her karar için ekranda değişiklik | BEKLIYOR | |

## AŞAMA E — ⭐ HAYALET BACKEND: niyet arkeolojisi → triyaj → yapım

Bağlam: Bir denetim "backend'de yazılmış ama kullanıcıya ulaşmamış ~17 blok · ~56 çağrılmayan uç · ~44 tekil kalem" buldu.
PO kararı: **hepsi ele alınacak** — ama "ele almak" hepsini yapmak değil. Önce NEDEN yazıldıkları anlaşılacak.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| E-1 | Ş0 | **NİYET ARKEOLOJİSİ (salt-okuma, sınırsız paralel alt-ajan).** Her kalem için: (1) ne yapıyor, (2) **neden yazılmış** — git log + commit mesajı + ilgili PR + `docs/` içindeki gerekçe, kanıt satırıyla, (3) kime yarar — hangi rol hangi işini kolaylaştırır, (4) bugün hâlâ anlamlı mı, (5) çalışır durumda mı yoksa bozuk mu. Çıktı: `docs/raporlar/kesif/hayalet-envanter-2026-09-10.md`, kalem başına tek satır. ⚠️ "neden yazıldı" bulunamıyorsa "GEREKÇE BULUNAMADI" yaz, UYDURMA. | 🟢 | Her kalem için gerekçe ya da "bulunamadı" | BITTI | Rapor: `hayalet-envanter-2026-09-19.md`. 35 öksüz uç + 4 ölü alan + 3 öksüz bileşen. Eski "~56/~44" şişmişti (gerçek 2026-08-02=6 uç). GEREKÇE BULUNAMADI=1 (Tenant.verifiedBy). 4 paralel alt-ajan. |
| E-2 | Ş0 | **TRİYAJ.** E-1 çıktısındaki her kalemi dört kovadan birine koy: **BAĞLA** (gerçek ürün boşluğu, kullanıcıya değer) · **KARANTİNA ADAYI** (işi yapan başka yol VAR — ikame kapsam beyanıyla kanıtlanır; ⛔ SİLİNMEZ, yalnız aday olarak işaretlenir) · **OPERASYON** (cron/purge/admin tetik — bugün UI'ı yok; ⚠️ "UI olmamalı" DEME, "admin için bir tetik butonu işe yarar mı" sorusunu PO'ya kart olarak aç) · **SOR** (ürün kararı gerekiyor). Kova sayıları raporun başına. SOR ve KARANTİNA ADAYI kovasındaki her kalem için §4 şablonuyla KARAR kartı. ⚠️ Gerekçesi bulunamayan hiçbir kalem KARANTİNA ADAYI olamaz — otomatik SOR olur. | 🟢 | Dört kova + sayılar + kartlar karar dosyasında | BITTI | 4 kova: BAĞLA~7 · KARANTİNA ADAYI~13(→KARAR-11) · OPERASYON~7 · SOR(→KARAR-9/10/12/15). 6 YENİ kart KARAR-12..17 (kümelenmiş). ~~[ESKİ] ≤10 kart sınırı~~ ⚠️ GÜNCELLEME (2026-09-19): kart ÜST SINIRI KALDIRILDI (PO); kural: KÜMELE + SIRALA + İNDEKS (bkz. CLAUDE.md "Karar kartı sayısı"). Mükerrerler mevcut KARAR-9/10/11'e referansla — tekrar kart AÇILMADI. |
| E-3 | değişir | **BAĞLA kovasını yap.** Her kalem ayrı PR, kendi şeridinde. Küçük ve migration'sız olanlar 🟢, diğerleri 🟡. Sırala: en az emekle en çok kullanıcı değeri önce. | 🟢/🟡 | Her kalem için kullanıcı ekranda bir şey görüyor | BEKLIYOR | (kapı gevşetildi 2026-09-19: kova alt-kalemleri tek tek — migration/hassas/geri-dönülmez değilse 🟢) |
| E-4 | Ş0 | **ARŞİV BELGESİ + KARANTİNA.** `docs/arsiv/silinenler-2026-09-10.md` oluştur: her karantina adayı için tam kod içeriği, neden yazıldığı, neden devre dışı bırakıldığı, son commit hash'i, geri alma komutu. Sonra karantinaya al (rota kapat / @deprecated), **SİLME**. ⛔ Gerçek silme ayrı bir turda, PO'nun ikinci onayıyla. | 🟡 | Arşiv belgesi tam + karantina PR'da; hiçbir şey silinmedi | BEKLIYOR | 🟡 KALIR: karantina = kaynak rota devre dışı bırakma (geri-dönüş hassas) + KARAR-11 silme protokolüne bağlı. |
| E-5 | Ş0 | **GERÇEK SİLME** — yalnız karantina bir tur sorunsuz geçtiyse ve KARAR-11 "sil" diyorsa. Arşiv belgesi zaten hazır. | 🔴 KARAR-11 + karantina turu geçmiş | Silinenler listesi + arşivden geri alma yolu çalışıyor | BEKLIYOR | |

## AŞAMA D — Kapanış

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| K-20 | Ş0 | **Belge senkronu — TEK commit, EN SON.** 09-DURUM (yeni bölüm en üste) · 00-KARAR-TAKIP (yalnız bu turda değişenler, numara verme, "aday" etiketi) · oturum günlüğü. ⚠️ Her iş sonrası DEĞİL, turun sonunda bir kez. | 🟢 | Üç belge güncel, tek PR | BITTI | PR #180 merged (CI 8/8). 09-DURUM+oturum günlüğü+KARAR-TAKIP tek PR. |

## AŞAMA F — BİLANÇO DEVRİ (2026-08-28 öncelik sırası → koda karşı doğrulandı 2026-09-19)

Bağlam: `docs/raporlar/bilanco/kararlar/00-ONCELIK-SIRASI-2026-08-28.md` (Faz 0-8, "87 işleme al") 21 gün işlenmemişti.
Bu tur her kalem **bugünün koduna karşı** doğrulandı (salt-okuma, 8 paralel alt-ajan). ✅/🗑️/⚫ kapananlar satır ALMADI;
örtüşenler mevcut K- satırının Not'una eklendi; açık (🟡/⬜) ve örtüşmeyen kalemler aşağıda. Faz sırası korundu.
Kaynak kalem kodu + doğrulama kanıtı (dosya:satır) her satırın Not'unda.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| F-01 | Ş0 | **Belge reorg — taşıyıcı-ad + büyük reorg.** G9 grubu Faz 1a'da çoğu ✅; kalan: 5 canonical taşıyıcı belge (09-DURUM/10-yol/00-INDEX/00-KARAR-TAKIP) taşınması + ~38 referans + ~68 belge isim/klasör standardizasyonu. | ~~🟡~~ 🟢 | Taşıyıcı adlar taşındı, referanslar kırılmadı | BEKLIYOR | (kapı gevşetildi 2026-09-19: belge işi) · =G9-11 (🟡) + G9-12 (⬜), Faz 1. Kanıt: 09-DURUM.md hâlâ `docs/kararlar/` kökünde ⚠️ = yönetişim konseyi §C.1/§C.3, ek bulgu: yeni açılan `docs/kararlar/konu/rtk-komut-rehberi.md` hiçbir `00-INDEX.md`'ye kaydedilmedi (KURAL 5 borcu) — bu reorg kapsamına girer. `grep -n rtk docs/kararlar/00-INDEX.md` → 0. |
| F-02 | Ş3 | **Message otomatik imha (G1-06 kalanı).** FeedbackLog 3-yıl + SystemLog 90g imhası ✅ yazılı; kullanıcı **mesajlarının** (Message) saklama-süre imhası yazılmadı. | 🟡 | Mesaj saklama süresi dolunca otomatik siliniyor | BEKLIYOR | 🟡 KALIR: KVKK/veri imha. =G1-06, Faz 2. Süre G1-10 avukat metnine bağlı. Kanıt: `gdprService.ts` TODO(G1-10); FeedbackLog `deleteMany` var ⚠️ = güvenlik konseyi §2.B.4, ek bulgu: `Message` için süre yazılmamış olması BİLİNÇLİ ve gerekçeli (`gdprService.ts:375-378` TODO(G1-10): "kodda keyfi süre yazarsak aydınlatma metniyle çelişir"). Kıyas doğrulandı: SystemLog 90 gün `:341,366-368` ✅ ve FeedbackLog 3 yıl `:342,371-373` ✅ UYGULANIYOR — eski raporların "uygulanmamış" iddiası BAYAT. Kilit: avukat metni (03-PO #16 · P-a). |
| F-03 | Ş3 | **OAuth rıza ayrımı (G1-08).** Google ile girişte KVKK/açık rıza UI'da ayrıca alınmıyor (implicit). Kayıt formunda checkbox var, OAuth'ta yok. | 🟡 | OAuth ile girişte de açık rıza kutusu görünüyor | BEKLIYOR | 🟡 KALIR: KVKK/rıza + auth (OAuth). =G1-08, Faz 2. Hukukçu görüşüne bağlı. Kanıt: `oauthService.ts:112-120` implicit; `OAuthButtons.tsx` checkbox yok ⚠️ = güvenlik konseyi §2.B.5, ek bulgu: OAuth'ta rıza backend'de VARSAYILARAK yazılıyor (`oauthService.ts:108-120`, yorum `:112-114` itiraf ediyor); *kapsam:* `OAuthButtons.tsx` içinde `kvkk|rıza|consent|onay` → 0. ⚠️ İKİNCİ BULGU: klasik kayıttaki TEK kutu hem KVKK açık rızasını hem 18+ beyanını BİRLEŞTİRİYOR (`_RegisterContent.tsx:413-417`) ve aynı tek kutudan `AYDINLATMA` + `ACIK_RIZA` iki ayrı satır yazılıyor (`consentService.ts:57-70`) → aydınlatmayı ve açık rızayı ayrı ayrı reddetme imkânı yok (kod bunu `:58`'de "ayrı kutular G1-08 işi" diye not ediyor). |
| F-04 | Ş4 | **logoUrl XSS guard (G1-23).** Logo URL çıplak `z.string().url()`; host/MIME allowlist + CSP yok. `javascript:`/veri-URI riski. | 🟡 | Logo URL host/MIME allowlist'e tabi, CSP kapsıyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — `logoUrl` XSS guard — kurum logosu yeni kurumun yüklediği İLK şey; CSP kapalı · 🟡 KALIR: güvenlik (`server.ts` CSP/XSS, K-14 ile aynı dosya). =G1-23, Faz 3. ⚠️ K-14 ile aynı dosya `server.ts` → SIRALI. Kanıt: `tenantController.ts:11,82`; `server.ts:44` global CSP kapalı. (⚠️ eskiden "guard var" sanıldı — o IDOR guard'ıydı, XSS değil) ⚠️ = güvenlik konseyi §D.2 (D-8), ek bulgu: F-04'ün `logoUrl` ayağı KISMEN kapanmış — `admin/branding/page.tsx:47-57` `isSafeLogoUrl()` https allowlist'i + `:70-72` kaydetme engeli ✅, `atoms/TenantLogo.tsx:26` `next/image` allowlist'i ✅. AÇIK KALAN TEK NOKTA: `organisms/TenantSwitcher.tsx:196-201` ham `<img src={logoUrl}>` (ne `isSafeLogoUrl` ne `next/image`). ⚠️ Risk XSS DEĞİL — `<img src="javascript:">` çalışmaz; gerçek etki izleme pikseli / `remotePatterns` allowlist baypası (her üyenin IP/UA'sı dış hosta sızar); backend `https:` zorlamıyor (`tenantController.ts:11,82` `z.string().url()` şema allowlist'i değildir). `TenantLogo`'ya çevirmek tek satırda kapatır. Global CSP ayağı (server.ts:44) ayrıca GV-03'ü de azaltır. |
| F-05 | Ş4 | **CAPTCHA/step-up (G1-26).** Public şüphe formunda IP-limit ✅ var; CAPTCHA + step-up doğrulama yok. | 🟡 | Public formda bot/spam koruması güçlendi | BEKLIYOR | 🟡 KALIR: güvenlik/auth (step-up doğrulama). =G1-26, Faz 3. Kanıt: `suspicionRoutes.ts:9` rate-limit var; CAPTCHA grep boş |
| F-06 | Ş4 | **Denetim izi hata yakalama (G1-14).** Kalibrasyon audit yazımı `void logger.info('AUDIT',...)` (fire-and-forget) — yazım sessizce düşebilir. | 🟢 | Audit yazımı hatası artık yutulmuyor | BITTI | backend PR #81 merged + pointer #215 (CANLIDA). `void`→`await`+`.catch(console.error)`; yazım tamamlanır, başarısızlık ops'a görünür. Mevcut audit testi deterministik hale getirildi. =G1-14, Faz 3. |
| F-07 | Ş4 | **Denetim izi saklama gerilimi (G1-15).** Kalibrasyon AUDIT izi SystemLog'ta 90 günde siliniyor → iz-koruma ↔ KVKK imha gerilimi. | 🔴 KARAR-19 | İz-koruma ile imha politikası uzlaştırıldı | BEKLIYOR | 🟡 KALIR: KVKK (saklama/imha) + KARAR-19. =G1-15, Faz 3. Kanıt: `gdprService.ts:341,366`. Tasarım/hukuki karar tarafı KARAR-19'da ⚠️ **KAPI DÜZELTİLDİ (2026-09-21): 🟡 → 🔴 KARAR-19.** `01-KARARLAR.md` İÇİNDEKİLER KARAR-19'u *"1 (F-07)"* diye sayıyor ve F-07'nin kendi Not'u *"hukuki karar tarafı KARAR-19'da"* diyor. 🟡 kalırsa cevaplanmış bir kararın işi yanlışlıkla PR'a gider.|
| F-08 | Ş0 | **Sektör asimetri paydası (G2-09).** Sektör skoru paydası menti-etiket sayısı yerine iki tarafın etiket **birleşimi** olmalı (B9.4). | 🟡 | Sektör skoru simetrik paydayla hesaplanıyor | BEKLIYOR | 🟡 KALIR: matching/skorlama (`scoring.ts`). =G2-09, Faz 5. KARAR-10'dan bağımsız küçük iş. Kanıt: `scoring.ts:34-40` payda `mentiSet.size` ⚠️ = psikometri konseyi §2 A.2, ek bulgu: güncel main'de HÂLÂ AÇIK — scoring.ts:40 paydası mentiSet.size (2026-09-21 teyidi); ayrıca ağırlık DÖRT yerde tanımlı (scoring.ts:89-90 · algorithmTuner.ts:28-33 · matching.ts:38 · scoring.config.ts:48) ve dördüncüsü ölü zincire ait. |
| F-09 | Ş0 | **12 SJT senaryo + arketip seed.** Şema/model ✅ (`SjtQuestion.triggersOn/signalsArchetype`); seed'de yalnız 3 senaryo var (12 değil). | 🟡 | Canlıda 12 SJT senaryosu + arketipler | BEKLIYOR | =Faz5 "12 senaryo+arketip". Seed=PO onayı + yedek. Kanıt: `seed.ts:530-573` 3 senaryo ⚠️ **KAPI DÜZELTİLDİ (2026-09-21): `🔴 seed` → 🟡.** Cevaplanacak bir KARAR **numarası yoktu** → sonsuz kilit; 13 🔴'nın numarasız tek'iydi. Emsal: **P-99 🟡** (*"seed dosyası değişir, seed çalıştırılmaz"*). ⛔ Seed **çalıştırılmaz**; yalnız dosya hazırlanır, çalıştırma PO onayı + yedek ister.|
| F-10 | Ş4 | **Havuz kartı FE (G4-01).** Backend rol-ayrımlı select ~%90 hazır; menti-yönü havuz kartı FE tasarımı yok (B10.3). | ~~🟡~~ 🟢 | Menti havuzu rol-bazlı kart olarak görüyor | BITTI | (kapı gevşetildi 2026-09-19: FE kart; backend DTO rol-güvenli — discType hariç, motor/skor DEĞİŞMEZ) · =G4-01, Faz 5. Kanıt: `matching.ts:337-349` RankedMentor DTO hazır, FE kart yok ✅ **DURUM DÜZELTİLDİ (2026-09-21, kod-teyitli): BEKLIYOR → BITTI.** Kuyruk *"menti-yönü havuz kartı FE tasarımı yok"* diyordu; **kart TAM VAR**: `frontend/src/app/(dashboard)/menti/page.tsx:288` (kodda birebir yorum *"Havuz KART görünümü (KARAR 2/7)"*) · `:311` `%{mentor.matchScore}` · `:317` *"Neden uyumlu:"*. Bayat durum kaydıydı.|
| F-11 | Ş0 | **Algoritma çekirdeği — KARAR-10 kilitli küme.** OCEAN/sektör motoru canlı eşleştirmeye bağlama (G2-07/08/G10-21) + yeni skor formülü %45/30/25 + 2 veto (V1/V2) + triggersOn derinleşme (madde 125/B6) + Big Five göç planı (B12). Motorlar YAZILI ama `matching.ts` onları çağırmıyor. | 🔴 KARAR-10 | KARAR-10 cevabına göre motor bağlanır/ertelenir | BEKLIYOR | =Faz5 çoğu. Hepsi KARAR-10'a kilitli. Kanıt: `matching.ts:3-5` yeni motoru import etmez; `scoring.ts:89-90` hâlâ 0.6/0.4 ⚠️ `= psikometri konseyi §3 B.4, ek bulgu: seçeneklerin somut maliyeti KARAR-10 kartına eklendi; motorun SIFIR birim testi var ⇒ hangi seçenek seçilirse seçilsin onarımın doğruluğunu kanıtlayacak kanıt örtüsü bugün yok.` **+ ⛔ çapraz atıf (bkz. §4)** ⛔ **BİRLİKTE YAPILMALI — önce silme yolu düzeltilir, SONRA Match yazımı açılır. Ters sıra = KVKK ihlali.** *(karşı taraf: **GV-08**. `createMatchIfEligible` ölü OCEAN motorunun İÇİNDE (`scoring.service.ts:137`) ve arketipi bizzat o yazıyor — KARAR-10'a "C" cevabı gelirse `Match` yazımı bu satır üzerinden açılır.)* |
| F-12 | Ş1 | **STK anket cevap tipi (G3-13).** Kurum-özel soru Likert-sabit; şıklı/açık cevap tipi seçimi yok (migration). | 🔴 KARAR-21 | Kurum soru eklerken cevap tipini seçiyor | BEKLIYOR | =G3-13, Faz 6. Migration. Kanıt: `Question` modelinde answerType yok (SjtQuestion.AnswerFormat farklı kavram) |
| F-13 | Ş0 | **Sertifika soru-ekleme gerekçe belgesi (G3-05).** Kısıt kodda ✅ (kurum sertifika sorusu ekleyemez); "neden" gerekçe belgesi zayıf/yok. | 🟢 | Karar-gerekçe belgesi yazıldı | BITTI | `docs/kararlar/sertifika-soru-standardi-gerekce-2026-09-21.md` yazıldı: kurum sertifika sorusu ekleyemez (global soru bankası) — karşılaştırılabilir kalite sinyali + ortak havuz güveni + kötüye kullanım. Kurum yalnız konu açıp/kapatır (min/red-line sınırında). =G3-05. |
| F-14 | Ş1 | **Menti personası çeşitlendirme (B8).** Sertifika/içerik senaryolarında menti persona çeşitliliği — içerik işi. | ~~🟡~~ 🟢 | Senaryolarda çeşitli menti personaları var | BEKLIYOR | (kapı gevşetildi 2026-09-19: içerik/belge işi) · =Faz6 B8. İçerik turu. Kanıt: persona alanı/çeşitlendirme grep boş |
| F-15 | Ş1 | **Bekleme anı (G4-22+G4-23).** Bekleyen mentiye öğrenme+DISC+umut sinyali ("N kişi bekliyor, mentörler geliyor") ile anlamlı bekleme; şu an statik banner. | ~~🟡~~ 🟢 | Bekleyen menti anlamlı/umut veren ekran görüyor | BITTI | PR #228 merged, CANLIDA. Bekleme banner'ına umut/anlam cümlesi (sayı UYDURMADAN) + beklerken öğrenme/DISC ile hazırlan nudge; öğrenme+DISC kartları zaten vardı. CANLIDA BAK: bekleyen menti umut veren metin görüyor. =G4-22/23 |
| F-16 | Ş1 | **Menti özgüven sunumu (G4-24).** DISC sonucu tüm rollere ortak; menti-özel "güçlü yanların" tonu yok. | ~~🟡~~ 🟢 | Menti kendi DISC'ini özgüven veren tonla görüyor | BITTI | PR #228 merged, CANLIDA. Menti panelindeki DISC rapel kartına (`DiscRecallCardView`) menti-özel özgüven cümlesi ('Bu güçlü yanlar senin…'); diğer rollerde gizli. 2 test. CANLIDA BAK: menti DISC kartını özgüven tonuyla görüyor. =G4-24 |
| F-17 | Ş2 | **Mentör reddi + yumuşatma (G4-25).** Mentör→menti ret akışı HİÇ yok (VisibilityOptIn onayı kaldırılmış); dolayısıyla yumuşatma da yok. Önce "mentör reddedebilsin mi" kararı gerekir. | 🔴 KARAR-20 | KARAR-20'ye göre ret akışı + nazik gösterim | BEKLIYOR | =G4-25, Faz 7. Kanıt: `requestController.ts:17` doğrudan talep, ret akışı yok ⚠️ **MÜKERRER NOTU (2026-09-21):** P-05 ile aynı konu (mentör reddi deneyimi); KARAR-22 kartı zaten *"P-05 ve F-17 atlanır, KARAR-20 ile kümelenmeli"* diyor. **P-05 kalsın**, bu satır onun referansı. ⚠️ Bu satırın varsayımı (*"ret akışı HİÇ yok"*) **çürüdü**: kurum üyelik reddi VAR; eksik olan **eşleşme** reddi akışı (bkz. I-16).|
| F-18 | Ş4 | **Yönetici/sponsor rapor export (G4-30).** Kuruma/sponsora PDF/Excel/CSV KPI raporu yok (KVKK kişi-JSON export'undan farklı). | 🟡 | Yönetici program raporunu dışa aktarabiliyor | BEKLIYOR | 🟡 KALIR: veri dışa aktarma (KVKK/aggregate k-anonimlik riski). =G4-30, Faz 7. Format/veri alt-kararı yapımda. Kanıt: `backend/src`'de PDF/CSV/xlsx 0 sonuç |
| F-19 | Ş4 | **Proaktif kırmızı uyarı (G4-31).** `pairSignal` GREEN/YELLOW/RED var ama ikili-görüşme sağlığı için; yöneticiyi harekete geçiren eşik-alarmı (bekleyen menti vb.) yok. | ~~🟡~~ 🟢 | Yönetici eşik aşımında proaktif kırmızı uyarı görüyor | PR-ACIK | (kapı gevşetildi 2026-09-19: admin panel uyarısı; migration/hassas değil) · =G4-31, Faz 7. Kanıt: `pairSignal.service.ts:12,25-58` farklı amaç ⚠️ **GÜNCELLEME (2026-09-21, BB turu):** kod YAZILDI ve dal push edildi (`otonom/F19-admin-proaktif-uyari-20260921`, commit `ac7a3f4`, **2026-09-21 08:56**), **PR #231 AÇIK** — sonra PC kapandı, merge edilmedi. ⛔ **BITTI YAZILMAZ:** merge olmadan canlıda değil (`CLAUDE.md` "Bitti tanımı" — kullanıcı görmüyor). CI yeşilse merge → o zaman BITTI + `02-ILERLEME.md`'ye "CANLIDA BAK" satırı. |
| F-20 | Ş1 | **Bildirim izni (G5-04).** Bekleme salonunda `Notification.requestPermission` istemi hiç yok. | 🟢 | Kullanıcıya bildirim izni isteniyor | BITTI | PR #218 merged (CI 8/8). `NotificationOptInButton` bekleme banner'ında; yalnız izin 'default' iken düğme. CANLIDA BAK: bekleyen menti '🔔 Bildirimlere izin ver' düğmesi görüyor. Saf view 4 test. =G5-04, Faz 7. |
| F-21 | Ş0 | **Erişilebilirlik paketi (G7-01+02+09).** aria/role/klavye (Likert radiogroup, modal role=dialog yok) + DISC kontrast + WCAG denetimi. | ~~🟡~~ 🟢 | Ekran okuyucu/klavye ile temel akışlar erişilebilir | BEKLIYOR | (kapı gevşetildi 2026-09-19: FE a11y; migration/hassas değil) · =G7-01/02/09, Faz 7. ⚠️ K-10 (dark-mode kontrast) ile kesişir. Kanıt: `DailyQuestionWidget.tsx:79-92` aria yok ⚠️ **MÜKERRER NOTU (2026-09-21):** K-10 ile kesişir → bu satır **"aria/role/klavye + WCAG"** ayağına daralsın; DISC kontrast ayağı K-10'da.|
| F-22 | Ş1 | **Görüşme-tamamlama paylaşım kartı (G4-39).** "Görüşme tamamladım 🎉" paylaşılabilir kutlama kartı yok (DISC sonuç kartıyla karıştırılmasın). | 🟢 | Görüşme sonrası paylaşılabilir kutlama kartı | BITTI | PR #226 merged, CANLIDA. COMPLETED görüşmede '🎉 tamamladın' + WhatsApp/LinkedIn paylaşım; `ShareButtons` ortak moleküle çıkarıldı (DRY). 2 test. CANLIDA BAK: tamamlanan görüşmede paylaşım düğmeleri. =G4-39 |
| F-23 | Ş4 | **adminSettings izolasyon deseni (G4-05).** Controller-içi elle `tenantId` filtresi; merkezî tenant-izolasyon deseni yok (güvenlik açığı değil, tutarsızlık). | 🟡 | adminSettings merkezî izolasyon desenine geçti | BEKLIYOR | 🟡 KALIR: yetkilendirme/tenant izolasyon deseni. =G4-05, Faz 8. Kanıt: `adminSettingsController.ts:82,97,143` elle filtre ⚠️ = güvenlik konseyi §2.A.4 (G-9), ek bulgu: bu satırın "güvenlik açığı değil, tutarsızlık" değerlendirmesi ÇÜRÜDÜ — `adminSettingsController.ts:11-21` `extractAdminPayload` yalnız `verifyToken` + `role==='ADMIN'` bakıyor; `TenantMembership.isActive` kapısı ve `aud` claim kontrolü YOK (kıyas `tenant.ts:87-97` ve `platformAuth.ts:27` ikisi de VAR). Üyeliği pasifleştirilmiş yönetici kurum ayarlarını değiştirebiliyor → GV-11. F-23 merkezî desene geçerse GV-11'in bu ayağı birlikte kapanır; `selfServeController.ts:61-71` ayağı ayrı kalır. |
| F-24 | Ş4 | **Platform tek-kullanıcı drill-down (G4-08).** `/tenants/:id/users/:userId` platform detay ucu yok. | 🟡 | Platform admin bir kullanıcının detayına iniyor | BEKLIYOR | 🟡 KALIR: auth (platform) + PII döndüren yeni uç. =G4-08, Faz 8. Kanıt: route grep — yok |
| F-25 | Ş4 | **Mail gerçek probe (G4-14).** Sistem sağlığı mail göstergesi yalnız config-var'a bakıyor; gerçek gönderim testi yapmıyor (bilinçli). | 🟢 | Mail göstergesi gerçek probe sonucunu gösteriyor | BITTI | PR #84(BE)+#227(pointer) merged, CANLIDA. Platform sağlık mail göstergesi config-var DEĞİL gerçek `verify()` sonucu (5sn timeout). CANLIDA BAK: platform panelde SMTP göstergesi gerçek durumu yansıtıyor. =G4-14 |
| F-26 | Ş4 | **`.env.example` eksik değişken (G4-17).** `PLATFORM_ADMIN_EMAIL` örnek env dosyasında yok. | 🟢 | .env.example tam | BITTI | ZATEN YAPILMIŞ (kod-teyit §5c): `backend/.env.example`'da `PLATFORM_ADMIN_EMAIL` mevcut (açıklama yorumuyla). Bir sonraki turda eklenmiş. =G4-17 |
| F-27 | Ş0 | **N+1 konuşma listesi (G6-01).** `listConversations` her konuşma için ayrı count+findFirst; pagination yok. | ~~🟡~~ 🟢 | Konuşma listesi tek sorguda + sayfalı | BITTI | PR #86(BE)+#229(pointer) merged, CANLIDA. `limit`/`offset` sayfalama (varsayılan 30, tavan 100) + gerçek `total` → sınırsız N+1 sayfa boyutuyla sınırlı. Saf `parseConversationPagination` 5 test. ⚠️ Tam 'tek sorgu' denormalizasyon = şema değişikliği → ayrı iş. =G6-01. Kanıt: `conversationController.ts` |
| F-28 | Ş0 | **Sayfa metni merkezileştirme (G6-05).** `registerMessages.ts`+`threeQuestionsText.ts` özel modüller var; genel sayfa-metni sözlüğü yok. | ~~🟡~~ 🟢 | Dağınık inline metinler merkezî sözlükte | BEKLIYOR | (kapı gevşetildi 2026-09-19: FE metin/i18n; hassas değil) · =G6-05, Faz 8. Kanıt: yalnız 2 özel modül; global i18n/dictionary yok |
| F-29 | Ş4 | **SEO teknik paketi (G7-03).** sitemap/robots/metadataBase/OG-image yok; `lang="tr"` (tr-TR değil). | 🟢 | sitemap+robots+metadataBase mevcut | BITTI | PR #219 merged (CI 8/8). `app/sitemap.ts`+`app/robots.ts`+`metadataBase`; lang tr→tr-TR. Tek kaynak `getSiteUrl()` (env `NEXT_PUBLIC_SITE_URL`, dev fallback). Saf helper 3 test. =G7-03, Faz 8. |
| F-30 | Ş1 | **Bayat yorum temizliği (G10-22).** `LoginForm.tsx:7` "Sprint 14'te tam entegrasyon" bayat yorumu. | 🟢 | Bayat yorum kaldırıldı | BITTI | PR #206 merged, CI 8/8. Yorum güncel gerçeğe hizalandı (TenantProvider/useTenant köprülemesi zaten var). Yalnız yorum. |
| F-31 | Ş3 | **Genel ürün geri-bildirim akışı (G5-05).** `/bildir` sayfası yalnız SuspicionReport gönderiyor; her sayfada "hata/öneri bildir"→ürün ekibine akış yok. | 🟡 | Kullanıcı üründen geri bildirim bırakabiliyor | BEKLIYOR | (kapı gevşetildi 2026-09-19: kimlikli kullanıcı geri-bildirim akışı; migration/KVKK-silme/matching değil) · =G5-05, Faz 8. Kanıt: `bildir/page.tsx` submitSuspicionReport ⚠️ **KAPI DÜZELTİLDİ (2026-09-21): 🟢 → 🟡 — TEHLİKELİ YÖN.** "Ürün geri-bildirim akışı" için uygun depo YOK (`SuspicionReport` = şüphe formu, `FeedbackLog` = görüşme NPS'i) → yeni model gerekir = **MIGRATION** → kapı kuralı istisna 1 gereği 🟡. 🟢 kalsaydı ajan şema değiştirip merge edebilirdi.|
| F-32 | Ş4 | **Sekme geçiş yavaşlığı (G8-13).** İstemci önbelleği/prefetch katmanı yok; her sekmede yeniden fetch. | ~~🟡~~ 🟢 | Sekme geçişleri hızlı (önbellek) | BEKLIYOR | (kapı gevşetildi 2026-09-19: FE istemci önbelleği; migration/hassas değil) · =G8-13, Faz 8. Kanıt: `lib/hooks` prefetch/swr/query 0 |
| F-33 | Ş4 | **Dashboard sol-alt kullanıcı kartı (G8-14).** Admin panelde var; menti/mentör dashboard'unda sağ-üst bar, sol-alt kart yok. | 🟢 | Dashboard'da sol-alt kullanıcı kartı/menü | BEKLIYOR | =G8-14, Faz 8. Kanıt: `(admin)/layout.tsx:128-142` var; `DashboardNav.tsx:44,64` top-bar |

> **Örtüşme — mevcut K- satırlarına eklenen kalemler (yeni satır AÇILMADI):**
> - **K-13** (mükerrer uçlar) = G10-01(c) MeetingScheduler mount edilmeyen bileşen · G4-09/G4-10 super-admin ikizi (FE 0 kullanım) · G10-23 (mentiRequestController — dosya silinmiş, çözülmüş). Hepsi E-2/E-3/E-4 hayalet akışında.
> - **K-14** (sunucu sertleştirme) = G1-28 (Faz 0 altyapı, KARAR-18) + F-04 (G1-23 CSP, aynı `server.ts` → SIRALI) + F-05 (G1-26 kısmi).
> - **K-16** (sertifika seed) = G3-08 (seed kodu 20/80 eski sürüm · finalize 22/88 içeriği `docs/raporlar/icerik/` 3 belgede yazılı, seed'e taşınmadı → içerik→seed taşıma adımı gerekir; ⚠️ 2026-09-21: bu "sürüm farkı" DEĞİL — 20'nin 15'i elenmiş, kalan 5'i yeniden yazılmış, 88 şıkın tamamı taşınacak, efor **L**; bkz. P-99) · G3-09 (npm runner yok, dosya-içi tsx guard var).
> - **K-04** (foto yükleme) = G8-01/G8-02 foto volume (Faz 0, KARAR-18).
> - **K-12** (KVKK insan-okunur özet) = G1-05 zaten ✅; K-12 onun bir sonraki adımı (ham JSON → okunur sayfa).

> **✅/🗑️/⚫ kapananlar (satır ALMADI, kanıt aşağıda) — "yapılmamış sanılıp YAPILMIŞ" olanlar dahil:**
> Faz1: G9-grubu·G9-05·G6-07·G7-12·G7-13 ✅. Faz2: G1-05·G1-07 ✅; G1-01 ⚫ (bilinçli kapsam düşürüldü→metin beyanı).
> Faz3: G7-04·G1-02·G1-19 ✅; G1-17 ⚫ (backend `requireRole`'de çözüldü, middleware admin-guard teknik imkânsız); G1-04 ⚫ (yeniden tanım: public form, tenant izolasyon açığı değil).
> Faz4: S21·Üç-soru·G6-03(onDelete) ✅. Faz5: G2-10 🗑️ (qm² çift-çarpım iddiası PR #138'de çürütüldü).
> Faz6: G3-19 (etiket havuzu — kart ⬜ der, `PendingTag`+tagController VAR)·B7·B8-yüzey ✅.
> Faz8: **G4-02·G4-04·G2-11·G10-25 = kart açık/eksik der ama KODDA VAR** (compatibilityReason FE · managers paneli · davetli=onaylı tetiği `authController.ts:165` · profil düzenleme çalışıyor); G10-23 🗑️.
> ❓ TEYİT (bulut/DB yapamaz): G3-16/G3-18 canlı içerik sayıları — DB-teyit turu gerekir.

## AŞAMA P — PANEL DENETİMİ BULGULARI (2026-09-19)

Kaynak: `docs/raporlar/kesif/panel-denetimi-mentor-menti-2026-09-19.md` (H turu, PR #186). Mentör+menti paneli "istenen vs kodda olan":
26 istek → ✅11 · 🟡11 · ⬜3 · ❓1. Asıl kütle 🟡 YARIM'da — özellik yazılmış, **son bir adım eksik** (düşük efor/yüksek getiri).
Aşağıya raporun §6 (en yüksek etkili 5) + §2/§3 tablolarındaki TÜM 🟡/⬜ kalemler alındı. Sıra: en az emek→en çok değer üstte (rapor ölçütü).
P-01..P-05 = raporun §6 beşlisi. P-00 = güvenlik (ayrı). ⛔ KOD YAZILMAZ — sıradaki otonom tur yapacak.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| P-00 | Ş4 | **⭐ GÜVENLİK: k-anonimlik eşiği yalnız FRONTEND'de.** `GET /api/users/mentor-count` ham sayıyı koşulsuz dönüyor; N<3 koruması yalnız `menti/page.tsx:189`'da. PENDING menti ucu doğrudan çağırıp 1-2 okuyabilir. | 🟡 | Backend N<3'te sayıyı maskeliyor (uç doğrudan çağrılınca da) | BITTI | PR #73 (M, backend) merged + pointer #192. Backend N<3 maskelemesi eklendi (`mask.ts` + unit test). CANLIDA BAK: PENDING menti `/api/users/mentor-count`'u doğrudan çağırsa da N<3'te sayı maskeli. ⛔ eski: CLAUDE.md "frontend guard yeterli değil" ihlali kapandı. |
| P-01 | Ş2 | **Menti mentörünün adını göremiyor.** "Görüşmelerim"de tarih+durum var, kiminle görüşeceği YOK. | 🟢 | Menti randevu kartında mentör adını görüyor | BITTI | PR #190 (N) merged, CI 8/8. =M9/§6#1. FE tip+satır düzeltmesi (opponent artık role'e göre mentor/menti). CANLIDA BAK: menti "Görüşmelerim"de mentörünün adını görüyor. |
| P-02 | Ş1 | **"Gönderilen Talepler" kartı sayfa yenilenince sıfırlanıyor.** Dün 5 talep gönderen bugün "0" görüyor. | 🟢 | Talep sayısı yenilemeden sonra korunuyor | BITTI | PR #211 merged (CI 8/8). Kalıcı kaynak `/api/conversations`; `countSentRequests` helper (kalıcı ∪ oturum). CANLIDA BAK: menti sayfayı yenileyince gönderilen talep sayısı korunuyor. =M13/§6#2. Kanıt: `menti/page.tsx:213` `sentIds.size` = oturum-içi `useState` (`:80`). Panel zaten `/api/meetings` çekiyor → PENDING'den türet. ⚠️ belge hatası DEĞİL (`00-KUYRUK:60` bilinçliydi). Efor S |
| P-03 | Ş1 | **DISC "özgüven aşısı" tek seferlik — panelde/testte rapel yok.** Kayıt sonrası arketip+güçlü yön kartı bir daha görünmüyor; `/disc-test` sonu çıplak "DISC Profiliniz Hazır!". | 🟢 | Kullanıcı DISC kartını (arketip+güçlü yön) tekrar görebiliyor | BITTI | PR #209 merged (CI 8/8). `DiscRecallCard` menti panelinde `/api/users/:id` discResultCard'ı salt-okunur render eder. CANLIDA BAK: DISC'ini bitiren menti arketip kartını panelde tekrar görüyor. =M1/MT3/§6#3. Kanıt: zengin kart yalnız `ResultStep.tsx:80-127`; `disc-test/page.tsx:76-79` çıplak. API HAZIR (`lib/api/profile.ts:10-20 discResultCard`) hiç render edilmiyor. Efor S |
| P-04 | Ş0 | **Mentör hiçbir yerde "uyum yüzdesi" görmüyor.** Onay kuyruğunda %rozeti hiç render edilmiyor; aday listesinde çıplak sayı ("74", %yok). Mentör P1 "%92 aha" vaadi oluşmuyor. | 🟡 | Mentör aday/talep ekranında %uyum görüyor | BEKLIYOR | 🟡 KALIR: matching (matchId akışı/skor kaynağı). =MT1/§6#4. Kanıt: `mentor/page.tsx:214` `m.match` daima null (`Meeting.matchId` hiç yazılmıyor); `:436` `.toFixed(0)` %yok. ⚠️ mimari seçim (matchId akışı mı / candidates'tan oku mu) — teknik, ajan karar verir. Mentör paneli şerit tanımında yok→Ş0. Efor M ⚠️ `= psikometri konseyi §0 ①, NOT DÜZELTMESİ: kuyruk "onay kuyruğunda %rozeti hiç render edilmiyor" diyor — YANLIŞ; rozet kodda VAR (mentor/page.tsx:292 "%{score} uyum", 2026-09-21 FE teyidi) ama m.match daima null olduğu için beslenmiyor. Aday listesindeki çıplak sayı tespiti (:491) DOĞRU.` **+ ⛔ çapraz atıf (bkz. §4)** |
| P-05 | Ş2 | **Ret tamamen çıplak — menti "İptal Edildi" kırmızı rozetinden öğreniyor.** Bildirim/e-posta/gerekçe/alternatif yok. Persona "en kritik risk". | 🔴 KARAR-22 | Reddedilen menti nazik gösterim + (karara göre) alternatif/bildirim görüyor | BEKLIYOR | =M5/§6#5. Kanıt: `meetingController.ts:563-585` rejectMeeting yalnız CANCELLED+notes; hiç notify yok (approve'da var → asimetri). FE gerekçe göndermiyor (`mentor/page.tsx:97`). Şema `rejectionReason` yok. Ürün+şema kararı → KARAR-22. Efor M |
| P-06 | Ş1 | **Umut sinyali/sosyal kanıt yarım.** "N onaylı mentor" var; "senin gibi N kişi bekliyor / yakında eşleşeceksin" YOK. N<3'te sayı gizlenince umut büsbütün kayboluyor. | 🟡 | Bekleyen menti "yakında eşleşeceksin" tarzı umut sinyali görüyor | BEKLIYOR | 🟡 KALIR: aggregate/k-anonimlik (KVKK-agregat; P-00/G1-22 akrabası). =M3. Kanıt: `menti/page.tsx:184-202`; akran/kuyruk sayısı 0 sonuç. Backend uç gerekebilir. Efor M · **= X §10#22** (minimum havuz/k-anon kapısı; P-00 backend maskesiyle kısmen kapandı, P-16 ile küme) ⚠️ **DURUM DARALTILDI (2026-09-21, kod-teyitli): YARIM.** *"'yakında eşleşeceksin' YOK"* ayağı **CANLIDA** — umut cümlesi F-15/PR #228 ile geldi: `menti/page.tsx:217-222` (💛 *"Sen yalnız değilsin…"*). **KALAN ayak:** bekleyen menti sayısı (k-anonim toplu gösterim) — hâlâ yok. ⚠️ **MÜKERRER: F-15 ile aynı ekran** → F-15 BITTI, bu satır yalnız sayaç ayağına daralır.|
| P-07 | Ş1 | **Küçük başarı/kilometre taşı kutlaması yok.** Menti 1. ve 10. görüşmede aynı jenerik "🎉 Teşekkürler". "İlk görüşmeni tamamladın!" gibi sayıya bağlı kutlama yok. | ~~🟡~~ 🟢 | Menti kilometre taşlarında kişiye özel kutlama görüyor | BITTI | PR #214 merged (CI 8/8). Saf `meetingMilestone(count)` + check-in done ekranı; ilk/5/10/25/10-katları eşikleri. CANLIDA BAK: değerlendirme sonrası kaçıncı görüşme olduğuna göre farklı kutlama. =M6 (⬜). Kanıt: `meeting-checkin/page.tsx:81-88` jenerik; milestone/streak 0 sonuç. Efor M |
| P-08 | Ş1 | **Öğrenme yolculuğu ilerlemesi kalıcı değil.** Panel kartı yalnız "başla/tamamlandı"; "neredeyim, sıradaki adım" yok. Sayfadan çıkan menti ilerlemeyi kaybediyor. | 🟡 | Menti yolculukta kaçıncı aşamada olduğunu kalıcı görüyor | BEKLIYOR | 🟡 KALIR: şema/migration gerekebilir (completedStages) — belirsiz (kanıt: "Backend/şema completedStages gerekebilir"). =M8. Kanıt: `ScenarioGuideEngine.tsx:97 useState(0)`; API `{completed,totalStages}` ama tamamlanan aşama SAYISI yok (`learningJourney.ts:79-84`). Backend/şema completedStages gerekebilir. Efor M |
| P-09 | Ş0 | **Mentör boş paneli kayıp + mesaj boş durumu menti ağzıyla.** Yeni mentör: 4× "—" + kaybolan talep kartı + çıplak "toplantınız yok"; gelen kutusunda *"Bir mentöre mesaj gönderdiğinizde…"* okuyor. | 🟢 | Mentör boş ekranda değer/yönlendirme + doğru rol metni görüyor | BITTI | PR #210 merged (CI 8/8). Onay kuyruğu kartı boşken kaybolmuyor (yönlendirici metin); mesaj boş-durum metni role göre dallandı. CANLIDA BAK: yeni mentör boş panelde 'Toplantı Talepleri' kartı + doğru rol metni. =MT2. Kanıt: `mentor/page.tsx:194,202,463-466`; `messages/page.tsx:52-58` rol dalı yok. Mentör paneli→Ş0. Efor S |
| P-10 | Ş4 | **Mentör bildirim/e-posta ulaşmıyor.** Talep tetikleyicisi var ama push STUB; canlı booking yolu (`/meetings/book`) e-posta da göndermiyor. Mentör talebi ancak paneli açınca öğreniyor. | ~~🟡~~ 🟢 | Mentör yeni talepte bildirim/e-posta alıyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2+T3)** — mentör bildirimi ulaşmıyor — mentör talebi ancak paneli açınca öğrenir → eşleşme döngüsü durur · (kapı gevşetildi 2026-09-19: bildirim/e-posta wiring; migration/auth/KVKK/matching değil) =MT5. Kanıt: `notificationService.ts:39-55` `[PUSH-STUB]`; e-posta yalnız `createMeeting:199`, `book:514-531`'de yok. Mevcut G5-07 push stub ile ilişkili. Efor M |
| P-11 | Ş0 | **Emek "kaç saat" görünmüyor.** "Kaç menti/görüşme" var, "kaç saat mentörlük" yok; "bu dönem" çerçevesi yok, dört çıplak sayı. | ~~🟡~~ 🟢 | Mentör toplam mentörlük saatini/dönem emeğini görüyor | BITTI | PR #82(BE)+#222(FE)+#223(pointer) merged, CANLIDA. `dashboard-metrics`'e `totalMentoringHours` (tamamlanan görüşme `durationMin` toplamı/saat) + panelde 'Mentörlük Saati' kartı. CANLIDA BAK: mentör panelinde toplam mentörlük saati. =MT6. |
| P-12 | Ş0 | **Sertifika rozeti kalıcı değil.** Sertifikalı mentör ertesi gün panelde hâlâ "Sertifikaya başla →" görüyor. | ~~🟡~~ 🟢 | Sertifikalı mentör panelde kalıcı rozet görüyor | BITTI | PR #82(BE)+#222(FE)+#223(pointer) merged, CANLIDA. `dashboard-metrics`'e `isCertified` (TenantMembership) + panel CTA sertifikalıysa '✅ Sertifikalı' rozeti gösterir. CANLIDA BAK: sertifikalı mentör ertesi gün 'Sertifikaya başla' yerine rozet görüyor. =MT7/BY-3. |
| P-13 | Ş0 | **"Şu an kime mentörlük yapıyorum" listesi panelde yok.** "Aktif Mentilerim" yalnız sayı; menti listeleyen bileşen yok. | ~~🟡~~ 🟢 | Mentör aktif mentilerini panelde listeleyebiliyor | BITTI | PR #82(BE)+#222(FE)+#223(pointer) merged, CANLIDA. `dashboard-metrics`'e `activeMentees` (sayacın arkasındaki `{id,fullName}` listesi, sayı≡liste) + panelde 'Mentilerim' listesi. CANLIDA BAK: mentör panelde mentilerinin adlarını listeliyor. =MT9. |
| P-14 | Ş0 | **Mentöre takdir/teşekkür cümlesi yok.** "Kendi etkim" tamamen sayısal; emeği anlatan tek cümle yok (tek olumlu dönüş NPS sayısı, veri yoksa "—"). | ~~🟡~~ 🟢 | Mentör emeğini anlatan takdir mesajı görüyor | BITTI | PR #216 merged (CI 8/8). Saf `mentorAppreciation(metrics)` + metrik altı '💚' banner; aktif menti + tamamlanan görüşmeye göre cümle. CANLIDA BAK: mentör panelinde emeğini anlatan takdir cümlesi. =MT10. Kanıt: mentör dosyalarında takdir/teşekkür/etki 0 eşleşme. İçerik/UX. Efor S |
| P-15 | Ş0 | **Mentör kapasite/doluluk dengesi yok.** "Aktif Mentilerim" bir tavanla kıyaslanmıyor; "hangi noktadan sonra yük" kavramı kodlanmamış. | 🟡 | Mentör doluluk/kapasite durumunu görüyor | BEKLIYOR | 🟡 KALIR: kapsam belirsiz — "kapasite kavramı olsun mu" ürün kararı (kart gerekebilir). =MT13 (⬜). Kanıt: capacity/kapasite/maxMenti 0 sonuç (mentör-bazlı). ⚠️ kapsam belirsiz — "kapasite kavramı olsun mu" PO'ya sonraki tur (kart gerekebilir). Efor M ⚠️ **KARTSIZ GİZLİ 🔴 (2026-09-21):** ürün kararı gerekebilir ama **kart YOK** → PO cevaplayamaz çünkü soru SORULMAMIŞ. Kart açılana kadar bu satır fiilen kilitli. ⚠️ = psikometri konseyi §5 D.5, ek bulgu: "KARTSIZ GİZLİ 🔴" durumu KAPANDI — kontenjan sorusu artık kart olarak açıldı (KARAR-??, güvenlik konseyinden sonra numaralanacak). Kanıt: kapasite kavramı kodda YOK (kapsam BE src/ · prisma/ · tests/, 7 terim iki dilli harf duyarsız → ilgili 0 eşleşme) ve scoreAndFilter'ın hiçbir continue koşulu (matching.ts:267,270,274,278,283) aktif menti sayısını sorgulamıyor. |
| P-16 | Ş4 | **mentor-count sayımı `User.role` üzerinden.** Kural "kurum-içi sayım kaynağı `TenantMembership.role`" diyor. Çok kurumlu kullanıcıda sayı şişer. | 🟢 | Sayım TenantMembership.role üzerinden, çok kurumluda doğru | BEKLIYOR | 🟡 KALIR: TEYİT GEREK (çok kurumlu veri) + rol/sayım kaynağı, P-00 k-anonimlik ucuyla ilişkili. =§6 yan bulgu. Kanıt: `userController.ts:125-135` `User.role`. ⚠️ TEYİT GEREK (çok kurumlu canlı veri var mı bilinmiyor). Efor S ⚠️ **KAPI DÜZELTİLDİ (2026-09-21): 🟡 → 🟢.** 🟡 gerekçesi *"TEYİT GEREK (çok kurumlu veri)"* idi — bu **üç istisnanın hiçbiri değil** (migration yok · auth/KVKK/matching dosyası yok · geri dönülmez dokunuş yok). `User.role` → `TenantMembership.role` sayımı.|
| P-99 | Ş0 | **⭐ Sertifika içeriğini seed dosyasına taşı — K-16'nın ÖNKOŞULU.** Finalize 22 senaryo/88 şık (`docs/raporlar/icerik/sertifika-oturum1/2/3-...-2026-09-08.md`) seed'e taşınmamış; `seed-certification.ts` hâlâ 20/80 eski sürüm. | 🟡 | seed-certification.ts 22 senaryo/88 şık içeriyor (seed ÇALIŞTIRILMAZ) | BEKLIYOR | 🟡 KALIR: SEED (seed-certification.ts, K-16 önkoşulu). TUR 3 B.2 bulgusu. ~~[ESKİ · 2026-09-21] K-16 seed atarsa yeni 2 senaryo+8 şık ÇIKMAZ.~~ ⚠️ **SAYI DÜZELTMESİ-2 (2026-09-21, içerik konseyi ① — kaynak-teyitli):** ~~[ESKİ · 2026-09-21] fark yalnız **2 senaryo + 8 şık** (20/80 → 22/88)~~ — **BU YANLIŞTI.** Doğrusu: `tam.md` (= seed'in birebir kaynağı, `seed-certification.ts:7`) 20 sahne sundu, finalize içerik bunların **yalnız 5'ini** aldı (%25) — yani seed'deki **20 senaryonun 15'i gerekçeli olarak ELENDİ** ("ELENEN SAHNELER" tabloları: yüzey ayrımı/çelişki). Kalan **5'inin 5'i de yeniden yazıldı** (5/5'inde metin farkı, **birinde puan anlamı TERS DÖNDÜ**). ⛔ Pratik sonuç: taşınacak şık sayısı 8 değil **88'in TAMAMI**; düşecek senaryo **15**. Efor **S değil L**. Kanıt: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md:16-19,118-122`. Seed dosyası değişir, seed çalıştırılmaz. ~~[ESKİ · 2026-09-21] Efor M~~ → **Efor L** |

> **Kapsam denetimi (İŞ2 disiplini):** Rapor §2/§3'te 11 🟡 + 3 ⬜ = **14 açık kalem** var; hepsi kuyruğa alındı
> (M3→P-06 · M5→P-05 · M6→P-07 · M8→P-08 · M9→P-01 · M13→P-02 · MT1→P-04 · MT2→P-09 · MT5→P-10 · MT6→P-11 · MT7→P-12 · MT9→P-13 · MT10→P-14 · MT13→P-15).
> Ek: P-03 (DISC rapel, §6#3 — M1/MT3 ✅ ama gap) · P-00+P-16 (güvenlik) · P-99 (seed önkoşulu). **Alınmayan:** ✅ VAR (11) + ❓ M11 (his, ölçülemez) — İŞ2 gereği doğru.

## AŞAMA U — UÇTAN UCA YOLCULUK BULGULARI (2026-09-19)

Kaynak: `docs/raporlar/kesif/uctan-uca-kurum-yolculugu-2026-09-19.md` (X turu). Kayıt→ilk görüşme hattı; zincir **9 yerde kopuyor (K1-K9, 6'sı sessiz)**, elle müdahale mutlu yolda 5.
Sıra: §6 (en kritik 5) → §10 (24 kalem) kalanları. Kapı = güncel politika (varsayılan 🟢; migration/seed·auth/KVKK/matching·geri-dönülmez → 🟡; ürün kararı → not'ta KARAR aday, merge YOK).

⚠️ **Numaralandırma notu (çakışma kontrolü uygulandı):** §6 kritik **#1 (bookMeeting 409 + FE "uygun" sayıyor) = K-05** ile ÖRTÜŞÜR → yeni satır AÇILMADI, K-05 Not'una işlendi. Bu yüzden U-satırları kritik #2'den başlar. §10#1,#2 → K-05; §10#22 (k-anon) → P-06/P-16 kümesine işlendi; §10#14 (ortak `EmptyState` bileşeni) rapor "numara adayı: hayır" → satır açılmadı (U-10 içinde değinilir).
⚠️ **PO/env bağımlı kalemler** (kurum bildirimi, SMTP) `03-PO-ELLE-ISLER.md`'ye de girdi; buradaki satır yalnız **AJAN kısmını** taşır, Not'ta "PO ADIMI OLMADAN ETKİSİZ" ile.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| U-01 | Ş2 | **`SCHEDULED → COMPLETED` geçişi yok.** UI yok, cron yok → check-in + feedback (değerlendirme) hiç açılmıyor; ürünün veri motoru boş kalıyor. | 🟡 | Görüşme sonrası menti/mentör değerlendirme akışını görüyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2)** — `SCHEDULED→COMPLETED` geçişi yok — görüşme hiç "tamamlandı" olmuyor → check-in + değerlendirme hiç açılmıyor · ⚠️ ÜRÜN KARARI GEREKLİ (KARAR aday, merge YOK): kim/ne zaman COMPLETED işaretler — mentör mü / görüşme saati+X sonrası otomatik mi. Ajan kısmı: geçiş mekanizması + FE. = X §6#2 / §10#3. Kanıt: tek yazıcı `updateMeetingStatus`; FE'den PATCH **0**. Efor M ⚠️ **KARTSIZ GİZLİ 🔴 (2026-09-21):** KARAR gerekli ("kim/ne zaman COMPLETED işaretler") ama **kart YOK** → PO cevaplayamaz çünkü soru SORULMAMIŞ. Kart açılana kadar bu satır fiilen kilitli.|
| U-02 | Ş2 | **Toplantı linki hiçbir ekranda görünmüyor.** `locationUrl/locationText/phoneNumber` 3 alana yazılıyor, 0 yerde render ediliyor; backend zaten JSON'da gönderiyor. | 🟢 | Online görüşmenin linki randevu/görüşme kartında görünüyor | BITTI | PR #201 merged, CI 8/8. `Meeting` tipine 3 alan + `MeetingCard`'a formata göre link/konum/telefon. Test 4 yeni (`meetings-location.test.tsx`). CANLIDA BAK: menti/mentör "Görüşmelerim"de online görüşmenin katılım linkini görüp tıklıyor; yüz yüze konum, telefon numara. ⚠️ = güvenlik konseyi §D.2 (D-6), ek bulgu: bu satırla eklenen `locationUrl` render'ı (`meetings/page.tsx:69` `<a href>`, link metni sabit `:74`) doğrulanmamış bir alanı tıklanabilir hale getiriyor; kaynak açığı (şemada `.url()` yok, `meetingController.ts:404`) U-02'den ÖNCE de vardı. Düzeltme GV-03'te (giriş + çıkış iki noktada). |
| U-03 | Ş4 | **Davet ekranı sessiz + kurum adı boş.** `/admin/invite`'ta `setMsg` tanımlı ama hiç çağrılmıyor (403'ler görünmez); `{KurumAdı}` boş dizeye çevriliyor → davet metninde kurum adı yok. | 🟢 | Davet hatası ekranda görünüyor, kopyalanan metinde kurum adı dolu | BITTI | PR #204 merged, CI 8/8. generateLink/saveTemplate hatası artık `setMsg` ile görünür; `{KurumAdı}` `useTenant()` ile gerçek ad. Test 2 yeni. CANLIDA BAK: davet linki/şablon hatası ekranda; kopyalanan metinde kurum adı dolu. |
| U-04 | Ş4 | **Kurum onay/ret durumu uygulama-içinde sessiz.** Bekleme ekranı e-posta sözü veriyor ama bildirim bayrağı kapalı → kurum belirsiz süre bekler, uygulama-içi durum göstergesi yok. | 🟢 | Kurum onay/ret durumunu uygulama içinde net görüyor | BITTI | PR #224 merged (CI 8/8), CANLIDA. `pending-review` artık `/api/auth/me`'den `tenant.verificationStatus` okur: onaylandı→olumlu+giriş · reddedildi→dürüst+gerekçe · düzeltme→not · bekliyor/oturumsuz→'inceleniyor'. CANLIDA BAK: kurum yöneticisi bekleme ekranında gerçek durumunu görüyor. 5 test. = X §6#5 / §10#8. ⚠️ E-posta bildiriminin AÇILMASI (§4.1 KARAR + SMTP) hâlâ PO ADIMI → 03-PO. |
| U-05 | Ş4 | **Yeni kurum başvurusunda platform admin'e bildirim yok.** `selfServeRegister` gövdesinde 0 bildirim çağrısı → PO panele elle bakmadıkça bekleyen kurumu görmüyor. | 🟢 | Platform admin panelinde bekleyen başvuru rozeti/göstergesi var | BITTI | ZATEN YAPILMIŞ (kod-teyit, §5c): platform dashboard'da **kırmızı 'Bekleyen Başvurular' sekme rozeti** (`platform/dashboard/page.tsx:160,196-200`, `stats.totals.pendingTenants`) + genel bakışta **kırmızı 'Bekleyen Başvuru' kartı** (`:239,247`). Uygulama-içi gösterge VAR → agent-parça bitti. ⚠️ E-posta bildirimi = PO ADIMI (SMTP, 03-PO). = X §10#7 / K1. |
| U-06 | Ş0 | **Davetli kullanıcı OAuth (Google) ile gelirse APPROVED olmuyor.** PENDING'de sessiz kalır; mentör/menti ekranında bunu söyleyen uyarı da yok. | 🟡 | OAuth ile gelen davetli ya APPROVED olur ya da net uyarı görür | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2+T3)** — davetli OAuth'la gelirse APPROVED olmuyor — davet ettiğin kişi sessizce PENDING'de kalır · 🟡 KALIR: auth (OAuth token/onay akışı). = X §5 K4 / §10#9. ⚠️ F-03 (OAuth rıza) ile aynı `oauthService.ts` → SIRALI. Kanıt: `oauthService.ts:109,135-140`; `authController.ts:163-164`. Efor M |
| U-07 | Ş0 | **LOCAL PENDING kullanıcı `/pending-approval`'da kendi e-postasını boş görüyor** (token yok → `user` null). | 🟢 | Bekleyen kullanıcı kendi e-postasını ekranda görüyor | BITTI | PR #202 merged, CI 8/8. Login formu e-postayı `?email=` query'siyle taşır; sayfa `user?.email ?? query` (Suspense sınırı). Test 3 yeni. CANLIDA BAK: bekleyen kullanıcı `/pending-approval`'da kendi e-postasını görüyor (eskiden boştu). |
| U-08 | Ş0 | **Onay kapısı yalnız `GET /api/users`'ta; `mentor-matches` PENDING'e açık.** `requireApproved` benzeri middleware yok → onaylanmamış menti eşleşme verisi alabilir. | 🟡 | PENDING kullanıcı eşleşme verisine erişemiyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — onay kapısı yalnız `/api/users`'ta — onaysız kullanıcı eşleşme verisi alabiliyor (❓ TEYİT) · 🟡 KALIR: auth (erişim kapısı) + ❓ TEYİT GEREK (gerçek hesapla denenmeli). = X §10#11 / §8#6. Kanıt: `matching`/`mentor-matches` uçlarında onay middleware yok. Efor M ⚠️ = güvenlik konseyi §2.A.5 (G-10), ek bulgu: ❓TEYİT ayağı KODLA kapandı — `matching.ts:355-366` çağıranı yalnız `isActive` + MENTI üyeliğiyle doğruluyor, `approvalStatus` HİÇ okumuyor (hedefler `:385` filtreli). Sızan alanlar: onaylı mentörlerin ad/avatar/sektör/uyum yüzdesi. Karşı örnek aynı kuralı doğru uygulayan `userController.ts:69-73`. Bu haftaki k-anonimlik açığıyla (P-00) birebir aynı sınıf. |
| U-09 | Ş4 | **Sıfır-kullanıcılı admin panelinde "davet et" yönlendirmesi yok + yanıltıcı metin.** `approvals` boş durumu "🎉 Tüm kayıtlar işlendi." diyor — hiç kayıt işlenmemişken. | 🟢 | Yeni kurum admini boş panelde "davet gönder" yönlendirmesi + doğru metin görüyor | BITTI | PR #203 merged, CI 8/8. Nötr metin + `/admin/invite`'a "Davet gönder" düğmesi (approvals + waiting-room). Test 1 yeni. CANLIDA BAK: yeni admin boş panelde yanıltıcı "işlendi" yerine doğru metin+davet düğmesi görüyor. |
| U-10 | Ş0 | **4 ekranda boş-durum hiç ele alınmamış:** `admin/questions`, `admin/certification`, mentör toplantı talepleri kartı (tamamen kaybolur), `book-meeting` müsaitlik kartı. | 🟢 | Bu 4 ekran boşken anlamlı boş-durum metni gösteriyor | BITTI | PR #221 merged (CI 8/8). admin/questions DISC+kuruma-özel boş-durum (kart kaybolmuyor) + book-meeting müsaitlik boşken yönlendirici kart. Kapsam denetimi: mentör toplantı talepleri P-09'da (PR #210) zaten çözülmüş; admin/certification pratikte hiç boş olmuyor → ikisine dokunulmadı. Boş-müsaitlik testi. = X §10#13. |
| U-11 | Ş4 | **Davet süresi üç yerde üç farklı:** ekran "90 gün", panel "30 gün", kod 30 gün; `INVITATION_TOKEN_EXPIRY` ölü ayar. | 🟢 | Ekranda gösterilen davet süresi kodun gerçeğiyle (30 gün) aynı | BITTI | PR #205 merged, CI 8/8. `Step5Invite.tsx` metni 30 güne hizalandı (kod gerçeği `expiresIn '30d'`). Test 1 yeni. ⛔ ölü `config.invitationTokenExpiry`(90d) SİLİNMEDİ (grep: 0 okuma; SİLME PROTOKOLÜ/PO). `gizlilik` "90 gün"=log saklama, dokunulmadı. CANLIDA BAK: onboarding son adımında davet süresi 30 gün. |
| U-12 | Ş0 | **Davet token'ı e-postaya bağlı değil, tek kullanımlık değil, iptal edilemez, 30 gün geçerli.** | 🟡 | Karara göre davet token modeli güçlendirildi | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — davet token'ı e-postaya bağlı değil, iptal edilemez, 30 gün — link sızarsa yabancı kalıcı üye olur · ⚠️ ÜRÜN/GÜVENLİK KARARI GEREKLİ (KARAR aday, merge YOK) + auth. = X §10#16. Kanıt: `selfServeController.ts:562-572`. Efor M ⚠️ **KARTSIZ GİZLİ 🔴 (2026-09-21):** ÜRÜN/GÜVENLİK KARARI gerekli (davet token modeli) ama **kart YOK** → PO cevaplayamaz çünkü soru SORULMAMIŞ. Kart açılana kadar bu satır fiilen kilitli. ⚠️ = güvenlik konseyi §2.C.3, ek bulgu: token modelinden BAĞIMSIZ bir sızıntı kanalı var — davet JWT'si URL path'inde taşınıyor (`invitationRoutes.ts:13`) ve `requestLogger.ts:22-23` `originalUrl`'i olduğu gibi stdout'a basıyor → log'a erişen herkes geçerli davet token'ı toplar. Bu kanal ayrı ve daha ucuz kapanır → **GV-14** olarak ayrı satır açıldı; U-12 kartı beklerken GV-14 ilerleyebilir. |
| U-13 | Ş4 | **Rol değiştiren (MENTOR↔MENTI) hiçbir uç yok; ADMIN düşürme körlemesine `MENTOR` yazıyor** (`adminController.ts:959`) → aslında MENTI ise rolü bozulur. | 🟡 | Yanlış rolle kayıt düzeltilebiliyor; ADMIN düşürme rolü bozmuyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1+T3)** — rol değiştirme ucu yok, ADMIN düşürme körlemesine `MENTOR` yazıyor — rol sessizce bozulur · 🟡 KALIR: auth/yetki (rol atama). = X §4.5 / §10#17. İki parça: (a) rol-değiştirme ucu yok (ürün) · (b) `:959` hardcoded MENTOR bug'ı. Kanıt: `UpdateUserSchema` `.strict()` (role yok); `adminController.ts:959`. Efor M ⚠️ = güvenlik konseyi §2.A.4 (G-8), ek bulgu: rol kaynağı sorunu yazma ucuyla sınırlı değil — `requireTenant` rolü `TenantMembership`'ten DEĞİL JWT payload'ından okuyor (`tenant.ts:82-85` membership'i çekiyor ama `select:{isActive:true}`, rol seçilmiyor; `:102` `role: payload.role`) → `demoteFromAdmin` (`adminController.ts:951-978`) iki tabloyu da güncellese bile token ≤1 saat ADMIN kalır. Ayrıca admin SAYIMLARI `prisma.user.count({role:'ADMIN'})` ile yapılıyor (`:909,929,960`) → `ensureMembershipSafe` non-fatal olduğu için senkron bozulursa `MAX_ADMINS` ve "son admin" koruması yanlış sayıya dayanır. Token iptali ayağı GV-10'da. |
| U-14 | Ş0 | **Süresi dolmuş davet/sıfırlama ekranlarında "yeni link iste" düğmesi yok** (REJECTED akışında var — şablon hazır). | 🟢 | Kullanıcı süresi dolmuş linkte "yeni link iste" düğmesi görüyor | BITTI | PR #212 merged (CI 8/8). Join hata ekranına 'Yeni davet iste' (mailto:destek) + 'Giriş yap' düğmeleri (pending-review deseni). CANLIDA BAK: geçersiz/expired davette eyleme dönük düğmeler. = X §4.5 / §10#18. Kanıt: `_ResetPasswordContent.tsx:32-37` (reset'te var, expired'da yok); davet expired ekranında düğme yok. Yalnız FE. Efor S |
| U-15 | Ş0 | **Şifre sıfırlama tek kanala (SMTP) bağımlı;** SMTP hatası kullanıcıya hiç yansımıyor, admin tarafında sıfırlama ucu yok → mail kapalıyken şifresini unutan geri giremez. | 🟡 | SMTP hatası kullanıcıya yansıyor / admin sıfırlama yolu var | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2+T3)** — şifre sıfırlama tek kanala bağlı, hata yansımıyor — şifresini unutan geri giremez, kimse görmez · 🟡 KALIR: auth (şifre sıfırlama). ⚠️ PO ADIMI: SMTP teyidi (`03-PO`). = X §4.4 / §10#19. Kanıt: `authController.ts:534-550`; `UpdateUserSchema` password yok. Efor M ⚠️ = güvenlik konseyi §2.C.5, ek bulgu: sıfırlama akışının KENDİSİ örnek nitelikte (256-bit entropi `:534` · SHA-256 hash'li DB kaydı `:535-541` · 60 dk · tek kullanımlık `:580-587` · kullanımda tüm refresh token'lar silinir `:586` · pasif hesaba token verilmez `:531,568`) — sorun kanal bağımlılığında. Ayrı eksik: oturum-içi şifre DEĞİŞTİRME ucu hiç yok ve karmaşıklık kuralı yok → GV-19 (aynı dosya, SIRALI). |
| U-16 | Ş0 | **`emailService.send()` başarı/başarısızlık bilgisini çağırana hiç vermiyor** → iki yanlış rapor: `feedbackController.ts:205` mail gitmese de "gönderildi" der; `cronScheduler.ts:143-153` mail atlansa da `reminderEmailSentAt` yazar (tek atımlık hatırlatma yakılır). | 🟢 | Mail gitmezse "gönderildi" yalanı ortadan kalkar, hatırlatma yakılmaz | BITTI | PR #83(BE)+#223(pointer) merged, CANLIDA. `send()`+2 sarmalayıcı boolean döner; feedbackController `delivered` (gerçek teslim, `count`=işlenen→V-07 korunur); cronScheduler `reminderEmailSentAt` yalnız teslimde yazar. CANLIDA BAK: hatırlatma yanıtı gerçek gönderim sayısını gösterir; SMTP yokken tek-atımlık hatırlatma yanmaz. `send()` dönüş 2 test. = X §4.4 / §10#20. |
| U-17 | Ş0 | **Temiz DB'de DISC havuzu boş kalıyor ve yalnız yıkıcı `prisma/seed.ts` ile dolabiliyor** (`createQuestion` DISC'i 403'lüyor). Yeni ortam kurulurken patlar. | 🟡 | Temiz DB'de DISC havuzu güvenli yolla dolabiliyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2)** — temiz DB'de DISC havuzu boş — yeni ortam kurulurken patlar; tek yol yıkıcı `seed.ts` · 🟡 KALIR: SEED + yıkıcı `seed.ts` riski (PO kararı). = X §4.6 / §10#21. Kanıt: `questionService.ts:174`; `questionController.ts:124-129`; `seed.ts:295-319` koşulsuz `deleteMany`. Efor L ⚠️ = güvenlik konseyi §3.3, ek bulgu: aynı dosyada ikinci sorun — `prisma/seed.ts` prod'da çalışmayı ENGELLEYEN guard taşımıyor (*kapsam:* `prisma/seed.ts`, desen `NODE_ENV|isProd` harf duyarsız → 0) ve `:293` sabit bir seed şifresi içeriyor (`:383` yorumu düz metin tekrarlıyor). U-17 ile BİRLİKTE yapılmalı: dosyaya dokunulurken guard da eklensin. ⚠️ = psikometri konseyi §4 C.1, ek bulgu: havuz boşken /disc-test DOĞRU davranıyor (DiscTestEmpty, K-02'de düzeltilmiş) ama /onboarding savunmasız (→ PS-11); ayrıca mantık hatası questionService.ts:173 ve questionController.ts:327'de 0 >= 0 ⇒ havuz TEK soruya düşerse ilk cevapta discAssessmentCompletedAt yazılır ve admin'e "test tamamlandı" e-postası gider. |
| U-18 | Ş0 | **`MatchRequest` durumsuz → mentörün mesaj talebini kabul/ret kapısı yok;** `Match` tablosuna yazan kod yok (`createMatchIfEligible` 0 çağıran). | 🟡 | Mentör mesaj talebini kabul/ret edebiliyor (karara göre) | BEKLIYOR | 🟡 KALIR: matching (MatchRequest/Match akışı) + ürün kararı payı. = X §3 K6 / §10#23 / §8#4. ⚠️ P-05 (görüşme reddi) ile karıştırma — bu MESAJ talebi. Kanıt: `schema.prisma:439-456` status yok. Efor M ⚠️ `= psikometri konseyi §5 D.1, ek bulgu: Match boşluğunun sonucu üç yüzeyi birden öldürüyor (mentör %uyum rozeti · yöneticinin /admin/eslesmeler tablosu · çift risk sinyali GREEN/YELLOW/RED) ve Meeting.matchId yazılabilir durumda (meetingController.ts:400,513) ama /book-meeting'e link veren tek yer menti/page.tsx:325 yalnız mentorId gönderiyor.` **+ ⛔ çapraz atıf (bkz. §4)** ⛔ **BİRLİKTE YAPILMALI — önce silme yolu düzeltilir, SONRA Match yazımı açılır. Ters sıra = KVKK ihlali.** *(karşı taraf: **GV-08** — anonimleştirme `Match.mentorArchetype`/`mentiArchetype`'ı atlıyor; `Match` yazımı GV-08 bitmeden AÇILMAZ. Kaynak: psikometri konseyi §5 D.1 ① ↔ güvenlik konseyi §2.B.2 ③.)* |
| U-19 | Ş0 | **Profil tamamlanma kapısı yok:** boş profilli mentör havuzda görünüyor; `computeProfileCompleteness` ve `mentorVisibilityEnabled` bağlanmamış. | 🟡 | Yalnız profili tamam mentör havuzda görünüyor | BEKLIYOR | 🟡 KALIR: matching/havuz görünürlüğü. = X §10#24. Kanıt: `computeProfileCompleteness` + `mentorVisibilityEnabled` bağlı değil. Efor M · **= W §4#19 / §4.5 KARAR** (`mentorVisibilityEnabled` niyet önce — bağlan mı / terk mi; silme protokolü adım1 gerekli). Kanıt: `schema.prisma:323` tanım, `userController.ts:183` salt-okuma, `matching.ts` filtrelerinde yok. `mentorVisibilityEnabled` bağlan mı/terk mi — ⭐ **KARAR-32 ile kapsandı (2026-09-21)**|

> **Örtüşme/alınmayan (X):** §6#1 & §10#1,#2 → **K-05** · §10#22 (k-anon) → **P-06/P-16** · §10#14 (ortak `EmptyState`, "numara adayı: hayır") → U-10 içinde. §7 (11 "zaten iyi"), §8 (TEYİT GEREK, PO canlı teyidi) satır ALMADI — §8 teyitleri `03-PO-ELLE-ISLER.md`/09-DURUM'a.

## AŞAMA V — OPERASYONEL HAZIRLIK BULGULARI (2026-09-19)

Kaynak: `docs/raporlar/kesif/operasyonel-hazirlik-2026-09-19.md` (W turu). İzleme·yedek·koruma·ortam. Sıra: raporun **§4 RİSK SIRALAMASI** (1 en tehlikeli üstte).
⚠️ **AYRIM (raporun "Kim çözer" sütunu):** yalnız **AJAN** kısmı buraya girer. **PO** kısmı (Dokploy/hesap/SMTP/Neon/sunucu) → `03-PO-ELLE-ISLER.md`; kuyruğa YAZILMADI. İkisi de gerekiyorsa satır AJAN kısmını taşır, Not'ta "⚠️ PO ADIMI OLMADAN ETKİSİZ".
⚠️ **Yeni 🔴 açılmadı** (🔴 sayısı 12 sabit). Ürün kararı gereken kalemler 🟡 + "KARAR aday" ile işaretlendi (merge YOK).
⚠️ **Çakışma/PO-only nedeniyle satır AÇILMAYAN riskler:** #1 avatar disk → **K-04** (PO→03-PO) · #3 trust proxy/`X-Tenant-Id` → **K-14** (⛔ yasak bölge) · #4 kurum bildirimi → **U-04** + Bölüm4/03-PO · #7 6-saat yedek + restore provası → **madde 120 / [G1-28] 🔴 çıkış blokeri** (§9.1: yeni numara VERİLMEZ, mevcuda bağlanır) → 03-PO · #12 yedek tablo DROP (S26/S37) → §4.4 KARAR + 03-PO · #14 `NEXT_PUBLIC_API_URL` build-time → PO (03-PO) · #18 `RATE_LIMIT_RPM` mimarisi → yasak bölge (yalnız Bölüm4 .env belgesi) · #19 `mentorVisibilityEnabled` → **U-19** + §4.5 KARAR. Belgelenmemiş env (§7#23/#24/#25) → **Bölüm 4** (.env.example).

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| V-01 | Ş0 | **`SMTP_HOST` boşsa/hatalıysa tüm mailler sessizce atılıyor** — çağıran katman `void`/`.catch`, dönüş değeri kontrol edilmiyor. | 🟢 | Mail gönderimi başarısızsa operatör bunu görebiliyor | BITTI | PR #84(BE)+#227(pointer) merged, CANLIDA. Açılışta `verifyTransporter()` + `/health`'e `smtp` durumu (önbellekli). CANLIDA BAK: `/health` yanıtında smtp alanı; SMTP bozuksa 'failed'. ⚠️ PO ADIMI: canlı SMTP değerleri → `03-PO`. = W §4#2 / §7#2. |
| V-02 | Ş0 | **500'ler + süreç çöküşü teşhis edilemiyor** — hata kaydında endpoint/kullanıcı/kurum yok (E1); `uncaughtException`/`unhandledRejection` handler'ı yok (E3) → sessiz restart. | 🟢 | Bir 500'ün hangi ekran/kurum/kullanıcı olduğu log'dan ayırt edilebiliyor | BITTI | PR #84(BE)+#227(pointer) merged, CANLIDA. 500 log meta'sına url+method+userId+tenantId (⛔ e-posta/ad ASLA) + `uncaughtException`/`unhandledRejection` handler'ları. ⛔ `server.ts` rate-limit/trust-proxy YASAK BÖLGE'ye dokunulmadı. = W §4#5 / §7#6,#8. |
| V-03 | Ş0 | **🔴 IDOR: `POST /api/mentors/:mentorId/visibility-optin` sahiplik kontrolü yok** — başkası adına opt-in yazılabiliyor. | 🟢 | Kullanıcı başkasının görünürlük ayarını değiştiremiyor | BITTI | ✅ PR #76 (backend) merged, CI 468 test (yeni 3: `visibility-optin-idor.test.ts`). `requireSelfOrAdmin('mentorId')` eklendi. CANLIDA BAK: bir mentör başka mentörün ID'siyle opt-in yazmaya çalışınca **403** (sahibi/ADMIN yazabilir). = W §4#6 / §7#9. Kanıt: `userRoutes.ts:89-93`. Efor S |
| V-04 | Ş0 | **🔴 `/health` DB'ye bakmıyor** ama Docker healthcheck onu kullanıyor + frontend `depends_on: service_healthy` → yalancı "healthy". | 🟢 | DB düştüğünde `/health` 503 dönüyor, konteyner "unhealthy" görünüyor | BITTI | ✅ PR #77 (backend) merged, CI 467 test (yeni 2: `health.test.ts`). `getHealthStatus()` hafif `SELECT 1`; DB down → `ok:false`/`db:down`/503. CANLIDA BAK: Postgres düşünce `/health` 503 → Docker konteyner "unhealthy" (eskiden yalancı healthy). = W §4#8 / §7#10. Kanıt: `server.ts` + `src/services/health.ts`. Efor S |
| V-05 | Ş4 | **k-anonimlik KPI/analytics/health-metrics uçlarında YOK** — `applyKAnonymity` yalnız `mentor-count`'ta; `GET /api/admin/kpi` ve `/platform/tenants/:id/analytics` küçük kurumda tek kişinin DISC'ini ifşa ediyor. | 🟡 | Küçük kurumda KPI/analytics tek kişinin kimliğini/DISC'ini ifşa etmiyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — k-anonimlik KPI/analytics/health-metrics'te YOK — küçük kurumda `/admin/kpi` tek kişinin DISC'ini ifşa ediyor · 🟡 KALIR: KVKK/k-anonimlik. = W §4#9 / §7#14. ⚠️ P-00 yalnız `mentor-count`'u maskeledi; bu FARKLI uçlar (P-00/P-16 kümesiyle akraba). Kanıt: `mask.ts:70` tek çağrı `userController.ts:140`; `admin/kpi/page.tsx:44-49` ham. Efor S |
| V-06 | Ş0 | **Fallback JWT secret kaynak koda gömülü** (`config.ts:16`); tek koruma `NODE_ENV==='production'`. Set değilse 7 koruma birden düşer. | 🟡 | JWT için gömülü fallback secret kaldırıldı | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — fallback JWT secret koda gömülü — depo PUBLIC; A3 tutmazsa token sahtelenebilir · 🟡 KALIR: auth (JWT secret). = W §4#10 / §7#15. ⚠️ PO ADIMI: canlı `NODE_ENV` teyidi (`GET /health`→`env`) → `03-PO`. Azaltıcı: `Dockerfile:39`+`compose:43` `NODE_ENV=production` ✅. Efor S ⚠️ = güvenlik konseyi §2.C.2, ek bulgu: V-06 yalnız gömülü JWT secret'ı konu ediyor; asıl kırılganlık `NODE_ENV`'in kendisi — 7 koruma (3 fail-fast guard `config.ts:12-28` + 4 cookie `secure` bayrağı `authController.ts:67,74`·`selfServeController.ts:33`·`platformController.ts:17`) tek stringe asılı ve durum `GET /health`→`env` ile kimlik doğrulamasız dışarıya ifşa ediliyor (`health.ts:45`, `server.ts:60-63`); açılışta "bilinen ortam" doğrulaması yok. |
| V-07 | Ş0 | **`POST /api/meetings/reminders/send` sınırsız toplu mail** — görüşme başına 2 mail, batch/cooldown yok → kurum spam'lenebilir, SMTP itibarı yanar. | 🟢 | Hatırlatma gönderimi cooldown/batch ile sınırlı | BITTI | backend PR #79 merged (CI pass). Çağrı başına batch tavanı + toplantı başına in-memory cooldown (şema değişmeden); yanıt skippedCooldown+remaining. ⚠️ CANLIYA pointer bump ile çıkar. = W §4#11 / §7#16. Kanıt: `feedbackController.ts:189-203`. Efor S |
| V-08 | Ş0 | **`BACKEND_URL` backend'e geçirilmiyor** → `backendBaseUrl = FRONTEND_URL` olur → avatar public URL'leri + KVKK zorunlu unsubscribe linki 404. | 🟢 | Avatar URL'leri ve unsubscribe linki doğru domaine gidiyor | BITTI | PR #225 merged, CANLIDA. `docker-compose.yml` backend servisine `BACKEND_URL` env eklendi. ⚠️ PO ADIMI: prod `BACKEND_URL` değeri set edilmeli → `03-PO`. = W §4#13 / §7#17. |
| V-09 | Ş4 | **CLAUDE.md "kasıtlı public uç" listesi kodla uyuşmuyor** — listede olmayan 11 public uç (+`/uploads`). Özellikle `POST /api/tenants/self-serve/register` kalıcı kurum+ADMIN yaratıyor, listede yok. | 🟢 | Belge public uç listesi kod gerçeğiyle uyuşuyor | BITTI | `CLAUDE.md` § "Güvenlik Kuralları — kod yazarken UY" › *KASITLI public olan endpoint'ler* güncellendi — 6 uçtan **17 uca** *(⚠️ 2026-09-21: eski `:453` atfı hem bayattı hem CLAUDE.md bölmesinde kırıldı → bölüm adına çevrildi)* (kod-teyitli tam liste: auth/refresh/logout/forgot/reset/OAuth + platform auth/logout + self-serve check-slug/register + /uploads). Denetim: 17 route dosyası tarandı, guard'sız uçlar listelendi (KURAL 13). = W §4#15 / §7#19. ⚠️ = güvenlik konseyi §2.A.1, ek bulgu (BITTI durumunu değiştirmez): 190 uç yeniden sayıldı (188 rota + 2 inline), "kazara public" 0 — listede olmayan 11 ucun hepsi ya token/parola ile kimlik kanıtlıyor ya rate-limit + yazılı gerekçeyle açık. Kalan yapısal risk: `GET /api/auth/:provider` catch-all (`authRoutes.ts:55`) → GV-21. ⚠️ = yönetişim konseyi §A.5, ek bulgu: Not'taki `CLAUDE.md:453` atfı YANLIŞ — `:453` KURAL 16 ADAYI gövdesiydi; public uç listesi bugün `CLAUDE.md:399-408`. Satır no yerine bölüm adı yazılmalı: `CLAUDE.md § Güvenlik Kuralları → KASITLI public olan endpoint'ler`. |
| V-10 | Ş0 | **`GET /api/users/:id/export` rate limitsiz** — ikizi `/me/data-export` 5/dk korunuyor; `:id`'ye kendi ID'sini yazan limiti atlatıyor. | 🟢 | `:id/export` ağır KVKK export'u da rate-limitli | BITTI | backend PR #78 merged (CI pass). `dataExportRateLimiter` route'a eklendi (aynı `data-export:<userId>` bucket → bypass kapandı). ⚠️ CANLIYA pointer bump ile çıkar. = W §4#16 / §7#20. Kanıt: `userRoutes.ts:187` vs `:197`. Efor S |
| V-11 | Ş0 | **`CRON_ENABLED=false` yazılırsa 8 iş sessizce durur** (varsayılan AÇIK ✅) — KVKK imhası, anlaşma yenileme, geri bildirim hatırlatması durur. | 🟢 | Cron kapalıysa operatör `/health`'te görüyor | BITTI | PR #84(BE)+#227(pointer) merged, CANLIDA. `/health`'e `cron` alanı (`isCronEnabled()` → enabled/disabled). CANLIDA BAK: `/health` yanıtında cron durumu. = W §4#17 / §7(CRON). ⚠️ = güvenlik konseyi §2.B.4, ek bulgu (BITTI durumunu değiştirmez): elle tetik kaçış kapısı ASİMETRİK — `POST /api/admin/cron/run-purge` (`adminRoutes.ts:81`) `CRON_ENABLED` bayrağına BAKMIYOR (yani admin imhayı elle çalıştırabilir) ama `runDraftTenantCleanup` için böyle bir uç YOK → bayrak kapalıyken terk edilmiş taslak kurumların `User` kayıtları (ad+e-posta) 96 saat kuralıyla hiç silinemez. `cronScheduler.ts:29-31` doğrulandı: unset/`'FALSE'`/`'0'`/`'no'` = AÇIK, yalnız tam `'false'` kapatır. |
| V-12 | Ş1 | **🔴 Frontend'de sıfır gözlemlenebilirlik** — error boundary 0, `not-found.tsx` 0 → React render hatası = beyaz ekran, iz yok. | 🟢 | Render hatasında kullanıcı beyaz ekran yerine anlaşılır hata ekranı görüyor | BITTI | ✅ PR #197 (çatı) merged, CI 8/8. `app/error.tsx` + `global-error.tsx` + `not-found.tsx`. CANLIDA BAK: bir sayfa çökerse beyaz ekran yerine "Bir şeyler ters gitti" + Tekrar dene/Ana sayfa; bilinmeyen adres → 404 ekranı. Stack kullanıcıya gösterilmez (yalnız konsol). Dış servis (Sentry) EKLENMEDİ → KARAR-27. = W §4#20 / §7#22. Efor S |
| V-13 | Ş0 | **`POST /api/tags/suggest` `requireTenant`sız mount → her zaman 401** (fail-closed ölü uç). | 🟢 | `tags/suggest` doğru mount ile çalışıyor (ya da bilinçli kapatılıyor) | BITTI | backend PR #80 merged (CI pass). requireTenant+requireAuth eklendi (fail-closed 401 ölü uç çözüldü); FE caller yok. ⚠️ CANLIYA pointer bump ile çıkar. = W §7#26. ⚠️ YANLIŞ SORU TUZAĞI: "ölü" demeden önce niyeti/ikame yolu kontrol et. Efor S |
| V-14 | Ş0 | **`.dockerignore` migration `.sql`'lerini eliyor ama `Dockerfile` `migrate deploy` çalıştırıyor** → boş DB'ye kurtarmada şema oluşmayabilir. | 🟢 | Kurtarma imajında migration `.sql`'leri mevcut | BITTI | PR #84(BE)+#227(pointer) merged, CANLIDA. `.dockerignore`'dan `prisma/migrations/*/migration.sql` elemesi kaldırıldı (boş DB kurtarmasında `migrate deploy` şemayı bu dosyalardan oluşturur). ⚠️ TEYİT: gerçek doğrulama `docker build` (CI/PO). = W §7#28 / B.6. |
| V-15 | Ş2 | **Oryantasyon kilidi canlı `bookMeeting` yolunda uygulanmıyor** — yalnız kullanılmayan `createMeeting`'de bağlı; FE de yalnız banner basıyor → kilitli menti randevu alabiliyor. | 🟡 | Karara göre oryantasyonsuz menti randevu alamıyor (ya da uyarı olarak kalıyor) | BEKLIYOR | ⚠️ ÜRÜN KARARI GEREKLİ (§9.2 KARAR aday, merge YOK): "Görüşme Kilidi Aktif" uyarı mı gerçek engel mi. AJAN fix tek satır (`bookMeeting`'e `checkOrientationLock`). = W §9.2 / §9.3#29. Kanıt: `meetingController.ts:162` (yalnız createMeeting), `bookMeeting:413-531` 0 satır. Efor S ⚠️ **KARTSIZ GİZLİ 🔴 (2026-09-21):** ÜRÜN KARARI gerekli (oryantasyon kilidi) ama **kart YOK** → PO cevaplayamaz çünkü soru SORULMAMIŞ. Kart açılana kadar bu satır fiilen kilitli. ⚠️ = güvenlik konseyi §2.A.5 (G-11), ek bulgu: kilidi BASAN yer `feedbackController.ts:92-96` — yani GV-02 ile zincirli: saldırgan GV-02 ile kurbana kilit bastırır, kurban V-15 ile kilidi atlar; ikisi de düzeltilmeli. `checkOrientationLock` tanım `meetingController.ts:140`, tek çağrı `:162` (yalnız `createMeeting`). |

> **Örtüşme/alınmayan (W):** §5 (44 "zaten iyi") + §6 (taranamadı, PO canlı teyidi) satır ALMADI — PO teyitleri `03-PO-ELLE-ISLER.md`'ye. §9.3#30 (kalem 11-12 numara verilmesin) aksiyon değil, §9.1 yönergesiyle uygulandı (→ G1-28). Kalıntı riskler (§7 sonu: magic-byte polyglot, avatar silme best-effort, `superAdminRoutes` limitçi, `systemLogController` AUDIT enum, `ALLOWED_ORIGINS` trim) — düşük öncelik, bu turda satır ALMADI, W raporunda kayıtlı.

---
## AŞAMA Y — YOL HARİTASI + KARAR TAKİBİ DEVRİ (2026-09-21)

> **Kaynak:** `docs/raporlar/kesif/devir-analizi-2026-09-21.md` §3.1/§3.3/§3.4. `10-yol-haritasi.md` ve `00-CIKIS-PLANI.md` 📸 donduruldu; açık kalemleri buraya devredildi.
> ⚠️ **Her satır bu turda koda karşı TEK TEK doğrulandı** (backend `b5415bd`). Kaynak raporun **6 iddiası düzeltildi**, **1 satır hiç açılmadı** (aşağıda gerekçesiyle).
> ⛔ **AÇILMAYAN: madde 71** (`SuspicionReport.tenantId` yok). Olgu doğru ama kart **G1-04 ⚫ GEÇERSİZ** diyor (`docs/raporlar/bilanco/kararlar/G1-guvenlik-kvkk.md:100,105`): *public-create + platform-only-read → tenant izolasyon açığı değil, **tasarım kararı***. **KURAL 15: çelişkide KART kazanır** → satır AÇILMADI. Spam sertleştirme istenirse ayrı kart **G1-26** altında izlenir.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| Y-01 | Ş0 | **`ALLOWED_ORIGINS` boşluk toleransı yok.** `.split(',')` trim etmiyor → env'de virgülden sonra boşluk varsa o origin CORS'ta **hiç eşleşmez**, site açılmaz, hata mesajı da yok. | 🟢 | Boşluklu env değeriyle de origin eşleşiyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T2)** — PO'nun B#8 işinin **kod tarafı**; bugün "boşluk koymamayı hatırlama" insan kuralına çevrilmiş. Kanıt: `backend/src/server.ts:53`. Düzeltme: `.map(s=>s.trim()).filter(Boolean)` + 1 birim test. Efor S. |
| Y-02 | Ş4 | **Platform okuma uçlarında denetim izi yok — 4 uç.** Bekleyen kurum PII'sine, loglara, kurum listesine ve şüphe bildirimlerine kim baktı iz kalmıyor (KVKK Md.12). | 🟡 | Bu dört ucu açan platform admin denetim izinde görünüyor | BEKLIYOR | 🟡 KALIR: KVKK-iz. **madde 94 = G1-03 ❓.** ⚠️ **KAPSAM GENİŞLETİLDİ (2026-09-21):** rapor tek uç (`listPendingTenants`) diyordu, doğrulama **4 uç** buldu: `platformController.ts:175` (getPlatformLogs) · `:233` (listPendingTenants) · `:259` (listAllTenants) · `:399` (listSuspicionReports). Kardeş uçlarda desen HAZIR: `:505` `auditPlatformAction('VIEW_USER_REPORTS')`, `:535` `('VIEW_ANOMALIES')`. Tek uç yazılırsa iş yarım kapanır. |
| Y-03 | Ş4 | **madde 47 — Zod doğrulama bloğu 30 controller'da kopyalanmış.** Her uç kendi hata biçimini elle yazıyor; biçim kayarsa kullanıcı tutarsız hata görür, güvenlik yaması 30 dosyaya ayrı uygulanır. | 🟢 | Girdi hataları tüm uçlarda aynı biçimde ve aynı Türkçe mesajla | BEKLIYOR | madde 47. Kanıt: `backend/src/middleware/` **8 dosya**, `validate.ts` YOK (`find src -iname "*valid*"` → 0). ⚠️ `firstValidationMessage` (madde 69, CANLIDA) ile hizalı kalsın; **davranış DEĞİŞMEZ**, yalnız tek yere toplanır. |
| Y-04 | Ş4 | **madde 48 kalanı — üç liste ucu sayfalamasız.** `requestController` · `feedbackLogController` · `clubController` tüm satırları tek yanıtta döndürüyor. | 🟢 | Listeler sayfalı geliyor, kayıt artınca ekran donmuyor | BEKLIYOR | madde 48. F-27 yalnız konuşmayı çözdü. Kanıt (5 terim `take:`/`skip:`/`limit`/`offset`/`page`, harf duyarsız → 0/0/0): `requestController.ts:96` · `feedbackLogController.ts:125,167` · `clubController.ts:90,244,279`. ⭐ **Desen HAZIR, kopyalanabilir:** `conversationController.ts:21-30` `parseConversationPagination` + `:19-20` `PAGE_DEFAULT=30`/`MAX=100`. |
| Y-05 | Ş0 | **madde 100 — `SystemLog.meta` JSON-yol sorguları indekssiz.** Log büyüdükçe kalibrasyon sayfası ve dürtme kontrolü yavaşlar. | 🟡 | Log büyüdükçe kalibrasyon sayfası yavaşlamıyor | BEKLIYOR | 🟡 KALIR: **MIGRATION** (yeni `@@index`). madde 100. Kanıt: `prisma/schema.prisma:683-696` — `meta Json?` `:688`, indeksler `:691-695` (level/category/createdAt) — `meta` yok. ⭐ **Sorguyu YAPAN kod (raporda yoktu, bu turda bulundu):** `algorithmTuner.ts:209` `meta:{path:['tenantId']}` **ve** `nudgeService.ts:30` `meta:{path:['targetUserId']}` — indeks ikisini de hedeflemeli. |
| Y-06 | Ş4 | **madde 58 — Footer yalnız ana sayfada; yasal linkler ÖLÜ METİN.** "Gizlilik Politikası"/"Kullanım Koşulları" `<span>` olarak duruyor, tıklanmıyor — sayfalar VAR ama hiçbir yerden bağlanmıyor. | 🟢 | Kullanıcı her public sayfanın altından Gizlilik/KVKK/Kullanım Koşulları'na tıklayıp gidiyor | BEKLIYOR | ⛔ **ÇIKIŞ BLOKERİ (T1)** — KVKK/aydınlatma metinlerine erişim yolu yok. Kanıt: `frontend/src/app/page.tsx:54` `<footer>` · **`:68` `<span>Gizlilik Politikası</span>` · `:70` `<span>Kullanım Koşulları</span>`** (gerçekten `<span>`, `<Link>` değil). Hedefler VAR: `app/gizlilik`·`kvkk`·`terms`·`metodoloji`. Paylaşılan Footer bileşeni **yok** → önce bileşen çıkarılmalı. |
| Y-07 | Ş4 | **madde 57 — Hakkımızda ve İletişim sayfaları yok.** | 🟢 | Ziyaretçi /hakkimizda ve /iletisim'i açıp iletişim yolunu görüyor | BEKLIYOR | Kanıt (4 desen `*hakkimizda*`/`*iletisim*`/`*about*`/`*contact*`, `find`, harf duyarsız → **0**): `frontend/src/app/` dizin listesi. ⚠️ Y-06 ile **aynı dosyalara** dokunur → **SIRALI** (önce Y-06 Footer bileşeni). İletişim bilgisi **PO'dan** gelmeli. |
| Y-08 | Ş4 | **madde 53 — dashboard/admin/platform arama motoruna kapalı sayılmıyor.** `robots.txt` tarama önerisidir, indekslemeyi yasaklamaz. | 🟢 | Özel alan sayfaları arama sonuçlarında görünmüyor | BEKLIYOR | Kanıt: `app/robots.ts:13` disallow VAR; `noindex`/`index:false` → **0**. ⚠️ **RAPOR DÜZELTMESİ:** rapor "`robots:` 0 sonuç" dedi — **yanlış**; `app/page.tsx:35-38` `{index:true,follow:true}` VAR. ⛔ **UYGULAMA KISITI (bu turda bulundu):** `(admin)/layout.tsx` ve `platform/layout.tsx` **`'use client'`** → `export const metadata` EDEMEZLER; düzeltme alt-layout veya doğrudan `<meta>` gerektirir. |
| Y-09 | Ş4 | **madde 51+52 — site simgesi Next.js varsayılanı; paylaşım GÖRSELİ yok ve OG yalnız ana sayfada.** | 🟢 | Sekmede ve link paylaşımında kurum logosu görünüyor | BEKLIYOR | ⚠️ **RAPOR DÜZELTMESİ:** rapor "root `openGraph` YOK" dedi — yarısı doğru. `layout.tsx:33-38`'de yok **ama `app/page.tsx:23-29` `openGraph` + `:30-33` `twitter:{card:'summary_large_image'}` VAR** (yalnız görselsiz). ⇒ İş "OG etiketi ekle" **değil**: **görsel üret** (`icon.png` + `opengraph-image`) **+ root layout'a taşı**. Kanıt: `find` 5 desen → tek sonuç `src/app/favicon.ico`; `public/` **boş**. |
| Y-10 | Ş4 | **madde 62 — JSON-LD yapısal veri yok.** | 🟢 | Arama sonucunda kurum zengin sonuç olarak görünüyor | BEKLIYOR | Kanıt: `frontend/src/` tamamı, 3 terim (`ld+json`·`schema.org`·`JsonLd`), harf duyarsız → **0 sonuç**. |
| Y-11 | Ş4 | **madde 60+61 — "yukarı çık" ve yüzen WhatsApp düğmesi yok.** | 🟢 | Kullanıcı sağ-altta iki düğmeyi görüp kullanıyor | BEKLIYOR | Kanıt: `frontend/src/`, 6 terim (`scrolltotop`·`scrollTo(0`·`scrollTo({ top: 0`·`yukarı çık`·`back-to-top`·`backToTop`), harf duyarsız → **0**. ⚠️ **RAPOR DÜZELTMESİ:** WhatsApp yalnız `ShareButtons.tsx:21` denmişti; **ikinci kullanım `(admin)/admin/invite/page.tsx:39,77,209`** (davet metni şablonu) — ikisi de yüzen iletişim düğmesi DEĞİL. Klavye erişimi + `aria-label` zorunlu. Numara PO teyidi ister. |
| Y-12 | Ş4 | **madde 56+67 — ölçüm kodu (GTM/GA4/Clarity) ve çerez izni.** ⭐ **PR #110'u açan anahtar.** | 🟡 | Kullanıcı ilk girişte çerez tercihini seçiyor; reddederse izleme yüklenmiyor; kurum sahibi ziyaretçi sayısını görüyor | BEKLIYOR | 🟡 KALIR: **KVKK/rıza.** ⚠️ **BİRLEŞTİRİLDİ (2026-09-21):** madde 67 **tek başına açılmamalı** — bugünkü main'de üçüncü-taraf çerez **SIFIR** (7 terim, harf duyarsız → ölçüm/izin kodu 0) ve `app/gizlilik/page.tsx:62-63` bunu açıkça beyan ediyor ⇒ bugün çerez bandı **yasal olarak gereksiz**. ⛔ **SIRA BAĞIMLILIĞI: 67 → 56.** 56 önce merge edilirse KVKK ihlali doğar. ⭐ Kod **YAZILMIŞ**: `origin/feat/analytics-seo-2026-08-22` (`dcf5d9a`) içinde `components/analytics/Analytics.tsx` var, main'de yok; **PR #110 AÇIK** (GitHub teyidi 2026-09-21, başlık: *"🛑 MERGE ETME — çerez izni yok, KVKK riski"*). Bu satır = **#110'u merge edilebilir hale getirmek**, sıfırdan yazmak değil. ⚠️ = güvenlik konseyi §2.B.6, ek bulgu: PR #110'un gerekçesi HÂLÂ GEÇERLİ ama risk GERÇEKLEŞMİŞ DEĞİL. *Kapsam:* `frontend/src` özyinelemeli harf duyarsız, `GTM-|gtag\(|clarity|next/script` → 0; genişletilmiş desen (`googletagmanager|hotjar|posthog|mixpanel|plausible|matomo|segment\.|fbq\(`) → yalnız kendi backend'ine giden iç çağrılar; `components/analytics/` main'de yok. Bugün tek bir izinsiz aktarım YOK; `app/gizlilik/page.tsx:60-65` çerez beyanı kodla TUTARLI. Merge edildiği an autodeploy ile risk gerçekleşir → sıra bağımlılığı (67→56) doğrulandı. |
| Y-13 | Ş4 | **madde 54 — sitemap elle yazılmış.** Yeni public sayfa eklenince sitemap'te çıkmaz. | 🟢 | Yeni public sayfa eklenince sitemap'te kendiliğinden görünüyor | BEKLIYOR | Kanıt: `app/sitemap.ts:8-18` `PUBLIC_PATHS` **statik dizi** (9 yol). ⚠️ Y-06/Y-07'den **SONRA** (yeni sayfalar eklenince anlamlı). Düşük öncelik — 9 sayfalık statik sitede bugün zararsız. |
| Y-14 | Ş3 | **madde 36 — onaylı üyeyi kurumdan çıkarma ekranda YOK.** Backend zaten yapabiliyor; eksik olan düğme ve doğru e-posta metni. | 🟡 | Yönetici onaylı bir üyeyi kurumdan çıkarabiliyor ve kişi doğru metinli bilgilendirme alıyor | BEKLIYOR | 🟡 KALIR: yetki/rol akışı. ⚠️ **RAPOR DÜZELTMESİ — iş sanılandan KÜÇÜK.** Backend VAR: `adminController.ts:740-781` `rejectUser`, tek engel `:755` (REJECTED→409); **APPROVED engellenmiyor**. FE sarmalayıcı da VAR: `lib/api/admin.ts:83`. Eksik: **düğmenin onaylı-üye ekranlarına mount'u** (`mentor-havuzu`/`menti-havuzu`/`managers` bugün yalnız `rejectedAt` OKUYOR) **+ ayrı e-posta metni** — `emailService.ts:174` *"dilerseniz tekrar başvurabilirsiniz"* onaylı üye çıkarılırken **yanlış** (`adminController.ts:771-777` bunu da gönderiyor). |
| Y-15 | Ş3 | **madde 75 — mentör görünürlük tercihi ekranda YOK.** Backend tam ve IDOR korumalı; kullanıcı arayüzü hiç yok. | 🟢 | Mentör kendini menti havuzuna açıp kapatabiliyor | BEKLIYOR | Kanıt — backend TAM: `userRoutes.ts:91-96` `POST /mentors/:mentorId/visibility-optin` + `requireRole('ADMIN','MENTOR')` + **`requireSelfOrAdmin('mentorId')`** (V-03 IDOR guard'ı, gerekçe `:89-90`); controller `matchingController.ts:124-176`; admin onay ucu `adminRoutes.ts:69`. FE: `frontend/src/` tamamı, 2 terim, CSS gürültüsü elenmiş → **0 sonuç**. ⚠️ `mentorVisibilityEnabled` alanıyla ilişkisi **PO kararı** (KARAR-32). |
| Y-16 | Ş4 | **madde 127 — etiket önerme ekranda YOK.** Backend ucu çalışıyor, çağıran yok. | 🟢 | Kullanıcı yeni etiket önerebiliyor, yönetici onaylıyor | BEKLIYOR | Kanıt: route **`server.ts:140-144` `POST /api/tags/suggest` → `suggestTag`** (⚠️ `tagRoutes.ts` YOK, doğrudan `server.ts`'e monte — terim taramasıyla kaçar); controller `tagController.ts:56`. FE: 3 terim → **0 sonuç**; admin yalnız `tags/pending|approve|merge|reject` kullanıyor (`lib/api/admin.ts:126`). Backend V-13 ile düzeltilmişti. |
| Y-17 | Ş0 | **madde 166 — iki farklı `rankMentorsForMenti` fonksiyonu.** Aynı ad, iki dosya, farklı imza (biri senkron biri async); yanlışını import etmek kolay. | 🟢 | Tek isim tek davranış; yanlış import imkânsız | BEKLIYOR | Kanıt (tam **2 tanım**): `scoring.service.ts:165` (senkron) ↔ `matching.ts:351` (async). Ayrı ayrı çağrılıyorlar: `sjtScoringController.ts:5,133` ve `sector-scorer.service.ts:4,110` → scoring.service · `matchingController.ts:5,107` → matching. ⚠️ I-13/I-14 ile aynı dosya ailesi → **SIRALI**. ⚠️ = psikometri konseyi §5 D.3 dipnotu, ek bulgu: güncel teyit — matching.ts:351 (canlı, async) ↔ scoring.service.ts:165 (ölü, senkron); karışıklık riski gerçek, bu raporun kendisi de ayrımı her seferinde açıkça yazmak zorunda kaldı. |
| Y-18 | Ş0 | **madde 126 — `answeredFollowup` olmayan tabloyu sorguluyor; profil tamamlanma yüzdesi sistematik DÜŞÜK.** | 🟢 | Profil tamamlanma yüzdesi gerçek veriye dayanıyor | BEKLIYOR | ⚠️ **RAPOR DÜZELTMESİ — etki sanılandan BÜYÜK.** Rapor *"try/catch sessizce 0 dönüyor"* dedi; gerçek daha kötü: `profile-completeness.service.ts:43-50` **`(prisma as any).answeredFollowup?.count(...)`** — optional chaining **fırlatmadan `undefined` döner** → `|| 0` → **HER ZAMAN 0** ve **`catch` bloğu ÖLÜ KOD**, yani `profileSource==='HYBRID' ? 1 : 0` yedeği **hiç çalışmıyor**. Tablo gerçekten yok (`grep answeredFollowup schema.prisma` → 0). |

> **Örtüşme/alınmayan (devir analizi):** §3.4'teki `madde 35` · `madde 7` · `madde 49` · `madde 70` · `madde 137` · `madde 128` · `madde 108` · `madde 171+170` · `madde 173` · `S38` · belge düzeltme paketi — bu turda **satır AÇILMADI**; kaynak raporda tek satır özet hâlinde duruyorlar ve çoğu 🔴 KARAR'a bağlı. Bir sonraki devir turunda ele alınacak.

---
## AŞAMA GV — GÜVENLİK VE KVKK KONSEYİ (2026-09-21)

> Kaynak: `docs/raporlar/kesif/konsey-guvenlik-kvkk-2026-09-21.md`. Önek **GV-** seçildi çünkü raporun `G-??`
> öneki bilanço kart kodlarıyla (`G1-22`, `G3-08`…) karışıyor.
> ⚠️ **GV-01 ve GV-02 bu tabloda DEĞİL** — kuyruğun en üstündeki ⛔⛔ bloğunda (bkz. yukarı).
> ⚠️ **GV-24 ve GV-25 RAPOR BOŞLUĞUDUR:** raporun §2.A.2'si 7 IDOR bulgusu sayıyor ama §3 tablolarına
> yalnız 5'i alınmış. KURAL 9 gereği (*"listede satır almayan bulgu, bulgu sayılmaz"*) satır açıldı ve `⚠️`
> ile işaretlendi — **PO onaylamazsa düşer, kayıt kalır.**
> ⚠️ **Kapı sapması:** GV-09 raporda 🟡 önerilmişti; aynı rapor onun için karar kartı açtığı için kuyruk kapı
> kuralı gereği **🔴 KARAR-38** yapıldı. Diğer tüm kapılar raporun önerisiyle birebir.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| GV-03 | Ş0 | **⛔ Görüşme bağlantısı doğrulanmıyor — karşı tarafın tarayıcısında kod çalıştırılabiliyor.** Kurban ekranda yalnız "Görüşmeye katıl" yazısını görüyor, adresi hiç göremiyor. | 🟡 | Kullanıcı artık `http(s)` dışı bir adres kaydedemiyor; karşı taraf tıklanabilir sahte bağlantı görmüyor | BEKLIYOR | 🟡 güvenlik. **D-6 · güvenlik konseyi §D.2, orkestratör uçtan uca teyitli.** Şiddet: 🔴 YÜKSEK (hesap devralma). Kanıt: `meetingController.ts:404` `z.string().optional()` (**`.url()` yok**) → `:521` DB → `meetings/page.tsx:69` `<a href={...}>`; link metni sabit `:74`. React `javascript:` şemasını **engellemez**. ⭐ Tek giriş + tek çıkış → iki noktada kapanır. ⚠️ **Render noktasını U-02 (BITTI) ekledi** — tamamlanmış iş açığı görünür kıldı, kaynak açık baştan vardı. ⚠️ F-04 (global CSP `server.ts:44` kapalı) kapanırsa azaltıcı olur. ⚠️ `meetingController.ts` = Ş2 dosyası → K-05/U-01/V-15 ile **SIRALI** |
| GV-04 | Ş0 | **⛔ Görüşme check-in notları kurum içinde herkese açık.** İlişkinin en mahrem serbest-metin verisi (1000 karakterlik açık not, endişe etiketi, hazırlık puanı) id tahminiyle okunabiliyor. | 🟡 | Kullanıcı artık başkasının check-in notlarını göremiyor; notları yalnız görüşmenin tarafları ve kurum yöneticisi görüyor | BEKLIYOR | 🟡 auth+KVKK. **G-3 · güvenlik konseyi §2.A.2/§3.1.** Şiddet: 🟠 YÜKSEK. Kanıt: `meetingRoutes.ts:119` yalnız `requireAuth()`; `meetingCheckInController.ts:100-108` `req.auth` **hiç okunmuyor**. Karşı örnek **aynı dosyada**: `submitCheckIn` `:56-61`. Sızan alanlar: `CheckInSchema:11-26` (`openNote`, `nextTopicNote`, `concernTag`, `menteePreparedness`) |
| GV-05 | Ş0 | **⛔ Mentör, başka bir mentör adına geri bildirim yazabiliyor ve kurumun eşleştirme öğrenmesini zehirleyebiliyor.** Kayıt diğer mentörün adına görünüyor; gerçek mentör o çift için bir daha yazamıyor. | 🟡 | Mentör artık başkası adına geri bildirim yazamıyor; kayıtta görünen isim gerçekten yazan kişi | BEKLIYOR | 🟡 auth+matching. **G-5 · güvenlik konseyi §2.A.2/§3.1.** Şiddet: 🟠 YÜKSEK. Kanıt: `feedbackLogRoutes.ts:17`; `feedbackLogController.ts:45-99` `req.auth` **hiç geçmiyor**, `mentorId`/`mentiId` gövdeden. ⭐ **Asimetri:** aynı dosyada **okuma** korunuyor (`:122-123`, `:159`), **yazma** korunmuyor. Etki: `applyFeedbackSignal:84-90` → `MatchCombinationScore` zehirlenmesi (geri alınması zor). ⚠️ Y-04 aynı dosyaya sayfalama için dokunuyor → SIRALI |
| GV-06 | Ş0 | **⛔ Menti, başka bir mentinin adına görüşme talebi yaratabiliyor.** Kurbanın haftalık görüşme hakkı doldurulur ve mentöre kurbanın adına e-posta gider. | 🟡 | Menti artık başkası adına randevu açamıyor; talepteki kimlik yalnız oturumdan geliyor | BEKLIYOR | 🟡 auth. **G-4 · güvenlik konseyi §2.A.2/§3.1.** Şiddet: 🟠 YÜKSEK. Kanıt: `createMeeting` `meetingController.ts:155-211`, `mentorId`/`mentiId` gövdeden `:133-138,161`. ⭐ İkizi `bookMeeting` **doğru yapıyor** (`:515` token'dan) → `createMeeting` eski/güvensiz ikiz. ⚠️ **KARAR-40** (düzelt mi / karantina mı) — ama **acil yama karardan BAĞIMSIZ** yapılabilir (kart önerisi: "A şimdi, B sonra"). ⚠️ **K-13 mükerrer-uç listesinde bu uç YOK** → K-13 Not'una eklendi. ⚠️ `meetingController.ts` = Ş2 → K-05/U-01/V-15/GV-03 ile SIRALI |
| GV-07 | Ş0 | **⛔ Başarısız platform girişinde yazılan e-posta adresi kalıcı sistem günlüğüne düşüyor.** Yanlış kutuya e-posta yazan herkesin adresi kaydediliyor; hesabını kapatmış kişinin adresi bile sistemde kalabiliyor. | 🟡 | Sistem günlüğünde artık ham e-posta yok; kayıt maskeli tutuluyor ya da hiç yazılmıyor | BEKLIYOR | 🟡 KVKK. **B.7 · güvenlik konseyi §2.B.7, orkestratör teyitli.** `backend/CLAUDE.md` kuralı #5'in **doğrudan ihlali**. Kanıt: `platformController.ts:42-45` `{ email, ip }`. ⚠️ **Yapısal sebep:** `logger.ts:23-28` `meta`'yı **hiç süzmüyor** (allow-list yok) → düzeltme iki katmanlı: çağrı yeri + logger. Zincir: `anonymizeUser` `SystemLog`'a dokunmuyor (GV-08) → tek çıkış 90 günlük cron → `CRON_ENABLED='false'` ise o da yok (PO B#9). ⚠️ **V-02 (BITTI)** yalnız `errorHandler` meta'sını düzeltti; bu **ayrı çağrı yeri**. Maskeleme yardımcısı `mask.ts` zaten var |
| GV-08 | Ş0 | **Anonimleştirme 6 tabloyu ve 4 alanı atlıyor — psikometrik arketip ve serbest yorumlar geride kalıyor.** Kişiye "geri döndürülemez şekilde anonimleştirildi" deniyor, arketip başka bir tabloda düz metin duruyor. | 🟡 | Hesabını kapatan kişi artık hiçbir tabloda psikometrik verisiyle bulunamıyor | BEKLIYOR | 🟡 KVKK. **B.2 · güvenlik konseyi §2.B.2, orkestratör teyitli.** Şiddet: 🔴 (taahhüt ihlali). Kanıt: `gdprService.ts:112-119` `UserProfile.archetype`'ı **özenle null'luyor**; `grep "tx.match\.\|matchFeedback\|pendingTag\|availabilityBlock\|clubMembership\|mentorFilter" gdprService.ts` → **0**. `Match.mentorArchetype/mentiArchetype` `schema.prisma:1035-1036` **NOT NULL String**; `MatchFeedback.fromUserId` `:1170` **FK yok** → cascade yakalayamaz. +4 alan: `Meeting.locationUrl`, `UserReport.reviewNote`, `User.password`, `User.rejectionReason`. +`SystemLog` (GV-07 zinciri). Taahhüt metni `:50`. ⚠️ **Yorum ayağı KARAR-39'a bağlı; arketip + 4 alan ayağı karardan BAĞIMSIZ** yapılabilir. `Match.*Archetype` NOT NULL → migration olası ⛔ **BİRLİKTE YAPILMALI — önce silme yolu düzeltilir, SONRA Match yazımı açılır. Ters sıra = KVKK ihlali.** *(karşı taraf: `00-KUYRUK` **U-18 / PS-04 / F-11 / I-15** — `Match` yazımı bu satır bitmeden AÇILMAZ.)* |
| GV-09 | Ş4 | **KVKK aydınlatma sayfası yanlış ülke ve yanlış hukuki rejim beyan ediyor.** Sayfa "İrlanda (AB), GDPR'a tabi" diyor; sunucu Londra/Birleşik Krallık'ta ve AB üyesi değil. OAuth ve e-posta sağlayıcısına aktarım hiç beyan edilmiyor. | 🔴 KARAR-38 | Kullanıcı aydınlatma metninde gerçek sunucu ülkesini, doğru hukuki rejimi ve tüm alıcıları görüyor | BEKLIYOR | 🔴 KARAR-38 + 🟡 KVKK/hukuk. **B.6 · güvenlik konseyi §2.B.6.** Kanıt: `app/kvkk/page.tsx:92-107` ↔ `menti-mentor-v2/CLAUDE.md:255` (madde 92, PO teyitli 2026-08-26); `:94-95` "yönetilen PostgreSQL" ↔ `docker-compose.yml:16-24` kendi konteyneri; `:60-64` OAuth+SMTP saymıyor; `:31-39` 8 veri kategorisi eksik (IP dâhil, `platformAudit.ts:32`). ⚠️ **PO ön koşulu: P-a (avukat sorusu) + P-b (sunucu ülkesi).** ⚠️ `app/gizlilik/page.tsx:76-81` ve `:13` de bayat (self-servis silme var, tarih eski). ⚠️ Y-06 ile aynı sayfalar → SIRALI |
| GV-10 | Ş0 | **Çıkış yapan, rolü düşürülen ve reddedilen kullanıcının erişimi bir saate kadar sürüyor.** Reddedilen kullanıcı, reddedildikten sonra da kurumun tüm uçlarını kullanabiliyor. | 🟡 | Kullanıcı çıkış/rol düşürme/red sonrası artık hiçbir ekranı açamıyor — erişim anında kesiliyor | BEKLIYOR | 🟡 auth. **C.3 + G-8 · güvenlik konseyi §2.C.3/§2.A.4.** Şiddet: 🔴/🟠. Kanıt (orkestratör teyitli): `authController.ts:504-513` yalnız refresh siliniyor (access blacklist yok) · rol `tenant.ts:102` **JWT'den**, `:84` membership'ten yalnız `isActive` seçiliyor · `rejectUser` `adminController.ts:760-768` `User.isActive=false` yapıyor ama **üyeliğe dokunmuyor**, `requireTenant` `User.isActive`'i **hiç okumuyor** (`tenant.ts:83-100`) · `getCachedTenant` tenant `isActive`'ini seçmiyor (`tenantCache.ts:43-50`, 5 dk TTL). ⭐ Doğru desen kodda **var**: `meetingController.ts:334-340`, `matching.ts:154-156` `TenantMembership.role` okuyor. ⚠️ **U-13 ile bitişik** (o rol *yazma* ucunu, bu rol *okuma* kaynağını konu ediyor) |
| GV-11 | Ş4 | **Üyeliği kapatılmış yönetici kurum ayarlarını hâlâ değiştirebiliyor.** Görüşme limiti, minimum eşleşme skoru ve üye engelleme token ömrü boyunca erişilebilir kalıyor. | 🟡 | Üyeliği kapatılan yönetici artık hiçbir kurum ayarını açamıyor | BEKLIYOR | 🟡 auth. **G-9 · güvenlik konseyi §2.A.4.** Kanıt: `extractAdminPayload` `selfServeController.ts:61-71` ve `adminSettingsController.ts:11-21` yalnız `verifyToken` + `role==='ADMIN'`; `TenantMembership.isActive` kapısı **yok** (kıyas `tenant.ts:87-97` **var**); `aud` claim'i de kontrol edilmiyor (kıyas `platformAuth.ts:27` **ediyor**). ⚠️ **F-23 ile aynı dosya** → F-23 merkezî izolasyon desenine geçerse `adminSettings` ayağı kendiliğinden kapanır, `selfServe` ayağı kalır → **SIRALI** |
| GV-12 | Ş4 | **Kurum kaydında "bu e-posta zaten kayıtlı" deniyor — üyelik bilgisi sızıyor.** Aynı kontrol normal kayıt akışında bilinçli olarak gizleniyor. | 🟡 | Kayıtlı ve kayıtsız e-posta artık iki kayıt yolunda da **aynı** yanıtı alıyor | BEKLIYOR | 🟡 auth. **C.5-B · güvenlik konseyi §2.C.5-B, orkestratör teyitli.** Şiddet: 🔴 açık oracle. Kanıt: `selfServeController.ts:262-267` `409 EMAIL_MEVCUT` ↔ `authController.ts:177-184` **bilinçli enumeration-safe** (kodda açıklayıcı yorum). Tek fren IP 5/dk (`rateLimiter.ts:239`) ≈ 7.200 adres/gün/IP. ⭐ k-anonimlik (P-00) ve IDOR ile **aynı sınıf**: koruma bir yolda var, ikinci yol açık. ⚠️ Zamanlama yan-kanalı (`login` bcrypt atlama `:292`, `forgot-password` `:531-550`) **ayrı ve daha küçük iş**; sabit-zaman deseni `platformController.ts:23-32`'de zaten var |
| GV-13 | Ş0 | **Yenileme anahtarı veritabanında açık metin tutuluyor.** Veritabanı kopyası sızarsa tüm aktif oturumlar doğrudan devralınabiliyor. | 🟡 | Veritabanı kopyası sızsa bile kimse mevcut oturumları devralamıyor | BEKLIYOR | 🟡 auth. **C.3 · güvenlik konseyi §2.C.3.** Şiddet: 🟠. Kanıt: `schema.prisma:823-834` `token String @unique`. ⭐ **Doğru desen kodda VAR:** reset token **SHA-256 hash'li** (`authController.ts:116-118`, `schema.prisma:889`) — refresh'e uygulanmamış. Ek: **yeniden-kullanım tespiti yok** (`:459-467`; desen `reuse\|family\|blacklist\|revoke` → 0) → çalınmış token sessizce kullanılır. ⚠️ V-06 ile aynı aile (JWT/oturum) |
| GV-14 | Ş0 | **Davet bağlantısı sunucu günlüğüne düşüyor.** Günlüğe erişen herkes geçerli davet linki toplayabiliyor. | 🟡 | Davet bağlantısı artık günlükte görünmüyor | BEKLIYOR | 🟡 auth. **C.3 · güvenlik konseyi §2.C.3.** Kanıt: `invitationRoutes.ts:13` token **URL path'inde**; `requestLogger.ts:22-23` `originalUrl`'i olduğu gibi basıyor. Aynı satır OAuth `?code=`/`?state=` ve kalıcı `?token=` (`unsubscribeToken`) de basıyor; ayrıca `authController.ts:686-690` access token'ı **query string**'de taşıyor. = **`U-12` akrabası, ek bulgu:** U-12 token **modelini** konu ediyor (kartsız kilitli); bu **sızıntı kanalı** ayrı ve tek dosyada kapanır → ayrı satır (gerekçe: U-12 kartı gelene kadar ucuz düzeltme donmasın) |
| GV-15 | Ş4 | **E-postaların gövdesi kaçış yapılmadan kuruluyor.** Adına HTML koyan bir kullanıcı, yöneticinin gelen kutusundaki mailin görünümünü değiştirebiliyor. | 🟡 | Kullanıcının yazdığı hiçbir metin artık e-postada biçim bozamıyor | BEKLIYOR | 🟡 güvenlik. **D-7 · güvenlik konseyi §D.2.** Şiddet: 🟠 sistematik. Kanıt: `emailService.ts` 337 satır, kaçış yardımcısı **0** (`escapeHtml\|sanitize\|encode` → 0); **13 şablonun tamamı** ham enterpolasyon. En açık: `:310` `${args.message}` · `:169-174` `rejectionReason` · `:152-153` `newUserFullName`. Girdi kısıtı yok (`authController.ts:28` HTML karakter kısıtı yok). ⭐ **Tek noktadan kapanır:** `send()` `:72` öncesi tek `escapeHtml()` → 13 şablon birden. ⚠️ U-16 (BITTI) aynı dosyaya dokundu → çakışma yok, ayrı satırlar |
| GV-16 | Ş3 | **Profil fotoğrafındaki konum bilgisi herkese açık servis ediliyor.** Telefonla çekilmiş fotoğraftaki GPS etiketi dosyada kalıyor, dosya adı kullanıcı kimliğiyle başlıyor. | 🟡 | Kullanıcının yüklediği fotoğraf artık konum bilgisi taşımıyor | BEKLIYOR | 🟡 KVKK (Md.4 veri minimizasyonu). **D-1 · güvenlik konseyi §D.1.** Şiddet: 🟠. Kanıt: `avatarStorage.ts:68` ham buffer birebir yazılıyor; `sharp`/`jimp`/`exif` bağımlılığı **yok**; `/uploads` `requireAuth`'suz servis ediliyor (`server.ts:72`); dosya adı `${userId}-…` (`:51`) → **konum ↔ kullanıcı doğrudan eşleşir**. ⚠️ Yeni bağımlılık → `npm audit` kuralı (CLAUDE.md). ⚠️ K-04 (kalıcı disk, PO A#1) ile aynı dosya ailesi |
| GV-17 | Ş3 | **Kişi kendi psikometrik profilini dışa aktaramıyor.** OCEAN/arketip/DISC türevlerinin tamamı ve kendi yazdığı mesajların içeriği dışa aktarımda yok. | 🟡 | Kullanıcı verilerini indirdiğinde artık kendi psikometrik profilini ve kendi yazdığı mesajları da görüyor | BEKLIYOR | 🟡 KVKK. **B.3 · güvenlik konseyi §2.B.3.** Kanıt: `exportUserData` `gdprService.ts:284-333` yalnız **6 kaynak**; `UserProfile` **hiç yok**; `:314` mesaj **yalnız `count`** (tip `:280-281` → FE `kvkk.ts:20-22`). **16 tablo + ≈22 `User` alanı** eksik. ⚠️ **K-12 (BITTI) FE işiydi** — özet backend'in verdiğinden fazlasını üretemez; eksiklik **backend kaynaklı**, K-12 bayat sayılmaz. ⚠️ V-10 (BITTI) bu ucun rate limitini kapattı |
| GV-18 | Ş3 | **Rıza sürümü kaydediliyor ama hiç kontrol edilmiyor.** Aydınlatma metni güncellenirse eski sürüme onay vermiş kullanıcılar hiçbir yeniden-onay ekranı görmüyor. | 🟡 | Metin sürümü değişince kullanıcı yeniden onay ekranı görüyor | BEKLIYOR | 🟡 KVKK. **B.5 · güvenlik konseyi §2.B.5.** Kanıt: `hasValidConsent` `consentService.ts:152-162` **doğru yazılmış**; *kapsam:* `grep -rn "hasValidConsent\|getActiveConsent\|getAllConsents" src tests` → `src/` içinde **0 çağıran**, tek tüketici `tests/consentService.test.ts`; `exportUserData` bile ham `prisma.consent.findMany` kullanıyor (`gdprService.ts:308-312`). ⚠️ `CONSENT_VERSION='v1.0'` **yer tutucu** (`:25-28` TODO G1-10) → **avukat metnine bağlı** (PO #16/P-a); F-02/F-03 ile aynı kilit |
| GV-19 | Ş4 | **Oturum içi şifre değiştirme yok ve şifre karmaşıklık kuralı yok.** Kullanıcı şifresini değiştirmek için e-posta akışına mecbur; "12345678" kabul ediliyor. | 🟡 | Kullanıcı mevcut şifresini girerek şifresini uygulama içinde değiştirebiliyor; zayıf şifre reddediliyor | BEKLIYOR | 🟡 auth. **C.5 · güvenlik konseyi §2.C.5.** *Kapsam beyanı:* backend+frontend, `changePassword\|change-password\|currentPassword` → **0**; `authRoutes.ts` tam okundu. Karmaşıklık: `password.*(regex\|refine)` → **0**, yalnız `min(8)` (`authController.ts:27,52`), bcrypt 12 tur ✅. ✅ Sıfırlama akışının **kendisi iyi yazılmış** (§C.5 — 256-bit, SHA-256, 60 dk, tek kullanımlık). ⚠️ **U-15 komşu** (o sıfırlamanın tek kanala bağlı olmasını, bu *değiştirme* ucunun yokluğunu konu ediyor) → aynı dosya, SIRALI |
| GV-20 | Ş0 | **Yüklenen görselin çözünürlük sınırı yok.** Tek bir yükleme ön yüz sunucusunun belleğini tüketebiliyor. | 🟢 | Aşırı çözünürlüklü görsel yüklenemiyor, kullanıcı anlaşılır hata görüyor | BEKLIYOR | **D-2 · güvenlik konseyi §D.1.** Kanıt: `detectImageType` `avatarStorage.ts:26-48` yalnız sihirli bayta bakıyor; `next.config.mjs:42-58` `/uploads/**`'ı `remotePatterns`'e ekliyor → **Next image optimizer decode ediyor**. Backend etkilenmez. **Etki büyüklüğü TEYİT GEREK** (ölçüm), kontrolün yokluğu kesin. ⚠️ GV-16 ile **aynı dosya, birlikte yapılmalı** (yeniden kodlama zaten piksel sınırı getirir) |
| GV-21 | Ş0 | **`/api/auth` altına eklenecek yeni adres sessizce OAuth'a düşüyor.** Bugün sorun yok ama bir sonraki geliştirici fark etmeden açık uç yaratabilir. | 🟢 | Yeni bir adres eklendiğinde yanlış yere düşmüyor; test bunu yakalıyor | BEKLIYOR | **§3.3 sertleştirme · güvenlik konseyi §2.A.1.** Şiddet: 🟡 DÜŞÜK (yapısal/gelecek riski). Kanıt: `authRoutes.ts:55` `GET /api/auth/:provider` catch-all; `/api/auth/me` (`:42`) ondan ÖNCE tanımlı olduğu için bugün güvenli; kod yorumu riski **belgelemiyor**. Teslimat: yorum + regresyon testi. = **`V-09` (BITTI) komşusu** — o belge listesini kapattı, bu yapısal riski kapatır |
| GV-22 | Ş0 | **Yükleme boyutu ayarı yanlış yazılırsa sınır sessizce kalkıyor.** Operatörün tek yazım hatası sınırsız yüklemeye dönüşüyor. | 🟢 | Ayar hatalı yazılırsa uygulama açılışta uyarıyor, sınır sessizce kalkmıyor | BEKLIYOR | **D-4 · §3.3 sertleştirme.** Kanıt: `config.ts:115` `Number()` → `NaN`; `limits.fileSize: NaN` → `size > NaN` daima false; `isFinite` kontrolü **yok**. ⚠️ Multer 2.x'in `NaN` davranışı **TEYİT GEREK** (`node_modules` kurulu değildi) — kontrolün yokluğu kesin. Düzeltme: `isFinite` + varsayılana düşme + açılışta uyarı |
| GV-23 | Ş3 | **Hesap kapatmada oturum çerezi diğer beş çağrıyla tutarsız temizleniyor.** Bugün çalışıyor, ancak çerez ayarları değişirse sessizce kırılır. | 🟢 | Hesabını kapatan kullanıcının oturumu her tarayıcıda kesin kapanıyor | BEKLIYOR | **§3.3 sertleştirme · güvenlik konseyi §2.C.4.** Kanıt: `gdprController.ts:129` `clearCookie` **opsiyonsuz** (httpOnly/secure/sameSite/path yok); diğer 5 çağrı (`authController.ts:65-70,74`, `selfServeController.ts:31-35`, `platformController.ts:60,66`) tutarlı. Derinlemesine savunma notu, bugün istismar yolu görülmedi |
| GV-24 | Ş0 | ⚠️ **Onaylanmamış kullanıcı, liste ekranının kapısını tek tek atlayabiliyor.** Herkes onaylanmamış/reddedilmiş üyelerin profilini tek tek okuyabiliyor. | 🟡 | Onaylanmamış kullanıcı artık başkasının profilini tek tek de okuyamıyor | BEKLIYOR | 🟡 auth. **G-6 · güvenlik konseyi §2.A.2.** Şiddet: 🟡 ORTA. Kanıt: `listUsers` iki kapı kuruyor (`userController.ts:46-57` 403 + `:74` `approvalStatus:'APPROVED'`, gerekçe `:69-73`); `getUser` `:187-215` **ikisini de yapmıyor**. Ham DISC/e-posta sızmıyor (`fullAccess` kapısı `:189,193` doğru). ⚠️ **RAPOR BOŞLUĞU: bu bulgu raporun §3 tablolarına alınmamış** (yalnız §2.A.2'de) → KURAL 9 gereği satır önerildi, **PO onayıyla eklenir**. ⚠️ U-08 ile aynı sınıf |
| GV-25 | Ş0 | ⚠️ **Kullanıcı başkasının kişiselleştirilmiş mentör sıralamasını isteyebiliyor.** Kimin kiminle ne kadar uyumlu göründüğü dışarı çıkabiliyor. | 🟡 | Kullanıcı artık yalnız kendi mentör sıralamasını isteyebiliyor | BEKLIYOR | 🟡 auth+matching. **G-7 · güvenlik konseyi §2.A.2.** Şiddet: 🟡 ORTA. Kanıt: `sjtScoringController.ts:90-142` `mentiId` gövdeden, sahiplik yok. Karşı örnek **aynı dosyada**: `computeProfileHandler:61-65` sahipliği zorluyor. *Sömürü zorluğu:* parametre `User.id` değil **`UserProfile.id`**, istemciye rutin dönmüyor → pratikte tahmin zor. ⚠️ **RAPOR BOŞLUĞU: §3'e alınmamış** → PO onayıyla eklenir. ⚠️ GV-01 ile **aynı dosya** → birlikte yapılmalı |

---

## AŞAMA PS — PSİKOMETRİ KONSEYİ (2026-09-21)

> Kaynak: `docs/raporlar/kesif/konsey-psikometri-2026-09-21.md`. Önek **PS-** (raporun `S-??` öneki yerine).
> ⚠️ **Kapı sapmaları gerekçeli:** PS-05 · PS-07 · PS-08 · PS-11 raporda 🟡 idi → **🟢** (üç istisnanın hiçbiri
> yok; PS-07/PS-08 yalnız `tests/` altına dokunuyor — motor dosyasına tek satır girerse kapı 🟡'ye döner).
> PS-10 raporda 🟢 idi → **🟡** (k-anonim maskeleme davranışına temas ediyor).
> ⚠️ **PS-04, U-18'in sonuç ayağıdır** — PO isterse açılmaz ve U-18 Not'una katlanır; ama o zaman
> `/admin/eslesmeler` tablosu ve çift risk sinyali **izsiz kalır** (bugün hiçbir satırda yoklar).

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| PS-01 | Ş0 | **Aday sorgularında `orderBy` yok → sıralama tekrarlanabilir değil, 500'den sonrası sessizce kesiliyor.** Deterministik ikincil anahtar (skor → id) + sorguya `orderBy`. | 🟡 | Menti sayfayı yenileyince mentör sırası değişmiyor; 500'den kalabalık kurumda hiçbir aday sessizce kaybolmadan listeye giriyor | BEKLIYOR | 🟡 KALIR: **matching/skorlama dosyası** (istisna 2). Kanıt (rapor, BE submodule boş → bu turda yeniden doğrulanamadı): comparator tie-breaker'sız `matching.ts:325`, `:429`; sorgular `orderBy`'sız `:149-172`, `:382-398`, ikisi de `take: 500`. `Array.sort` ES2019'dan stabil ⇒ giriş sırası korunur ama **PostgreSQL'de `ORDER BY`'sız giriş sırası garanti değil**. ⚠️ Kaymanın **sıklığı** TEYİT GEREK (ölçüm gerekir); kodda **yokluk** kesin. = psikometri konseyi §2 A.3 |
| PS-02 | Ş0 | **⛔ CANLI SESSİZ HATA — onboarding vektörü `confidence`'sız yazılıyor.** Vektör eşleştirmede tamamen yok sayılıyor, ama API "%100 güven" raporluyor. | 🟡 | Onboarding'i bitiren kullanıcının cevapları eşleştirme skoruna gerçekten katılıyor; ekranda/yanıtta gösterilen güven değeri doğru | BEKLIYOR | 🟡 KALIR: matching/scoring. **⛔ ÇIKIŞ BLOKERİ (T3) ADAYI — PO/ajan onaylamalı** (kuyruk kuralı: *"bloker olduğu KANITLANIR, şüpheyle işaretlenmez"*; T3 = *"sessiz yanlış: kimse fark etmez, sistem çalışıyor görünür"* tanımına birebir uyuyor — ama rapor bu etiketi vermedi, bu yüzden **aday**). Kanıt zinciri (rapor): `onboardingController.ts:201` tip `{D,I,S,C}` (**`confidence` yok**) → `:474-478` doğrudan `User.discVector`'a yazım → `scoring.ts:72` `undefined > 0` = false ⇒ **vektör yok sayılır** → `scoring.ts:110` `?? (mentiDisc ? 1 : 0.5)` ⇒ **`confidence: 1` raporlanır** → `discVectorService.ts:37-50` `parseDiscVector` bu JSON'u **`null`** sayar. ⚠️ `matching.ts:286` `as DiscVector` cast'i runtime doğrulaması yapmıyor — hata buradan sızıyor. ⚠️ Bilinçli tasarım mı regresyon mu → **TEYİT GEREK (ürün niyeti)**; kodda gerekçe yorumu yok. = psikometri konseyi §4 C.2 |
| PS-03 | Ş0 | **Likert yolu (`/disc-test`) `discType`'ı hiç yazmıyor** → testini güncelleyen kullanıcı onboarding'den kalma ESKİ `discType` ile eşleştirilmeye devam ediyor. | 🟡 | Kullanıcı DISC testini güncelleyince eşleştirmesi gerçekten değişiyor | BEKLIYOR | 🟡 KALIR: matching/scoring. Kapsam beyanı (rapor): `discVectorService.ts` tamamı, `discType` harf duyarsız → **0 eşleşme**. Matris skoru (`scoring.ts:74,83`) ve anti-match (`:24-29`) `discType`'a dayanıyor. ⚠️ **PS-02 ile AYNI DOSYA AİLESİ → SIRALI.** ⚠️ **KARAR-?? (DISC tekrar testi) ile ilişkili**: tekrar test açılmazsa bu satırın kullanıcı faydası yalnız *ilk* Likert turunda görünür. = psikometri konseyi §4 C.2 |
| PS-04 | Ş0 | **`Match` tablosu hiç yazılmadığı için üç yüzey birden ölü:** mentörün `%uyum` rozeti · yöneticinin `/admin/eslesmeler` tablosu · çift risk sinyali (GREEN/YELLOW/RED). | 🟡 | Yönetici eşleşme listesini ve çift risk sinyalini gerçekten görüyor; mentör onay kuyruğunda %uyum rozeti çıkıyor | BEKLIYOR | 🟡 KALIR: matching. ⚠️ **KÖK = U-18** (*"`Match` tablosuna yazan kod yok"*); bu satır **sonuç ayağı**. **PO isterse AÇILMAZ** → U-18 + P-04 Not'larına işlenir (aşağıdaki (B) metinleri hazır). **Ama:** üç yüzeyden **ikisi** (`/admin/eslesmeler` tablosu · risk sinyali) bugün **hiçbir kuyruk satırında yok** — satır kapatılırsa bu iki ayak izsiz kalır. Kanıt (FE bu turda doğrulandı): rozet `mentor/page.tsx:292` `%{score} uyum` **VAR** ama `m.match` null · tablo `eslesmeler/page.tsx:134,142` arketip sütunları **VAR** + boş-durum `:98-101` *"Henüz eşleşme yok"* · risk sinyali tek üretici `adminController.ts:433` ↔ tek tüketici `eslesmeler/page.tsx:170` (rapor). ⚠️ **Silinecek ölü kod DEĞİL** — hesap doğru ve **testli** (`degerlendirme-metrik-asama1.test.ts:301-336`), `MeetingCheckIn` verisi FE'den gerçekten yazılıyor (`meeting-checkin/page.tsx:49-73`); kopan yer **gösterim**. **⛔ BİRLİKTE YAPILMALI — önce silme yolu düzeltilir, SONRA Match yazımı açılır. Ters sıra = KVKK ihlali.** = psikometri konseyi §5 D.1 |
| PS-05 | Ş4 | **Yöneticinin "Başarı oranı" KPI kartı daima boş ve bunu söylemiyor** — `—` gösteriyor, "veri yok" demiyor. | 🟢 | Yönetici, başarı oranının **neden** boş olduğunu ekranda okuyor (sessiz `—` yerine) | BEKLIYOR | 🟢 **KAPI SAPMASI (rapor 🟡 demişti):** üç istisnanın **hiçbiri** yok — migration/seed yok · dokunulan dosya auth/KVKK/matching değil (yalnız admin KPI **gösterimi**) · geri dönülmez dokunuş yok. ⚠️ Backend'e alan eklenirse 🟡'ye döner. Kanıt (rapor): `adminController.ts:91-96` `successRate` kaynağı `feedbackLog.groupBy` (`:77-82`); `FeedbackLog`'a FE **hiç yazmıyor** (kapsam: FE `src/`, `feedback-logs\|feedbackLog` → POST **0**) ⇒ daima `null`; FE `?? '—'` ile gizliyor (`admin/kpi/page.tsx:70`). ⚠️ Hangi NPS'in canonical olduğu **ürün kararı** → yeni KARAR-?? (öğrenme döngüsü) / **KARAR-12**; bu satır yalnız **dürüst boş-durum**, veri kararını BEKLEMEZ. ⚠️ V-05 ile aynı ekran (`admin/kpi`) → **SIRALI**. = psikometri konseyi §5 D.2 |
| PS-06 | Ş0 | **`dimensionalTotal` cache'inde tenant filtresi yok** → başka kurumun soruları paydaya giriyor, `confidence` olması gerekenden düşük çıkıyor. | 🟡 | Kullanıcının profil güven değeri **kendi kurumunun** soru havuzuna göre hesaplanıyor | BEKLIYOR | 🟡 KALIR: scoring/profil (skoru besliyor) + kurum izolasyonu. Kanıt (rapor): `discVectorService.ts:73-75` `where: { isActive: true, discDimension: { not: 'GENERAL' } }` — **`tenantId` yok**; 5 dk TTL cache (`:62-79`). Etki: `confidence` düşük çıkınca `scoring.ts:75-78` harmanı matrise ağırlık verir ⇒ kullanıcının gerçek vektörü hafifler. ⚠️ **PS-02 ile aynı dosya ailesi → SIRALI.** = psikometri konseyi §4 C.2 |
| PS-07 | Ş0 | **Eşleştirmenin ANLAMINI test eden test yok** — iki aday arasında beklenen sıralamayı assert eden tek test bile yok. | 🟢 | (iç kalite — kullanıcı etkisi: formül bozulursa CI yakalar, yanlış sıralama canlıya çıkmaz) | BEKLIYOR | 🟢 **KAPI SAPMASI (rapor 🟡):** teslimat **yalnız `tests/` altında yeni dosya**; motor/skorlama dosyasına **tek satır dokunulmaz** ⇒ merge kontrol listesindeki *"auth/KVKK/matching dosyalarına dokunulmadı"* maddesi korunur. ⛔ **Koşul:** `matching.ts`/`scoring.ts`'e tek satır bile girerse kapı **🟡'ye döner**. Kanıt (rapor): 14 dosya · **141 test**; kova B yalnız 2 yer (`scoring.unit.test.ts:35-41,104-118` saf fonksiyon · `feedback-loop.test.ts:95-128` uçtan uca). `grep "items\[0\]" tests/matching.test.ts tests/mentor-matches.test.ts` → tek isabet `matching.test.ts:244` (uyarı rozeti). `matching.test.ts:45-47` yalnız *"azalan sırada mı"* diyor. **Eşitlik davranışı (PS-01) hiç test edilmiyor** → PS-01 ile birlikte yapılırsa iki iş tek testte kapanır. = psikometri konseyi §5 D.4 |
| PS-08 | Ş0 | **İki "anlamlılık" testi de KOŞULLU — boş listede sessizce yeşil geçiyor** (vacuous). | 🟢 | (iç kalite — yeşil CI gerçekten bir şey kanıtlıyor) | BEKLIYOR | 🟢 **KAPI SAPMASI (rapor 🟡):** yalnız `tests/` altındaki iki dosya düzeltilir. Kanıt (rapor): `feedback-loop.test.ts:123` assert `if (goodItems.length > 0 && badItems.length > 0)` **içinde** ⇒ liste boşsa assert hiç çalışmaz · `matching.test.ts:176-184` `?minMatchScore=95` isteniyor ama assert `toBeGreaterThanOrEqual(90)` (**eşikle uyumsuz**) ve `forEach` ⇒ boş listede boşuna geçer. ⭐ **KURAL 14'ün ders kitabı örneği** (CI yeşil ≠ test koştu). ⚠️ PS-07 ile **aynı dosyalar → SIRALI/tek PR**. = psikometri konseyi §5 D.4 |
| PS-09 | Ş0 | **`scoring.test-cases.ts` — formülün en iyi koruması `npm test`'in DIŞINDA.** vitest kapsamına alınmalı. | 🟡 | (iç kalite — formül regresyonu artık CI'da yakalanıyor) | BEKLIYOR | 🟡 KALIR: dosya **`src/services/` altında ve scoring ailesinden** (`scoring.test-cases.ts`) — taşıma/dönüştürme skorlama dosya ailesine dokunur ⇒ *"belirsizse 🟡"*. Kanıt (rapor): içerik kova-B malzemesi (`:49-64` sektör · `:69-89` matris+vektör harmanı **elle hesaplı** · `:95-119` toplam · `:124-130` anti-match) ama `console.log` + `process.exit(1)` betiği. Tek çağıran `package.json:17` `test:scoring`. `vitest.config.ts:30` `include: ['tests/**/*.test.ts']` **kapsamıyor**, `:33` `exclude`'da. Kaynak kodda **0 import**. ⚠️ Dönüştürürken **beklenen değerler değiştirilmez** — değişirse formül sessizce kayar. = psikometri konseyi §5 D.4 |
| PS-10 | Ş1 | **Menti "uygun mentor yok" mesajında kendi profilini suçluyor** — "kurumda hiç mentör yok" ile "eşleşen yok" ayrımı yapılmıyor. | 🟡 | Kurumda hiç mentör yokken menti **doğru sebebi** okuyor ve gereksiz yere DISC testine gönderilmiyor | BEKLIYOR | ⚠️ **KAPI SAPMASI — rapor 🟢 demişti, 🟡'ye ÇEKİLDİ.** Gerekçe: ayrımı yapmak için "kurumda mentör var mı" bilgisi gerekiyor; FE'de hazır kaynak **VAR** (`menti/page.tsx:62` `mentorCountData`, `:205` `count >= 3` — bu turda FE'de doğrulandı) **AMA** bu sayı **P-00 ile backend'de k-anonim maskeleniyor** (N<3) ⇒ **0 ile 1-2 ayrımı maskeleme davranışına bağlı** ⇒ KVKK/k-anonimlik istisnasına (istisna 2) temas ediyor. ⚠️ **TEYİT GEREK:** `mask.ts` N<3'te ne döndürüyor (null mu 0 mı) — backend submodule boş, okunamadı. Maskelenmiş değerden ayrım yapılamıyorsa **backend'e boolean bayrak** gerekir → o zaman kesin 🟡. Kanıt (rapor): `matching.ts:382,406` ikisi de `{items: []}` · `matchingController.ts:114` HTTP 200; (FE, doğrulandı) `menti/page.tsx:276-287` tek metin + `/disc-test` linki. ⚠️ **ÇIKIŞSIZ DÖNGÜ:** gönderildiği `/disc-test`, havuzu tamamlamış kullanıcıyı 2,5 sn sonra geri atıyor ⇒ **KARAR-?? (DISC tekrar testi)** cevaplanmadan bu döngü tam kapanmaz. = psikometri konseyi §5 D.6 |
| PS-11 | Ş1 | **`/onboarding` adım 2 boş soru listesinde SONSUZ SPINNER** + ilerleme yüzdesi `NaN`. | 🟢 | (savunma — bugün tetiklenmiyor; backend boş liste dönerse kullanıcı sonsuza kadar dönen ikonda takılmıyor, anlamlı metin görüyor) | BEKLIYOR | 🟢 **yalnız FE**, matching/KVKK/migration değil. Kanıt **bu turda FE'de doğrulandı**: `app/onboarding/_OnboardingContent.tsx:209-212` — `questions.length === 0` dalı **spinner** çiziyor, "yükleniyor" ile "boş" ayrımı yok (ayrı `loading` state yok, başlangıç `[]` `:96`) · `app/onboarding/_steps/DiscTestStep.tsx:116` `Math.round((currentIndex / total) * 100)` → `total = 0` ⇒ **`NaN`**. ⚠️ Bugün tetiklenmez (sorular kodda sabit — `onboardingController.ts:109-190`, rapor). ⭐ **Emsal kodda hazır:** `/disc-test` bunu **doğru** yapıyor (`disc-test/page.tsx:94-96` `DiscTestEmpty`). ⚠️ **U-17 komşusu** (havuzun nasıl dolacağı) ama **aynı iş değil**: U-17 seed tarafı (🟡, efor L), bu satır FE savunması. = psikometri konseyi §4 C.1 |

---

## AŞAMA IC — İÇERİK KONSEYİ (2026-09-21)

> Kaynak: `docs/raporlar/kesif/konsey-icerik-2026-09-21.md`. Önek **IC-** (raporun `C-??` öneki yerine).
> ⚠️ AŞAMA I ile **örtüşen** bulgular buraya YENİ satır olarak girmedi — ilgili `I-xx` satırının Not'una
> *"= içerik konseyi §…, ek bulgu: …"* biçiminde eklendi (I-01 · I-03 · I-10 · I-15 · I-16 · I-17 · K-18).
> Kapı dağılımı: 🟢 10 · 🟡 2 (IC-06, IC-08 — ikisi de auth) · 🔴 2 (IC-13, IC-14). Migration/seed gerektiren **yok**.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| IC-01 | Ş0 | **Mentör panelindeki 4 İngilizce DISC etiketi Türkçeleşsin — ve 5 ayrı DISC sözlüğü tek kaynağa insin.** 2026-09-09 kullanıcı testi bulgusunun doğrudan kaynağı. | 🟢 | Mentör, "Engellenecek DISC Profilleri" filtresinde İngilizce yerine Türkçe DISC adı görüyor | BEKLIYOR | Kaynak: içerik konseyi §6.1①. `mentor/page.tsx:27-30` (`'D — Dominant'`…), render `:403` `.map` → `:412`; ayrıca `DiscRecallCard.tsx:80` · `ResultStep.tsx:69` "Dominant" basıyor. ⚠️ **Kalıcı çözüm tek kaynaktır** — aynı 4 boyut için **5 uyuşmayan sözlük** var: `DiscBadge.tsx:13-16` + `profile/page.tsx:30-33` (Öncü/Ateşleyici/Yapı Taşı/Kâşif) · `types/discTest.ts:134-140` (Kararlılık/Etki/Denge/Titizlik) · `mentor/page.tsx:27-30` (İngilizce) · BE `analyticsEngine.ts:67-70` (karma). Tek kaynağa inilmezse **6. sözlük doğar**. ⚠️ `F-28` (sayfa metni merkezileştirme) ile aynı yöne bakar → SIRALI değil ama desen ortak. Efor S |
| IC-02 | Ş0 | **Davet şablonunda tek cümlede iki yazım ("mentörlük … mentor").** Yöneticinin kopyalayıp kurum DIŞINA gönderdiği metin. | 🟢 | Davet metninde tek ve tutarlı "mentör" yazımı | BEKLIYOR | §6.2. `admin/invite/page.tsx:57` (ve `:79`) *"sizi mentörlük programına **mentor** olarak davet etti"* — birebir teyitli. Aynı-ekran çakışması 4 ekranda daha: `mentor/page:45,156,181` · `menti/page:173,251` · `meetings/page:36,198` · `menti/agreement/[id]:90,98`. Oran: `mentör` ~130 / `mentor` ~40. ⚠️ `U-03` (davet ekranı sessizliği) ile **aynı dosya** → SIRALI. Efor S |
| IC-03 | Ş0 | **12 ham-enum render noktası mevcut sözlüklerden geçsin** (rol · etiket durumu · görüşme formatı · sertifika durumu · log seviyesi · "Journey" başlığı) **+ 2 e-posta şablonundaki ham rol enum'u.** | 🟢 | Yönetici/platform ekranlarında ve e-postada `MENTOR`, `IN_PERSON`, `COOLDOWN` gibi ham değer yerine Türkçe karşılık | BEKLIYOR | §6.1②④. Yüksek etkili: `admin/waiting-room/page.tsx:133` · `PendingUserCard.tsx:78` (`MENTOR`/`MENTI`) · `admin/tags/page.tsx:111` (`APPROVED`/`MERGED`/`REJECTED`). Orta: `MeetingsTable.tsx:66` · `MembersTable.tsx:92,72`. Düşük: `platform/dashboard/page.tsx:263,264,566`. ⚠️ `MembersTable:92` için **sözlük ZATEN VAR ama kullanılmıyor** (`sertifika-sonuclari/page.tsx:37-41` aynı enum'u tam Türkçe çeviriyor). E-posta ayağı BE: `emailService.ts:110,165`; FE'de sözlük var (`TenantSwitcher.tsx:32-33`), BE'de yok. ⚠️ `M1`/`m1` kodları bu satıra DAHİL DEĞİL → **IC-14** (önce ad↔kod kararı gerek). Efor S-M |
| IC-04 | Ş0 | **Sertifika sonuç ekranı YANLIŞ SEBEP gösteriyor.** Backend 4 eleme sebebi döndürüyor, ekran yalnız 1'ini tanıyor. | 🟢 | Kriz/kritik konudan elenen mentör gerçek sebebi görüyor — %80 üstü puan almışken *"Sertifika için en az %80 gerekli"* yazmıyor | BEKLIYOR | §6.4. BE 4 sebep: `certification.service.ts:150,179,216` (`COOLDOWN_ACTIVE`·`NO_ACTIVE_TOPICS`·`RED_LINE_FAILED`·`BELOW_THRESHOLD`) ↔ FE tipi yalnız `'BELOW_THRESHOLD'` (`types/certification.ts:64`) ↔ ekran `failReason`'ı **hiç okumuyor** (kapsam: `frontend/src` tüm `.tsx` → tek isabet bir test dosyası); basılan metin `mentor/certification/page.tsx:205-209`. ⛔ **Sessiz yanlış** (T3 adayı — kimse fark etmez, sistem "çalışıyor" görünür; PO bloker ilan ederse ⛔ işareti eklenir). ⚠️ **`I-03` ile AYNI EKRAN ve aynı blok → SIRALI**; PO isterse tek PR'da birleşir. Efor S |
| IC-05 | Ş0 | **Backend'e global Türkçe Zod `errorMap`.** ~75 mesajsız kısıt bugün İngilizce varsayılan basıyor. | 🟢 | Form hatalarında *"String must contain at most 1000 character(s)"* yerine Türkçe mesaj | BEKLIYOR | §6.1⑤. Kapsam beyanı: BE `src`, `errorMap|setErrorMap|zod-i18n` → **0 sonuç** (global errorMap yok). Sızma yolu: `client.ts:26-34` backend `fieldErrors`'ından ilk mesajı ekrana basıyor; dosyanın kendi yorumu *"(zaten Türkçe)"* — **bu varsayım yanlış** (ör. `userController.ts:332,342` · `agreementController.ts:30`). **Tek mimari hamle ~75 kısıtı birden kapatır.** ⚠️ `Y-03` (madde 47, doğrulama bloğunun merkezileştirilmesi) ile **aynı dosya ailesi** → SIRALI; Y-03 "biçim", IC-05 "mesaj dili" — ikisi farklı kök. Efor M |
| IC-06 | Ş0 | **Giriş hatası hesabın varlığını sızdırıyor.** | 🟡 auth | Kayıtlı ve kayıtsız e-posta için aynı yanıt | BEKLIYOR | §4.2 H-12. `_LoginContent.tsx:14` *"Bu e-posta başka bir yöntemle kayıtlı."* ↔ projenin **kendi** enumeration-safe kuralı `registerMessages.ts:8-11`. 🟡 KALIR: auth dosyası. Efor S |
| IC-07 | Ş0 | **20 jenerik hata mesajının 5'inde backend'in gerçek hatası tamamen yutuluyor.** | 🟢 | Kullanıcı "İşlem başarısız" yerine gerçek sebebi görüyor | BEKLIYOR | §6.4. Ortak fallback `useMutation.ts:45` *"İşlem başarısız."*; `res.error` hiç okunmayan noktalar: `bildir/page.tsx:31` · `admin/certification/page.tsx:64`. *"Bir hata oluştu"* birebir kalıbı 5 noktada. ✅ 500 yolu temiz (`errorHandler.ts:24` iç detay sızdırmıyor) — bu iş yalnız FE tarafı. ⚠️ Ayrıca teknik alan adı sızıyor: `authController.ts:607` *"tenantSlug zorunlu"*. Efor S |
| IC-08 | Ş0 | **Onay bekleyen kullanıcıya "düzeltme notu" uygulama içinde hiç gösterilmiyor.** | 🟡 auth | Onay bekleyen kullanıcı neyi düzelteceğini ekranda okuyor (mail kapalı olsa da) | BEKLIYOR | §6.4. Not yazılıyor: `adminController.ts:703-706` → `rejectionReason`; ama PENDING 403 yanıtı bunu **döndürmüyor** (`authController.ts:323-327`) ve `/pending-approval` göstermiyor → tek kanal e-posta; SMTP kapalıysa **hiç**. 🟡 KALIR: auth yanıt gövdesi değişiyor. ⚠️ `U-07` (pending-approval'da e-posta boş) ile **aynı ekran** → SIRALI. Efor S |
| IC-09 | Ş0 | **Kuruma gönderilen düzeltme şablonlarının tonu suçlayıcı.** 5 metin emir kipinde, e-postayla kullanıcıya gidiyor. | 🟢 | Kullanıcı azarlanmadan ne yapacağını okuyor | BEKLIYOR | §6.4. `types/admin.ts:167-173` *"Uzmanlık etiketleriniz **çok geneldir**. Daha spesifik **belirtiniz**."* · *"Biyografi bölümünüz **yetersiz**…"* Render yolu teyitli: `CorrectionNoteDialog.tsx:55` → `adminController.ts:683-726` → **e-posta**. Ürünün kendi ton kuralı zaten yazılı (`TenantCorrectionBanner.tsx:10`) ve ≈286 mesajda "yanlış yaptınız" kalıbı **0** — bu 5 metin istisna. Metin = teknik karar (CLAUDE.md), PO'ya sorulmaz. Efor S |
| IC-10 | Ş1 | **madde 139'un eksik yarısı: 4 menti "şimdilik" varyantı YAZILSIN.** Bugün yalnız mentör tarafı yazılı (4/8). | 🟢 | (ön koşul işi — kullanıcı etkisi `I-15` ile birlikte görünür: çoklu-arketip çıkan **menti** de "şimdilik" metnini okur) | BEKLIYOR | §0② · §2.1. Belge `arketip-ve-yaklasim-icerigi-2026-09-03.md:269` menti sürümü için **metin değil talimat** bırakmış; yazılı 4 varyant `:271,279,287,295` **yalnız mentör**. ⛔ **`I-15`'in ön koşulu** — I-15 bugün kodlanırsa menti tarafı boş kalır. Ayrı satır açıldı çünkü I-15 🔴 KARAR-10 kilitli, bu iş 🟢 ve ondan bağımsız ilerler. ⚠️ Eşik dalı yazılırken sihirli sayı YOK: eşik sabiti `scoring.config.ts:31` komşusuna. ⚠️ Ad seçimi ad↔kod kararına bağlı (kart §6#1). Efor S (yazım) |
| IC-11 | Ş0 | **Terim birleştirme: randevu ↔ toplantı ↔ görüşme.** Aynı `Meeting` kaydı dört farklı adla anılıyor. | 🟢 | Menti "randevu" gönderip mentörün "toplantı" alması bitiyor; tek terim | BEKLIYOR | §6.2. Menti *"Randevu Al / Randevu Talebi"* · mentör *"Toplantı Talepleri"* · e-posta *"Yeni Toplantı Talebi"* · menü *"Görüşmelerim"*; `meetings/page.tsx:154` **tek cümlede ikisi** (*"Tüm randevularınız ve geçmiş görüşmeleriniz"*). Noktalar: `book-meeting:102,209` · `mentor/page:251,261` · `emailService:74,170` · `meetings/page:154`. Sayım: `görüşme` 110 / `toplantı` 17 / `randevu` 9 / `buluşma` 5 → baskın **görüşme (%76)**. Terim seçimi **teknik karar** (CLAUDE.md "isimlendirme → sen karar ver"), PO'ya sorulmaz. Efor M |
| IC-12 | Ş0 | **İçerik belgeleri hijyeni** — `bolumler/` 5 belge indekse · 3 zayıf durum etiketi · bayat `backend/` yolları · faz6'nın aşıldığı notu · `CLAUDE.md`'deki yanlış `registerMessages` beyanı. | 🟢 | (belge işi — kullanıcı etkisi yok; sonraki turlar doğru belgeyi okur, bayat sürümü canlıya taşımaz) | BEKLIYOR | §5 D.2-D.3 · §9.1. `docs/raporlar/icerik/00-INDEKS.md:35` (`bolumler/` 5 belge tek tek listelenmiyor) · `:52` bayat `backend/prisma/...` yolu (bu çalışma alanında `menti-mentor-v2/backend` **boş** submodule; gerçek yol `menti-mentor/prisma/...`) · `:20` hâlâ faz6'yı *"20 sertifika senaryosu"* diye sunuyor, 2026-09-08 serisinin onu **aştığı** hiç yazmıyor · 3 oturum belgesinin `:3` satırı *"📸 Üretim: …"* diyor ama **"DONDURULMUŞ"/"YAŞAYAN" sözcüğü yok** · `prisma/senaryo-bankasi-tam.md:3` kendini *"TAM TASLAK — kullanıcı onayı bekliyor"* sayıyor, oysa **canlı seed'in kaynağı** (`seed-certification.ts:7`). ⚠️ **Ek:** `CLAUDE.md` *"`registerMessages.ts` … dosya HENÜZ kodda YOK: grep boş"* diyor — **dosya VAR** (`frontend/src/lib/registerMessages.ts`, BE'de `REGISTER_MESSAGES` `authController.ts:125`) → aynı desenle damgalanmalı. ⛔ SİLME YOK, `⚠️ GÜNCELLEME` deseni. Efor S |
| IC-13 | Ş1 | **Menti tarafında kriz/kötü-muamele içeriği ve bildirim kanalı YOK.** | 🔴 KARAR-31 | Menti, kendisi ya da mentörü kaynaklı bir sorunda ne yapacağını ekranda okuyor ve nereye başvuracağını biliyor | BEKLIYOR | §4.3. `menti/orientation-guide/page.tsx:17-63` dört senaryo içeriyor, **hiçbiri** kriz/kötü muamele/sınır ihlali değil. Menti *"sadece konuşacak biri lazım"* diyebiliyor (`threeQuestionsText.ts:30`) ama bu yalnız eşleştirme sinyali, hiçbir destek çerçevesine bağlanmıyor. Tek "bildir" kanalı kriz için değil: `/bildir` kapsamı sahte kurum kaydı/yetkisiz davet (`bildir/page.tsx:55`). ⚠️ **`I-18`/KARAR-31'in MENTİ ayağı** — I-18 yalnız "kurum yöneticisine bildirim" tarafını kapsıyor. ⚠️ KARAR-4 (somut destek kaynağı adı) buraya da bağlı: ürün içindeki tek somut kaynak adı *"okul psikoloğu"* (`seed-learning-journey.ts:196`) — yetişkin menti/STK için karşılığı yok. Efor M |
| IC-14 | Ş0 | **`M1`/`m1` ham arketip kodları yöneticiye çıplak görünüyor.** | 🔴 ad↔kod kartı (§6#1) | Yönetici eşleşme tablosunda kod yerine arketip adı görüyor | BEKLIYOR | §6.1②. `admin/eslesmeler/page.tsx:134,142` (`{match.mentorArchetype}`) — **hiçbir sözlük yok**, yönetici çıplak `M1`…`m4` görüyor. Sözlük yazılabilmesi için **hangi ad hangi koda** kararı şart: kodun tanıdığı değerler `disc-to-ocean.adapter.ts:27-43` · `scoring.config.ts:33-44` · `schema.prisma:997`; yeni 8 ad için **0 eşleme** (kapsam: `docs/**/*.md` + BE `src`/`prisma`, 8 ad × 8 kod, harf duyarsız → tek isabet, o da **eski** adlarla `03-psikometri-ve-algoritma.md:14-15`). ⚠️ `IC-03`'ten AYRI tutuldu: diğer 12 enum'un sözlüğü bugün yazılabilir, bu yazılamaz. Efor S (karar sonrası) |

---

## AŞAMA YN — YÖNETİŞİM KONSEYİ (2026-09-21)

> Kaynak: `docs/raporlar/kesif/konsey-yonetisim-2026-09-21.md`. Önek **YN-** çünkü `Y-` zaten yol haritası
> devri için kullanılıyor (AŞAMA Y).
> ⚠️ **YN-01 ve YN-02 bu turda KISMEN KAPANDI:** CLAUDE.md bölme KADEME 1 bu PR'da uygulandı ve
> **commit edildi** (47.456 → 34.742 karakter, pay 258). YN-01'in "commit edilmemiş" uyarısı bu PR ile düştü;
> **Kademe 2 açık kalıyor.** YN-02'nin doğurduğu KURAL 8 mükerreri de bu turda oluştu ve AÇIK.

| # | Şerit | İş | Kapı | Bitti demek | Durum | Not |
|---|---|---|---|---|---|---|
| YN-01 | Ş0 | ⭐ **CLAUDE.md bölme KADEME 1 diskte uygulanmış ama COMMIT EDİLMEMİŞ + pay yalnız 258 karakter.** `git status` → ` M CLAUDE.md` · ` M belge-duzeni-rehberi.md` · `?? rtk-komut-rehberi.md`. Bulut oturumu yalnız **commit edilmiş** dosyaları görür → bir sonraki tur eski CLAUDE.md'yi okur. | 🟢 | Ajan oturum başında boyut uyarısı almıyor ve bir sonraki tur da almıyor | BEKLIYOR | §A.3 · E-11. **Denetim GEÇTİ (bu turda doğrulandı):** 47.456 −8.103 −5.020 +408 = 34.741 ↔ gerçek 34.742 · 0 satır kaybı · KURAL 8-16 başlıkları 9/9 hedefte. ⚠️ **Pay 258 karakter ≈ 3 satır** — rapor 1.141 öngörmüştü; fark BC'nin +640'ı. ⇒ **Kademe 2 (§A.3 `:87-94`, → 28.715) aynı turda yapılmalı**, yoksa ilk eklemede sınır aşılır. ⛔ `rtk-komut-rehberi.md` commit edilmezse RTK rehberi **tamamen kaybolur**. |
| YN-02 | Ş0 | ⭐ **Taşıma iki YENİ tutarsızlık doğurdu (bu turda oluştu).** (a) `belge-duzeni-rehberi.md`'de **KURAL 8 artık İKİ KEZ** var: `:99-109` (kendi gövdesi) + `:143-152` (CLAUDE.md'den taşınan kopya). (b) `rehber:6` hâlâ *"bu **6** kurala uyar"* diyor — dosyada artık **18 kural bloğu** var; `rehber:3` künyesi *"Son güncelleme: **2026-08-23**"*. | 🟢 | Rehberi okuyan her kuralı bir kez ve tam görüyor; künye dosyanın gerçek hâlini söylüyor | BEKLIYOR | §A.7 AM-3 · §B.1 · §B.4-1. ⚠️ **Rapor B.4-1 "mükerrer çözülür" diyordu (`:337`, kazanç 933) — mükerrer ÇÖZÜLMEDİ, tek dosyanın içine TAŞINDI.** Rehberin kendi KURAL 1'i (tek gerçek kaynağı) kendi dosyasında ihlal oluyor. ⛔ Gövde SİLİNMEZ → biri `## GEÇMİŞ`e veya `~~[ESKİ]~~` damgasıyla. Sayım hatası raporda da vardı: `rehber:6` "6" · eski `CLAUDE.md:377` "8" · gerçek 18 (§B.1 `:276`). |
| YN-03 | Ş4 | **`rtk-komut-rehberi.md` hiçbir indekse kaydedilmedi (KURAL 5 borcu) + `docs/otonom/` hâlâ hiçbir klasör tanımı belgesinde yok.** `grep -n "rtk" docs/kararlar/00-INDEX.md` → **0**. `grep -ci otonom docs/kararlar/00-INDEX.md` → **0**. | 🟢 | INDEX'e bakan yeni RTK rehberini ve `docs/otonom/` klasörünü görüyor | BEKLIYOR | §C.1 · E-13. **= F-01 ile kesişir** (F-01 Not'una çapraz atıf eklendi). Yeni dosya açılmasının kendi kuralı ihlal edildi: `rehber:69-74` KURAL 5. |
| YN-04 | Ş0 | ⭐ **Güvenlik kuralı yanlış kanıta dayanıyor — 24 gündür.** `CLAUDE.md:442` aynen: *"`registerMessages.ts` örnek addır, dosya HENÜZ kodda YOK: grep boş"*. **Dosya VAR:** `frontend/src/lib/registerMessages.ts` (bu turda `ls` ile doğrulandı). | 🟢 | Güvenlik kuralının gerekçesi kod gerçeğiyle uyuşuyor | BEKLIYOR | §B.3-1 · E-3. ⚠️ Rapor `:519` diyor — **bölme sonrası satır `:442`'ye kaydı** (rapor BB numarası). Desen: `~~[ESKİ · 2026-08-28] …~~` + `⚠️ GÜNCELLEME (2026-09-21): dosya var — kanıt: frontend/src/lib/registerMessages.ts`. F-28 ile aynı DOSYA adı ama **farklı iş** (F-28 = metin merkezileştirme). |
| YN-05 | Ş0 | ⭐ **Canonical rehber, DONDURULMUŞ belgeyi canonical gösteriyor.** `belge-duzeni-rehberi.md:13` *"Canonical'lar: … iş kuyruğu → `10-yol-haritasi.md`"*; o belge `:4`'te **📸 DONDURULMUŞ (2026-09-21)**. Rehberi okuyan ajan ölü belgeye yönlendiriliyor. | 🟢 | Rehberi okuyan canlı kuyruğa (`00-KUYRUK.md`) yönleniyor | BEKLIYOR | §B.3-2 · E-2. KURAL 7 tablosundaki `10-yol` ve `00-CIKIS-PLANI` satırları da aynı durumda. Karşı kanıt: `CLAUDE.md § AKTİF İŞ KAYNAĞI TEKTİR` — *"Aktif iş kaynağı tektir: `docs/otonom/00-KUYRUK.md`"*. |
| YN-06 | Ş4 | **2 belge künyesinde 🔄 YAŞAYAN diyor ama ölü** — `kararlar/konu/08-acik-sorular.md:5` *"canonical açık-karar takibi artık `00-KARAR-TAKIP.md`"* · `raporlar/icerik/kod-kalemleri-2026-09-03.md:8` *"✅ NUMARALANDI — madde 138-160'a işlendi"*. İkisi de devir kanıtını kendi içinde taşıyor. | 🟢 | Okuyan bayat belgeyi güncel sanmıyor | BEKLIYOR | §D.1 · E-1. **Hazır dondurma notları raporda `:381-388`** (kopyala-yapıştır). Künyedeki `**🔄 YAŞAYAN**` → `~~[ESKİ · 2026-09-21] **🔄 YAŞAYAN**~~`. Diğer 2 "ölü adayı" PO kararına bağlı (kart §6). |
| YN-07 | Ş0 | **Otonom sözlük çelişkisi — motorun her tur okuduğu üç dosya üç farklı set sayıyor, ikisi "başkası YASAK" diyor.** `OTONOM-PROMPT.txt:80-81` 7 kod (`PR-ACIK` dahil) · `00-KUYRUK.md:53-54` 6 kod (`PR-ACIK` **yok** ama gövdede F-19'da kullanılıyor) · `CLAUDE.md § KURAL 10` 6 tamamen farklı kod. Ayrıca prompt `:148-150` **"AŞAMA K"** sayıyor, kuyrukta `grep -c "AŞAMA K"` = **0**; kuyruktaki `A·B·C·D` promptta yok. | 🟢 | Motor her turda tanımlı kod setiyle çalışıyor; aşama listesi 11 = 11 | BEKLIYOR | §D.3 Ç-1/Ç-4 · E-4. `OTONOM-PROMPT.txt:80-84` canonical (en yeni); kuyruk ona hizalansın. İki sözlüğe **"hangi belge için geçerli"** cümlesi eklenmeli. |
| YN-08 | Ş0 | **Mod etiketi 2 mi 3 mü + şerit sistemi promptta hiç yok.** `CLAUDE.md § MOD ETİKETİ` tablosu **2 mod** (🟥/🟩) ↔ `§ Çalışma Sözleşmesi` **3 mod** (PLAN/BYPASS/**MANUEL-ONAY**, renk yok). `00-KUYRUK.md:58-66` 5 şerit (Ş1-Ş4 + **Ş0**) ↔ `CLAUDE.md` "en fazla 4 şerit" ↔ `OTONOM-PROMPT.txt`'te şerit **hiç geçmiyor**. | 🟢 | Üç dosya aynı mod ve şerit tanımını veriyor | BEKLIYOR | §D.3 Ç-2/Ç-3 · E-5. Mod tablosu canonical; üçüncü mod ya `~~[ESKİ]~~` ya tabloya renk koduyla girsin. Şerit: kuyruk canonical (dosya sahipliği orada), prompta tek satır atıf, CLAUDE.md "4" → "4 paralel + Ş0 sıralı". |
| YN-09 | Ş0 | **1.000+ karakterlik satırlar AZALMADI, ARTTI** — `00-KARAR-TAKIP.md` 30→**33**, `00-KUYRUK.md` 2→**5**; en uzunu `00-KARAR-TAKIP.md:858` = **4.994 karakter**. Kural (`CLAUDE.md § tarihsel iz satırın İÇİNDE tutulmaz`) ve `## GEÇMİŞ` altyapısı **zaten var**, 3 kalemde uygulanmış, 38'ine uygulanmamış. | 🟡 | Kuyruk ve karar-takip satırları ekranda tek bakışta okunabiliyor | BEKLIYOR | §D.2 · E-8/E-9. ⛔ **`kalan + taşınan = önceki` denetimi ZORUNLU, kapanış raporunda SAYIYLA.** ⚠️ `00-KUYRUK.md`'de `## GEÇMİŞ` bölümü **YOK** (`grep` → 0), önce açılmalı. ⚠️ `CLAUDE.md`'deki ölçüm bayat: *"74 satır, en uzunu 6.460"* → bugün 80 / 4.994. 🟡: motorun her tur okuduğu dosyaya dokunuyor. |
| YN-10 | Ş0 | **`CLAUDE.md:<satır>` atıfları satır numarasına dayanıyor — bölme bugün numaraları kaydırdı.** Yaşayan belgelerde 15 atıf var; **1'i taşınan bloğa bakıyordu ve zaten yanlıştı** (`00-KUYRUK.md:302` → `:453`). Kalan 14'ü bugün doğru ama her düzenlemede kırılır. 📸 belgelerdeki ~190 atıf **zaten kırık**. | 🟢 | Atıflar taşıma/bölmeden sonra da doğru yere gidiyor | BEKLIYOR | §A.5 · E-7. **Çözüm satır numarası değil BÖLÜM ADI:** `CLAUDE.md § MERGE POLİTİKASI`. ⛔ 📸 belgelere **DOKUNMA** (KURAL 6). Düzeltilecek tam liste bu raporun §2a/§2b tablosunda. ⚠️ Rapor `:470` aynı hastalığın kendi düzeltmesinde tekrarlandığını gösteriyor (`CLAUDE.md:179` yine bayat numara üretmiş). |
| YN-11 | Ş4 | **`raporlar/` 91 dosyadan 5'i hiç etiketsiz, 3'ü zayıf etiketli (KURAL 3 ihlali).** En yanıltıcısı `bilanco/bilanco-po-ozet-2026-08-26.md` — **PO'ya hitap eden özet**, 25 gün eski, hiç etiketi yok; ayrıca `:22` "196" diyor, doğrusu 259. `panel/00-INDEX.md` ve `persona/00-INDEX.md` de etiketsiz. | 🟢 | Her rapor "güncel mi dondurulmuş mu" sorusunu kendi başlığında cevaplıyor | BEKLIYOR | §C.5. Zayıf 3: `icerik/sertifika-oturum…` dosyalarında 📸 "üretim tarihi" anlamında kullanılmış → belirsiz. ❓ `G3-icerik.md` TEYİT GEREK (başlık 📸 ama `:172`'de 2026-09-02 düzenlemesi var). |
| YN-12 | Ş4 | **İndeks adı 4 ayrı kalıpta yaşıyor** (`00-INDEX.md` · `00-INDEKS.md` · `00-icerik-index.md` · `00-KART-INDEKSI.md`) — kuralın kendi koruma komutu `ls <klasör>/00-IND*` (`rehber:46`) **üçüncüyü yakalamıyor** (önek `00-ic…`). **13 klasörde hiç indeks yok.** | 🟢 | Tek desenle her klasörün giriş noktası bulunuyor | BEKLIYOR | §C.3 · C.4. ❓ **TEYİT GEREK:** `00-icerik-index.md` kasıtlı mı, gözden mi kaçtı. Ayrıca `raporlar/bilanco/kararlar/` 5 adet `00-` dosyayla **iki farklı giriş sinyali** veriyor. |
| YN-13 | Ş0 | **Kişi adı yasağı kendi dosyasında ihlal ediliyor.** Kural `CLAUDE.md § Kişi Adı Yasağı` *"Hiçbir kod/yorum/commit/PR/belgeye kişi adı YAZMA"*; ihlal **aynı dosyanın 279 satır yukarısında** (`CLAUDE.md:8`) + `00-KUYRUK.md:2` + toplam **13 dosyada 15 geçiş**. Kuralın kendi istisnası (*"ayrı bir temizlik işinde giderilir"*) → iş **hiç açılmadı**. ⚠️ **Repo PUBLIC.** | 🟡 | Public repoda kişi adı geçmiyor | BEKLIYOR | §B.3-4. ⚠️ KVKK metinlerindeki **4 geçiş yasal zorunluluk**, hariç (G9-14 "DOKUNULMADI" kararı). 🟡: KVKK dosyalarına komşu. **Bu satır ve bu rapor ad listesi ÜRETMEZ** — `grep` PO'nun elinde. |
| YN-14 | Ş0 | **Kalan kural birleştirmeleri ≈4.494 karakter — yeni kural YOK, hiçbir kural kaldırılmadan.** Bölme B.4-1/2/8'i zaten yuttu; kalan B.4-3/4/5/6/7/9/10. En büyüğü **DB/ortam dörtlüsü** — içinde **birebir aynı ~530 karakterlik ÇELİŞKİ paragrafı İKİ KEZ** var. | 🟡 | Aynı kural iki yerde yazmıyor; okuyan tek yerden okuyor | BEKLIYOR | §B.4 · E-10. ⚠️ **Toplamları TOPLANMAZ** (rapor `:350`) — bölme sonrası kalan ek kazanç ≈4.494 → CLAUDE.md ≈**30.200**. 🟡: kural gövdelerine dokunuyor, gözden geçirme ister. ⛔ **Hiçbir kural gövdesi silinmez**, birleşir/taşınır. |
| YN-15 | Ş0 | **🟡 kapısının üç istisnası `CLAUDE.md`'de yok.** `OTONOM-PROMPT.txt` ve `00-KUYRUK.md:14-19` üç istisnayı (migration/seed · hassas dosya · geri dönülmez canlı dokunuş) sayıyor; `CLAUDE.md:44` yalnız *"riskli/geniş"* diyor. CLAUDE.md'yi tek başına okuyan 🟡'nin sınırını bilemez. | 🟢 | CLAUDE.md tek başına okunduğunda kapı kuralı eksiksiz | BEKLIYOR | §D.3 granülerlik boşluğu · E-6. ⚠️ **+~200 karakter, BİLİNÇLİ büyütme** — YN-01'deki 258 karakterlik payı **neredeyse tüketir** ⇒ **Kademe 2'den SONRA yapılmalı**, önce değil. |

---

## Ajan kuyruğa iş EKLEMEZ
Çalışırken "şu da yapılmalı" görürse `01-KARARLAR.md`'ye **KUYRUK ADAYI** olarak yazar. PO ekler.
